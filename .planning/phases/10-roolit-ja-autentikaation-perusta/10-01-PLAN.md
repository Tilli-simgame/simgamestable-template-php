---
phase: 10-roolit-ja-autentikaation-perusta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrate_roles.sql
  - database/schema.sql
  - public/src/includes/helpers.php
  - public/admin/login.php
  - public/admin/ei-oikeutta.php
autonomous: true
requirements: [ROLE-01, ROLE-02, ROLE-03]
user_setup:
  - service: mysql-phpmyadmin
    why: "Roolisarakkeet (role/is_active) on lisättävä admin_users-tauluun ennen kuin login.php ja sivukohtaiset gate-tarkistukset voidaan testata. Tässä koodikannassa ei ole automaattista migraatiokomentoa — migraatiot ajetaan käsin phpMyAdminin Import-toiminnolla (konventio: database/migrate_theme.sql)."
    dashboard_config:
      - task: "Aja database/migrate_roles.sql phpMyAdminissa (Import -> valitse tiedosto -> Suorita). Tämä lisää role- ja is_active-sarakkeet admin_users-tauluun ja nostaa olemassa olevan admin-tunnuksen role='admin'-arvoon."
        location: "phpMyAdmin (localhost:8080 Docker-kehitysympäristö) -> tietokanta -> Import"

must_haves:
  truths:
    - "admin_users-taulussa on role ENUM('admin','mod','author')- ja is_active TINYINT(1) -sarakkeet migraation ajon jälkeen"
    - "Olemassa oleva admin-tunnus on role='admin', is_active=1"
    - "Onnistuneen kirjautumisen jälkeen $_SESSION['admin_role'] sisältää käyttäjän roolin DB-rivistä"
    - "Deaktivoitu tunnus (is_active=0) ei voi kirjautua sisään ja saa saman geneerisen virheen kuin väärä salasana"
    - "currentRole() palauttaa session roolin, requireRole() ohjaa ei-oikeutta.php:hen kun rooli ei ole sallittu"
    - "admin/ei-oikeutta.php renderöityy admin_header.php-layoutilla ja tarjoaa Takaisin-linkin admin/index.php:hen"
  artifacts:
    - database/migrate_roles.sql
    - public/admin/ei-oikeutta.php
    - "helpers.php: requireRole(), currentRole(), isAdmin()"
  key_links:
    - "login.php kirjoittaa $_SESSION['admin_role'] vain DB-verifioidusta rivistä (password_verify + is_active=1)"
    - "requireRole() -> redirect() -> ei-oikeutta.php (redirect-kohde on oltava olemassa ennen kuin gatet käyttävät sitä)"
    - "currentRole() on ainoa paikka joka lukee $_SESSION['admin_role'] — Phase 11:n DB-recheck vaihtaa vain tämän funktion rungon"
---

<objective>
Rakenna Phase 10:n rooli-infrastruktuuri: tietokannan sarakkeet, keskitetyt rooligate-funktiot, kirjautumisen roolinkirjoitus + is_active-tarkistus, sekä "Ei käyttöoikeutta" -redirect-kohde. Tämä on koko vaiheen perusta — Plan 02 (sivugatet) ja Plan 03 (nav + salasananvaihto) riippuvat näistä funktioista.

Purpose: Ilman requireRole()/currentRole()-funktioita ja role-saraketta yksikään sivukohtainen suojaus ei voi toimia. Tämä plan luo ROLE-01:n (roolivarastointi), ROLE-02:n session- ja login-mekaniikan sekä ROLE-03:n redirect-kohteen.
Output: migrate_roles.sql, schema.sql-päivitys, helpers.php:n kolme uutta funktiota, laajennettu login.php, uusi ei-oikeutta.php.
</objective>

<artifacts_produced>
## Koko Phase 10:n tuottamat symbolit (viitteeksi kaikille planeille)

Tämä plan (01) tuottaa lihavoidut kohdat; muut planit tuottavat loput:

- **`helpers.php`: `requireRole(string ...$allowedRoles): void`** — vaatii kirjautumisen JA sallitun roolin (ROLE-02/03)
- **`helpers.php`: `currentRole(): ?string`** — palauttaa $_SESSION['admin_role'] tai null (ainoa lukukohta)
- **`helpers.php`: `isAdmin(): bool`** — oikotie currentRole() === 'admin'
- **`database/migrate_roles.sql`** — uusi migraatiotiedosto (role + is_active + admin-backfill)
- **`admin_users.role` ENUM('admin','mod','author')** ja **`admin_users.is_active` TINYINT(1)** — uudet sarakkeet
- **`$_SESSION['admin_role']`** — uusi session-avain (kirjoitetaan vain login.php:ssä)
- **`public/admin/ei-oikeutta.php`** — uusi ROLE-03-redirect-kohdesivu
- `public/admin/change_password.php` — uusi salasananvaihtosivu (Plan 03, AUTH-06)
- admin_header.php: roolikohtainen nav-näkyvyys + sidebar-footer salasananvaihtolinkki (Plan 03, ROLE-04)
- ~27 admin/*.php: `requireLogin()` -> `requireRole(...)` gate-swapit (Plan 02)
- 4 sekatiedostoa: inline `requireRole('admin')` -alagate delete-haarassa (Plan 02, D-02)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-roolit-ja-autentikaation-perusta/10-CONTEXT.md
@.planning/phases/10-roolit-ja-autentikaation-perusta/10-RESEARCH.md
@.planning/phases/10-roolit-ja-autentikaation-perusta/10-PATTERNS.md
@.planning/phases/10-roolit-ja-autentikaation-perusta/10-VALIDATION.md

# Muokattavat/analogilähteet
@database/migrate_theme.sql
@database/schema.sql
@public/src/includes/helpers.php
@public/admin/login.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Luo migrate_roles.sql ja päivitä schema.sql (ROLE-01)</name>
  <files>database/migrate_roles.sql, database/schema.sql</files>
  <read_first>
    - database/migrate_theme.sql (migraatiokonventio: kommenttiheader + phpMyAdmin-import-ohje + eksplisiittiset lauseet)
    - database/schema.sql rivit 245-252 (nykyinen admin_users CREATE TABLE — ei vielä role/is_active)
    - 10-PATTERNS.md "database/migrate_roles.sql (NEW)" -osio (rivit 212-257) — valmis migraatiorunko
  </read_first>
  <action>
    Luo uusi tiedosto database/migrate_roles.sql migrate_theme.sql:n konvention mukaisesti (kommenttiheader + "Aja phpMyAdminissa: Import" -ohje). Migraation sisältö per D-06/D-07: yksi ALTER TABLE admin_users joka lisää role ENUM('admin','mod','author') NOT NULL DEFAULT 'author' (AFTER username) ja is_active TINYINT(1) NOT NULL DEFAULT 1 (AFTER role), molemmat COMMENT-selitteineen. Sen jälkeen eksplisiittinen UPDATE admin_users SET role='admin' WHERE username='admin' — älä luota pelkkään DEFAULT-arvoon olemassa olevan tilin osalta (D-06). Päivitä LISÄKSI database/schema.sql:n admin_users CREATE TABLE -lohko (rivit 245-252) lisäämällä samat role- ja is_active-sarakkeet, jotta tuore asennus vastaa päivitettyä asennusta (sama pariutus kuin muilla migrate_*.sql/schema.sql -pareilla tässä projektissa). Käytä turvallisinta fallback-oletusta 'author' DEFAULTissa per D-06.
  </action>
  <verify>
    <automated>MISSING — tässä koodikannassa ei ole DB-assertointityökaluja (ei testikehystä, VALIDATION.md). Manuaali: aja migraatio phpMyAdminissa (user_setup), sitten `DESCRIBE admin_users;` näyttää role ENUM + is_active, ja `SELECT role, is_active FROM admin_users WHERE username='admin';` palauttaa 'admin' / 1.</automated>
  </verify>
  <acceptance_criteria>
    - migrate_roles.sql on olemassa, noudattaa migrate_theme.sql:n kommenttiheader-/import-ohjekonventiota
    - ALTER TABLE lisää role ENUM('admin','mod','author') NOT NULL DEFAULT 'author' JA is_active TINYINT(1) NOT NULL DEFAULT 1
    - Eksplisiittinen UPDATE ... SET role='admin' WHERE username='admin' on mukana
    - schema.sql:n admin_users-lohko sisältää samat kaksi uutta saraketta
  </acceptance_criteria>
  <done>migrate_roles.sql + schema.sql-päivitys valmiit; DESCRIBE admin_users näyttää role ENUM + is_active; admin-tunnus role='admin'/is_active=1 (ROLE-01)</done>
</task>

<task type="auto">
  <name>Task 2: Lisää requireRole()/currentRole()/isAdmin() helpers.php:hen (ROLE-02/03)</name>
  <files>public/src/includes/helpers.php</files>
  <read_first>
    - public/src/includes/helpers.php rivit 40-62 (redirect(), isLoggedIn(), requireLogin() — mallipohja ja uudelleenkäytettävä redirect)
    - 10-PATTERNS.md "helpers.php" -osio (rivit 23-115) — valmiit funktiorungot ja doc-comment-tyyli
  </read_first>
  <action>
    Lisää helpers.php:hen välittömästi requireLogin()-funktion jälkeen (rivin 62 jälkeen) kolme uutta funktiota mirroraten olemassa olevaa isLoggedIn()/requireLogin()-tyyliä ja tiedoston PHP 8.2 -tyypitettyä allekirjoituskonventiota: currentRole(): ?string joka palauttaa $_SESSION['admin_role'] ?? null; isAdmin(): bool joka palauttaa currentRole() === 'admin'; ja requireRole(string ...$allowedRoles): void joka ensin kutsuu olemassa olevaa requireLogin()-funktiota (ohjaa login.php:hen jos ei kirjautunut) ja sen jälkeen tarkistaa in_array(currentRole(), $allowedRoles, true) — jos rooli EI ole sallittu, kutsuu olemassa olevaa redirect(SITE_URL . '/admin/ei-oikeutta.php')-funktiota. ÄLÄ muuta tai poista olemassa olevaa requireLogin()/isLoggedIn()-funktiota — requireRole() käyttää requireLogin()-funktiota sisäisesti ja logout.php/login.php käyttävät requireLogin()-funktiota edelleen suoraan. currentRole() on suunniteltava koodikannan AINOAKSI paikaksi joka lukee $_SESSION['admin_role'] (Phase 11 vaihtaa vain tämän funktion rungon DB-recheckiin ilman ~30 kutsupaikan muutosta). Käytä deny-by-default -allow-listaa (in_array), ei deny-listaa.
  </action>
  <verify>
    <automated>MISSING — ei testikehystä (VALIDATION.md). Manuaali: `php -l public/src/includes/helpers.php` (syntaksitarkistus) palauttaa "No syntax errors". Toiminnallinen tarkistus tapahtuu Task 3:n login-testin ja Plan 02:n gate-testien yhteydessä.</automated>
  </verify>
  <acceptance_criteria>
    - helpers.php sisältää currentRole(): ?string, isAdmin(): bool, requireRole(string ...$allowedRoles): void
    - requireRole() kutsuu requireLogin() ensin, sitten in_array(currentRole(), $allowedRoles, true) -> redirect ei-oikeutta.php:hen jos ei salittu
    - Olemassa olevaa requireLogin()/isLoggedIn() ei ole muutettu eikä poistettu
    - `php -l` läpäisee ilman syntaksivirheitä
  </acceptance_criteria>
  <done>helpers.php sisältää currentRole()/isAdmin()/requireRole(); requireRole ohjaa ei-oikeutta.php:hen kun rooli ei sallittu; olemassa oleva requireLogin() ennallaan; php -l puhdas (ROLE-02/03)</done>
</task>

<task type="auto">
  <name>Task 3: Laajenna login.php (rooli + is_active) ja luo ei-oikeutta.php (ROLE-02/03)</name>
  <files>public/admin/login.php, public/admin/ei-oikeutta.php</files>
  <read_first>
    - public/admin/login.php rivit 19-33 (nykyinen SELECT + session-kirjoitus + virhehaara — exact-verifioitu 10-PATTERNS.md:ssä)
    - 10-PATTERNS.md "login.php" -osio (rivit 119-160) ja "ei-oikeutta.php" -osio (rivit 348-372) — valmiit diffit ja tiedostorunko
    - public/admin/settings.php (minimaalinen admin_header.php-shell + sivun sulkumarkkaus </div></div></body></html> vahvistettavaksi)
  </read_first>
  <action>
    login.php: Laajenna nykyinen kirjautumis-SELECT hakemaan myös role ja is_active (SELECT id, username, password, role, is_active FROM admin_users WHERE username = :username LIMIT 1). Laajenna onnistumisehto tarkistamaan LISÄKSI (int)$row['is_active'] === 1 password_verify()-tarkistuksen rinnalla. Onnistumishaarassa lisää olemassa olevien session-kirjoitusten (admin_logged_in, admin_id, admin_username) jälkeen uusi rivi $_SESSION['admin_role'] = $row['role']. ÄLÄ erottele virheviestissä "deaktivoitu tili" vs "väärä salasana" — pidä sama geneerinen "Väärä käyttäjätunnus tai salasana." -viesti molemmille (information-disclosure-esto). Säilytä kaikki muu tiedostossa muuttumattomana (CSRF-tarkistus, sanitize(), HTML-lomake, olemassa oleva session_regenerate_id(true)-kutsu).

    ei-oikeutta.php: Luo uusi tiedosto per D-04/D-05. require_once db.php, sitten requireLogin() (mikä tahansa kirjautunut rooli saa laskeutua tänne — tämä ON "olet kirjautunut mutta ei oikeutta" -sivu). Aseta $pageTitle = 'Ei käyttöoikeutta', require admin_header.php. Sisältö: .admin-card jonka sisällä .flash-err-luokalla kiinteä viesti "Sinulla ei ole käyttöoikeutta tähän sivuun." ja .btn-linkki jonka href on aina SITE_URL . '/admin/index.php' (Takaisin) — ei HTTP_REFERER-logiikkaa, ei rooli->etusivu-mappausta (D-05). Sulje sivu samalla </div></div></body></html>-markkauksella kuin muut admin-sivut. Käytä olemassa olevia .admin-card/.flash-err/.btn CSS-luokkia (ei uutta CSS:ää). Älä lisää sivua navigaatioon.
  </action>
  <verify>
    <automated>MISSING — ei testikehystä. Manuaali (migraation ajon jälkeen): `php -l public/admin/login.php` ja `php -l public/admin/ei-oikeutta.php` läpäisevät. Kirjaudu admin-tunnuksella -> onnistuu, ja väliaikaisella `UPDATE admin_users SET is_active=0 WHERE username='admin'` kirjautuminen estyy geneerisellä virheellä (palauta is_active=1 jälkeen). Avaa /admin/ei-oikeutta.php kirjautuneena -> sivu renderöityy Takaisin-linkillä.</automated>
  </verify>
  <acceptance_criteria>
    - login.php:n SELECT hakee role + is_active; onnistumisehto vaatii (int)is_active === 1; $_SESSION['admin_role'] kirjoitetaan onnistumishaarassa
    - Virheviesti pysyy geneerisenä molemmille epäonnistumistapauksille
    - ei-oikeutta.php renderöityy admin_header.php-layoutilla, kiinteä D-04-viesti, Takaisin-linkki aina admin/index.php:hen
    - Molemmat tiedostot läpäisevät `php -l`
  </acceptance_criteria>
  <done>login.php kirjoittaa $_SESSION['admin_role'] ja estää is_active=0-kirjautumisen geneerisellä virheellä; ei-oikeutta.php renderöityy redirect-kohteena Takaisin-linkillä; molemmat php -l puhtaita (ROLE-02/03)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selain -> login.php | Autentikoimaton käyttäjä lähettää käyttäjätunnuksen/salasanan; luottamaton syöte ylittää tässä |
| DB-rivi -> $_SESSION | Rooli/aktiivisuustila luetaan admin_users-rivistä ja kirjoitetaan sessioon vain onnistuneen autentikoinnin jälkeen |
| $_SESSION -> requireRole() | Rooli luetaan sessiosta pääsypäätöstä varten (palvelinpuolella, ei asiakaspuolella) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering / Elevation of Privilege | $_SESSION['admin_role'] | high | mitigate | admin_role kirjoitetaan täsmälleen kerran, vain login.php:ssä, ainoastaan password_verify()-täsmätystä DB-rivistä. Mikään muu koodipolku tässä planissa ei kirjoita kyseistä session-avainta — rooli ei ole asetettavissa pyyntösyötteestä (ASVS V4). |
| T-10-02 | Spoofing / Elevation of Privilege | login.php is_active-tarkistus | high | mitigate | Onnistumisehto vaatii (int)is_active === 1 password_verify()-tarkistuksen rinnalla — deaktivoitu tunnus ei voi kirjautua (välitön turvallisuushyöty, D-07). |
| T-10-03 | Information Disclosure | login.php virheviesti | low | mitigate | Sama geneerinen "Väärä käyttäjätunnus tai salasana." -viesti sekä väärälle salasanalle että deaktivoidulle tilille — ei paljasteta tilin olemassaoloa/tilaa autentikoimattomalle kutsujalle. |
| T-10-04 | Elevation of Privilege | currentRole() choke point | medium | mitigate | Roolivertailu keskitetään ainoastaan requireRole()/isAdmin()/currentRole()-funktioihin — ei hajautettuja $_SESSION['admin_role'] === 'Admin' -vertailuja joissa casing-/kirjoitusvirheitä. Deny-by-default in_array-allow-lista. |
| T-10-SC | Tampering | Pakettiasennukset (npm/pip/cargo) | low | accept | Ei mitään pakettiasennuksia tässä vaiheessa (ei Composeria, ei npm:ää) — natiivi PHP 8.2/MySQL. Ei supply-chain-pinta-alaa (vahvistettu 10-RESEARCH.md Package Legitimacy Audit). |
</threat_model>

<verification>
## Plan-tason verifiointi (manuaali — ei testikehystä, VALIDATION.md)

**Wave 0 -huomio:** Phase 10:ssä ei ole vielä oikeaa mod/author-tunnusta (luodaan vasta Phase 11:ssä). ROLE-03-testaus tapahtuu väliaikaisella role-flip-menettelyllä olemassa olevalla admin-tunnuksella (ks. Plan 02 verifiointi). Tässä planissa testataan vain login-polku ja infrastruktuuri.

1. Aja migrate_roles.sql phpMyAdminissa (user_setup) -> `DESCRIBE admin_users;` näyttää role + is_active; `SELECT role FROM admin_users WHERE username='admin';` = 'admin'.
2. `php -l` jokaiselle muokatulle PHP-tiedostolle -> ei syntaksivirheitä.
3. Kirjaudu admin-tunnuksella -> onnistuu, $_SESSION['admin_role']='admin' (varmennettavissa väliaikaisella tarkistuksella tai Plan 02:n nav-testillä).
4. Väliaikainen `UPDATE admin_users SET is_active=0 WHERE username='admin'` -> kirjautuminen estyy geneerisellä virheellä -> palauta is_active=1.
5. Avaa /admin/ei-oikeutta.php kirjautuneena -> sivu renderöityy, Takaisin-linkki vie admin/index.php:hen.
</verification>

<success_criteria>
- admin_users-taulussa on role ENUM + is_active-sarakkeet, admin-tunnus role='admin'/is_active=1 (ROLE-01)
- currentRole()/isAdmin()/requireRole() olemassa helpers.php:ssä ja php -l -puhtaita (ROLE-02/03)
- login.php kirjoittaa admin_role sessioon ja estää is_active=0 -kirjautumisen (ROLE-02)
- ei-oikeutta.php on olemassa ja renderöityy redirect-kohteena (ROLE-03)
</success_criteria>

<output>
Create `.planning/phases/10-roolit-ja-autentikaation-perusta/10-01-SUMMARY.md` when done
</output>
