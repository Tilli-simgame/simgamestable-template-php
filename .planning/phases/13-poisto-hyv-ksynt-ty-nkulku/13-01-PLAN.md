---
phase: 13-poisto-hyv-ksynt-ty-nkulku
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrate_delete_approval.sql
  - database/schema.sql
  - public/src/includes/helpers.php
autonomous: true
requirements: [DEL-05]
must_haves:
  truths:
    - "pending_deletions-taulu on olemassa dev-DB:ssä ja hyväksyy pending/approved/rejected-statuksia"
    - "foals/competitions/showrecords/posts-tauluissa on is_deleted + deleted_at -sarakkeet"
    - "insertPendingDeletion() ei luo toista pending-riviä samalle entity_type+entity_id-parille (DEL-05)"
  artifacts:
    - database/migrate_delete_approval.sql
    - "pending_deletions-taulu (entity_type, entity_id, requested_by, requested_at, status, reviewed_by, reviewed_at)"
    - "is_deleted/deleted_at -sarakkeet neljässä sisältötaulussa"
    - "insertPendingDeletion(string,int,int): void helpers.php:ssä"
    - "entityTypeToTable(string): string helpers.php:ssä"
  key_links:
    - "insertPendingDeletion() käyttää getDB()-yhteyttä ja check-then-insert-logiikkaa"
    - "entityTypeToTable() on whitelist-match(), ei dynaamista SQL:ää käyttäjän syötteestä"
---

<objective>
Rakenna poisto-hyväksyntätyönkulun tietokantaperusta ja jaetut apufunktiot: uusi `pending_deletions`-jonotaulu, soft-delete-sarakkeet neljään sisältötauluun, sekä `insertPendingDeletion()`- ja `entityTypeToTable()`-apufunktiot. Kaikki myöhemmät Wave 2 -planit (delete-haarautuminen, admin-hyväksyntänäkymä, audit-passi) nojaavat tähän.

Purpose: Ilman jaettua jonotaulua ja duplikaattiestoa poisto-hyväksyntää ei voi rakentaa (tutkimuksen Pitfall 8 + Pitfall 11). D-01 (jaettu polymorfinen taulu), D-02 (soft-delete-sarakkeet), D-03 (PHP-tason duplikaattiesto).
Output: `migrate_delete_approval.sql`, päivitetty `schema.sql`, kaksi uutta apufunktiota `helpers.php`:ssä, ja migraatio ajettuna dev-DB:hen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-poisto-hyv-ksynt-ty-nkulku/13-CONTEXT.md
@.planning/phases/13-poisto-hyv-ksynt-ty-nkulku/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: migrate_delete_approval.sql + schema.sql-peilaus + ajo dev-DB:hen</name>
  <files>database/migrate_delete_approval.sql, database/schema.sql</files>
  <read_first>
    - database/migrate_posts_author.sql (migraatiokonventio: kommenttiheader + phpMyAdmin-ohje + additiiviset ALTER-lauseet)
    - database/schema.sql rivit 76-136 (horses-taulun `is_deleted`/`deleted_at` + KEY `idx_horses_deleted` — ainoa olemassa oleva soft-delete-esimerkki, malli)
    - .planning/phases/13-poisto-hyv-ksynt-ty-nkulku/13-PATTERNS.md §"database/migrate_delete_approval.sql (migration)" — täydellinen SQL-runko
    - docker-compose.yml (dev-DB: kontti `virtuaalitalli-db`, MySQL 8, tunnukset vt_user/vt_password/virtuaalitalli)
  </read_first>
  <action>
    Luo uusi `database/migrate_delete_approval.sql` migrate_posts_author.sql-konvention mukaisesti (kommenttiheader joka selittää tarkoituksen + `Aja phpMyAdminissa: Import`-ohjerivi + pelkät additiiviset lauseet, ei DROP/DELETE-lauseita).

    Migraation sisältö kahdessa osassa:
    1. `ALTER TABLE` neljälle taululle `foals`, `competitions`, `showrecords`, `posts` — kukin saa `is_deleted TINYINT(1) NOT NULL DEFAULT 0`, `deleted_at TIMESTAMP NULL DEFAULT NULL` ja `ADD KEY idx_<table>_deleted (is_deleted)`. Käytä horses-taulun sarakemäärittelyä (schema.sql) mallina täsmälleen.
    2. `CREATE TABLE IF NOT EXISTS pending_deletions` sarakkeilla: `id INT UNSIGNED AUTO_INCREMENT PK`, `entity_type ENUM('horse','foal','competition','showrecord','post') NOT NULL`, `entity_id INT UNSIGNED NOT NULL`, `requested_by INT UNSIGNED NOT NULL`, `requested_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP`, `status ENUM('pending','approved','rejected') NOT NULL DEFAULT 'pending'`, `reviewed_by INT UNSIGNED DEFAULT NULL`, `reviewed_at TIMESTAMP NULL DEFAULT NULL`. Lisää `KEY idx_pending_deletions_status (status)`, `KEY idx_pending_deletions_entity (entity_type, entity_id)`, FK `requested_by → admin_users(id) ON DELETE CASCADE`, FK `reviewed_by → admin_users(id) ON DELETE SET NULL`. ENGINE=InnoDB, CHARSET=utf8mb4, COLLATE=utf8mb4_unicode_ci.

    Peilaa samat rakenteet `database/schema.sql`:ään: lisää `is_deleted`/`deleted_at`-sarakkeet + KEY kunkin neljän taulun CREATE TABLE -määrittelyyn (horses-taulun tyyliin) ja lisää koko `pending_deletions` CREATE TABLE -lohko sopivaan kohtaan (admin_users-taulun jälkeen, jotta FK-riippuvuus resolvoituu).

    Aja migraatio dev-DB:hen: `docker exec -i virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli < database/migrate_delete_approval.sql`. Migraation tulee olla idempotentti-turvallinen ajaa kertaalleen (ALTER ei ole IF NOT EXISTS -suojattu MySQL 8:ssa, joten aja vain kerran).
  </action>
  <verify>
    <automated>docker exec -i virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli < database/migrate_delete_approval.sql && docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -N -e "SELECT COUNT(*) FROM information_schema.columns WHERE table_schema='virtuaalitalli' AND column_name='is_deleted' AND table_name IN ('foals','competitions','showrecords','posts'); SHOW TABLES LIKE 'pending_deletions';"</automated>
  </verify>
  <acceptance_criteria>
    - Kysely palauttaa `4` (kaikilla neljällä taululla on is_deleted-sarake) ja listaa taulun `pending_deletions`.
    - `pending_deletions.entity_type` on ENUM viidellä arvolla; `status` ENUM kolmella arvolla (pending/approved/rejected) DEFAULT 'pending'. Vahvista: `docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -N -e "SHOW COLUMNS FROM pending_deletions LIKE 'status';"` sisältää 'pending','approved','rejected'.
    - `database/schema.sql` sisältää tekstin `pending_deletions` ja `idx_foals_deleted` (grep -c > 0 kummallekin).
    - Migraatio ei sisällä yhtään `DROP`- tai `DELETE`-lausetta (additiivinen).
  </acceptance_criteria>
  <done>Neljä sisältötaulua ovat soft-delete-kelpoisia dev-DB:ssä, `pending_deletions`-jonotaulu on olemassa oikeilla ENUM-arvoilla ja FK-rajoitteilla, ja schema.sql peilaa nämä muutokset.</done>
</task>

<task type="auto">
  <name>Task 2: insertPendingDeletion() + entityTypeToTable() helpers.php:hen</name>
  <files>public/src/includes/helpers.php</files>
  <read_first>
    - public/src/includes/helpers.php rivit 78-116 (`requireOwnResourceOrAdmin()` + `requireRole()` — sama doc-comment-tyyli ja sessioidentiteetti-konventio; uudet funktiot samaan tiedostoon samalla tyylillä)
    - .planning/phases/13-poisto-hyv-ksynt-ty-nkulku/13-PATTERNS.md §"helpers.php — new insertPendingDeletion() function" — täydellinen funktiorunko + entityTypeToTable()-match()-runko
  </read_first>
  <action>
    Lisää `public/src/includes/helpers.php`:hen kaksi uutta funktiota olemassa olevalla suomenkielisellä doc-comment-tyylillä:

    1. `insertPendingDeletion(string $entityType, int $entityId, int $requestedBy): void` — hakee `getDB()`-yhteyden, tekee ensin check-then-insert-tarkistuksen (`SELECT id FROM pending_deletions WHERE entity_type = :t AND entity_id = :id AND status = 'pending'`), ja jos rivi jo löytyy palauttaa heti luomatta duplikaattia (DEL-05, D-03). Muutoin `INSERT INTO pending_deletions (entity_type, entity_id, requested_by) VALUES (:t, :id, :by)`. Käytä prepared statementeja kaikkiin parametreihin. `$requestedBy` tulee AINA kutsujalta `$_SESSION['admin_id']`-arvona, ei käyttäjän syötteestä.

    2. `entityTypeToTable(string $entityType): string` — whitelist-`match()`-lauseke joka mappaa `'horse'→'horses'`, `'foal'→'foals'`, `'competition'→'competitions'`, `'showrecord'→'showrecords'`, `'post'→'posts'`, ja `default` heittää `InvalidArgumentException`. Tämä on ainoa sallittu tapa johtaa taulunimi entity_type-arvosta — EI dynaamista taulunimen rakennusta käyttäjän syötteestä (SQL-injektioesto, tutkimuksen kriittinen pitfall).

    Älä muuta olemassa olevia funktioita. Sijoita uudet funktiot loogiseen kohtaan (esim. `requireOwnResourceOrAdmin()`:n jälkeen).
  </action>
  <verify>
    <automated>php -l public/src/includes/helpers.php && grep -c 'function insertPendingDeletion' public/src/includes/helpers.php && grep -c 'function entityTypeToTable' public/src/includes/helpers.php && docker exec virtuaalitalli-web php -r "require '/var/www/html/src/includes/db.php'; echo entityTypeToTable('showrecord');"</automated>
  </verify>
  <acceptance_criteria>
    - `php -l` läpäisee ilman syntaksivirheitä.
    - Molemmat grep-laskurit palauttavat vähintään 1.
    - `entityTypeToTable('showrecord')` tulostaa `showrecords`; `entityTypeToTable('post')` tulostaa `posts` (aja container-php:llä).
    - `entityTypeToTable('../horses; DROP')` tai muu whitelistin ulkopuolinen arvo heittää InvalidArgumentException (ei palauta merkkijonoa).
    - Behavior: kahdesti peräkkäin kutsuttu `insertPendingDeletion('horse', <sama id>, 1)` tuottaa vain yhden `status='pending'`-rivin pending_deletions-tauluun.
  </acceptance_criteria>
  <done>`insertPendingDeletion()` estää duplikaatit check-then-insert-logiikalla ja `entityTypeToTable()` tarjoaa turvallisen whitelist-mappauksen, molemmat käytettävissä kaikille Wave 2 -planeille.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kutsuja→insertPendingDeletion() | entity_type/id välitetään funktiolle; requested_by on aina sessiosta |
| entity_type→taulunimi | polymorfinen mappaus voi altistaa dynaamiselle SQL:lle jos ei whitelistattu |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | entityTypeToTable() taulunimen johto | high | mitigate | `match()`-whitelist, `default` heittää poikkeuksen — ei koskaan käyttäjän syötettä suoraan taulunimeen |
| T-13-02 | Injection | insertPendingDeletion() SQL | high | mitigate | Pelkät prepared statementit, ei string-konkatenaatiota parametreille |
| T-13-03 | Spoofing | requested_by-identiteetti | medium | mitigate | Kutsusopimus: requested_by aina `$_SESSION['admin_id']`, dokumentoitu doc-commentissa |
| T-13-04 | Tampering | duplikaattirivit pending-jonossa | low | accept | PHP-tason check-then-insert (D-03); jäljelle jäävä kilpailutilanne hyväksytään yhden tallin matalalla volyymilla |

Tässä planissa ei ole paketinhallinta-asennuksia (puhdas PHP/MySQL), joten toimitusketju-uhkaa (T-13-SC) ei sovelleta.
</threat_model>

<verification>
- Migraatio ajettu dev-DB:hen ilman virheitä; neljä soft-delete-saraketta + pending_deletions-taulu olemassa.
- schema.sql peilaa migraation.
- `php -l helpers.php` puhdas; molemmat uudet funktiot löytyvät ja käyttäytyvät odotetusti.
</verification>

<success_criteria>
- pending_deletions-taulu ja neljä soft-delete-saraketta ovat käytettävissä dev-DB:ssä.
- insertPendingDeletion() estää duplikaatit (DEL-05).
- entityTypeToTable() tarjoaa SQL-injektiosuojatun whitelist-mappauksen.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- `database/migrate_delete_approval.sql` (new file)
- Table `pending_deletions` (columns: `id`, `entity_type` ENUM, `entity_id`, `requested_by`, `requested_at`, `status` ENUM, `reviewed_by`, `reviewed_at`; keys: `idx_pending_deletions_status`, `idx_pending_deletions_entity`; FKs to `admin_users`)
- Columns `is_deleted TINYINT(1)` + `deleted_at TIMESTAMP` on tables `foals`, `competitions`, `showrecords`, `posts` (+ `idx_<table>_deleted`)
- Function `insertPendingDeletion(string $entityType, int $entityId, int $requestedBy): void` in `public/src/includes/helpers.php`
- Function `entityTypeToTable(string $entityType): string` in `public/src/includes/helpers.php`
</artifacts_produced>

<output>
Create `.planning/phases/13-poisto-hyv-ksynt-ty-nkulku/13-01-SUMMARY.md` when done
</output>
