---
phase: 13-poisto-hyv-ksynt-ty-nkulku
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - public/admin/deletions.php
  - public/admin/deletion_approve.php
  - public/admin/deletion_reject.php
  - public/admin/index.php
  - public/admin/includes/admin_header.php
autonomous: true
requirements: [DEL-01, DEL-02, DEL-03, DEL-04]
must_haves:
  truths:
    - "Admin näkee yhdestä näkymästä kaikki odottavat poistopyynnöt kaikista viidestä sisältötyypistä (DEL-01)"
    - "Admin voi hyväksyä pyynnön → status='approved', sisältö pysyy piilossa (DEL-02)"
    - "Admin voi hylätä pyynnön → sisältö palautuu näkyväksi (is_deleted=0) ja status='rejected' (DEL-03)"
    - "Admin-etusivulla näkyy odottavien pyyntöjen laskuri (DEL-04)"
    - "deletions.php/approve/reject ovat vain adminille (mod/author ohjautuvat ei-oikeutta-näkymään)"
  artifacts:
    - public/admin/deletions.php
    - public/admin/deletion_approve.php
    - public/admin/deletion_reject.php
    - "admin/index.php stat-kortti pending-laskurilla"
    - "admin_header.php Poistopyynnöt-nav-linkki (admin-only)"
  key_links:
    - "deletion_reject.php käyttää entityTypeToTable() + PDO-transaktiota kahden UPDATE:n atomisointiin"
    - "deletions.php JOIN pending_deletions → viisi sisältötaulua entity_type-suodatuksella"
    - "index.php COUNT(*) FROM pending_deletions WHERE status='pending'"
---

<objective>
Rakenna adminin hyväksyntätyönkulun käyttöliittymä: yhtenäinen `deletions.php`-lista kaikista odottavista poistopyynnöistä (DEL-01), erilliset hyväksy/hylkää-käsittelijät (DEL-02/DEL-03), odottavien pyyntöjen laskuri admin-etusivulle (DEL-04), sekä admin-only nav-linkki. Hyväksyntä muuttaa vain jonon tilaa (sisältö on jo soft-deletetty); hylkäys palauttaa sisällön näkyväksi ja merkitsee jonon rivin hylätyksi (rivi säilyy auditointia varten, D-05).

Purpose: Ilman tätä admin ei näe eikä pysty ratkaisemaan mod:n poistopyyntöjä. D-06 (yksi yhtenäinen taulunäkymä), D-07 (laskuri stat-korttina), D-08 (nav-linkki Sivusto-osioon).
Output: Uusi `deletions.php` + kaksi käsittelijää, muokattu `index.php` ja `admin_header.php`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-poisto-hyv-ksynt-ty-nkulku/13-CONTEXT.md
@.planning/phases/13-poisto-hyv-ksynt-ty-nkulku/13-PATTERNS.md
</context>

<tasks>

<!-- planner-discipline-allow: is_deleted = 0 -->

<task type="auto">
  <name>Task 1: deletions.php — yhtenäinen odottavien poistopyyntöjen lista (DEL-01)</name>
  <files>public/admin/deletions.php</files>
  <read_first>
    - public/admin/users.php rivit 1-80 (taulukkomallinen listanäkymä + per-rivin inline-POST-lomakkeet — D-06 peilaa Phase 11 D-01:tä; malli deletions.php:lle)
    - .planning/phases/13-poisto-hyv-ksynt-ty-nkulku/13-PATTERNS.md §"deletions.php (new — approval list view)" — JOIN-kyselyrunko + taulukko/nappirakenne
    - public/admin/includes/admin_header.php (sivupohjan include-malli: `$pageTitle`, `require admin_header.php`, sulkeva `admin_footer.php`)
    - public/src/includes/helpers.php (`generate_csrf_token()`, `e()`, `formatDate()`)
  </read_first>
  <action>
    Luo uusi `public/admin/deletions.php`. Aloita `require_once __DIR__ . '/../src/includes/db.php';` + `requireRole('admin');` (vain admin — DEL-01, D-08). Aseta `$_activePage = 'deletions';` ennen headerin includea nav-aktiivitilaa varten.

    Hae odottavat pyynnöt yhdellä kyselyllä: `SELECT pd.id, pd.entity_type, pd.entity_id, pd.requested_at, u.username AS requested_by_name` + ihmisluettava kohdenimike (`entity_label`) LEFT JOIN -liitoksilla viiteen sisältötauluun, kukin `pd.entity_type='<type>' AND <t>.id = pd.entity_id` -ehdolla. Käytä kohdenimikkeenä kunkin taulun luettavinta saraketta (horses → `name`, foals → `foal_name`, posts → `title`; competitions/showrecords → sopiva luettava kenttä tai fallback `CONCAT(entity_type,' #',entity_id)`). JOIN `admin_users u ON u.id = pd.requested_by`. Suodata `WHERE pd.status = 'pending'`, järjestä `ORDER BY pd.requested_at DESC`.

    Renderöi yksi `<table>` (D-06, ei per-tyyppi-tabeja) sarakkeilla Tyyppi / Kohde / Pyytäjä / Pyydetty / Toiminnot. Tyyppisarakkeeseen suomenkielinen nimike (hevonen/varsa/kilpailu/näyttelytulos/postaus) — käytä `match()`-mappausta entity_type-arvosta. Kohde-sarake voi olla linkki kohteen muokkaussivulle (valinnainen, Claude's Discretion). Jokaiselle riville kaksi erillistä inline-POST-lomaketta: yksi `action="…/deletion_approve.php"` (nappi "Hyväksy"), toinen `action="…/deletion_reject.php"` (nappi "Hylkää", `.btn-danger`). Molemmissa hidden `csrf_token` (`generate_csrf_token()`) + hidden `id` (`(int)$row['id']`). ÄLÄ lisää `confirm()`-onclickia — molemmat toiminnot ovat peruutettavissa (CONTEXT.md). Näytä flash-viesti `?approved=1`/`?rejected=1`-GET-lipuista. Tyhjä lista → ystävällinen "Ei odottavia poistopyyntöjä" -viesti. Sulje sivu `require __DIR__ . '/includes/admin_footer.php';` -kutsulla (admin-sivujen konventio).
  </action>
  <verify>
    <automated>php -l public/admin/deletions.php && grep -c "requireRole('admin')" public/admin/deletions.php && grep -c 'deletion_approve.php' public/admin/deletions.php && grep -c 'deletion_reject.php' public/admin/deletions.php && grep -c "status = 'pending'" public/admin/deletions.php && grep -c 'generate_csrf_token' public/admin/deletions.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l` läpäisee.
    - Gate on täsmälleen `requireRole('admin')` (ei mod/author).
    - Kysely suodattaa `status = 'pending'` ja liittää kaikki viisi sisältötaulua (grep löytää `horses`, `foals`, `competitions`, `showrecords`, `posts` LEFT JOIN -riveiltä).
    - Jokaisella rivillä on sekä approve- että reject-lomake CSRF-tokenilla ja id:llä.
    - Behavior: kun pending_deletions sisältää rivejä eri entity_type-arvoilla, kaikki näkyvät yhdessä taulukossa (ei virhettä LEFT JOIN -null-arvoilla).
  </acceptance_criteria>
  <done>deletions.php listaa kaikki odottavat poistopyynnöt viidestä sisältötyypistä yhdessä taulukossa ja tarjoaa per-rivin hyväksy/hylkää-lomakkeet.</done>
</task>

<task type="auto">
  <name>Task 2: deletion_approve.php + deletion_reject.php käsittelijät (DEL-02/DEL-03)</name>
  <files>public/admin/deletion_approve.php, public/admin/deletion_reject.php</files>
  <read_first>
    - public/admin/user_toggle_active.php / public/admin/user_delete.php (pieni yhden-tarkoituksen POST-only-käsittelijä: requireRole + POST-check + CSRF + id-validointi + yksi UPDATE + redirect — malli)
    - public/admin/horse_delete.php (POST/CSRF/redirect-runko)
    - .planning/phases/13-poisto-hyv-ksynt-ty-nkulku/13-PATTERNS.md §"deletion_approve.php / deletion_reject.php" — täydelliset target-mallit
    - public/src/includes/helpers.php (`entityTypeToTable()` Plan 01:stä, `validate_csrf_token()`)
  </read_first>
  <action>
    `deletion_approve.php` (DEL-02): `requireRole('admin')`. Jos ei POST → redirect `deletions.php`. Jos CSRF ei validi → redirect `deletions.php`. `$id = (int)($_POST['id'] ?? 0)`. Jos `$id > 0`: `UPDATE pending_deletions SET status = 'approved', reviewed_by = :by, reviewed_at = NOW() WHERE id = :id AND status = 'pending'` (`:by` = `$_SESSION['admin_id']`). EI muuteta sisältötaulua — sisältö on jo `is_deleted=1` (D-05). Redirect `deletions.php?approved=1`.

    `deletion_reject.php` (DEL-03): `requireRole('admin')`. POST- ja CSRF-tarkistukset kuten yllä. `$id = (int)($_POST['id'] ?? 0)`. Jos `$id > 0`: hae `SELECT entity_type, entity_id FROM pending_deletions WHERE id = :id AND status = 'pending'`. Jos rivi löytyy: johda taulunimi turvallisesti `entityTypeToTable($row['entity_type'])` (whitelist-match(), EI dynaamista SQL:ää syötteestä). Suorita kaksi kirjoitusta yhdessä PDO-transaktiossa (`beginTransaction()` … `commit()`): (1) `UPDATE `<table>` SET is_deleted = 0, deleted_at = NULL WHERE id = :eid` — palauttaa sisällön näkyväksi, (2) `UPDATE pending_deletions SET status = 'rejected', reviewed_by = :by, reviewed_at = NOW() WHERE id = :id`. Taulunimi tulee AINA `entityTypeToTable()`:sta, backtick-lainattuna. Rivi säilytetään (ei DELETE — auditointi, D-05, Pitfall 12). Redirect `deletions.php?rejected=1`.
  </action>
  <verify>
    <automated>php -l public/admin/deletion_approve.php && php -l public/admin/deletion_reject.php && grep -c "requireRole('admin')" public/admin/deletion_approve.php public/admin/deletion_reject.php && grep -c 'validate_csrf_token' public/admin/deletion_approve.php public/admin/deletion_reject.php && grep -c "status = 'approved'" public/admin/deletion_approve.php && grep -c 'entityTypeToTable' public/admin/deletion_reject.php && grep -c 'beginTransaction' public/admin/deletion_reject.php</automated>
  </verify>
  <acceptance_criteria>
    - Molemmat `php -l` läpäisevät.
    - Molemmilla gate `requireRole('admin')` ja `validate_csrf_token()`-tarkistus; ei-POST → redirect.
    - `deletion_approve.php` päivittää vain `pending_deletions`-rivin (`status='approved'`), ei kosketa sisältötauluihin.
    - `deletion_reject.php` käyttää `entityTypeToTable()`-whitelistiä taulunimeen (ei string-konkatenaatiota entity_type-arvosta) ja kääri kaksi UPDATE-lausetta `beginTransaction()`/`commit()`-transaktioon.
    - Behavior: hylkäys asettaa sisällön `is_deleted=0` JA `pending_deletions.status='rejected'`; hyväksyntä jättää sisällön `is_deleted=1`:ksi ja asettaa `status='approved'`. Molemmat toimivat vain `status='pending'`-riveille.
  </acceptance_criteria>
  <done>Admin voi hyväksyä (tila approved, sisältö pysyy piilossa) tai hylätä (sisältö palautuu näkyväksi, tila rejected) poistopyynnön turvallisesti ja atomisesti.</done>
</task>

<task type="auto">
  <name>Task 3: DEL-04-laskuri admin-etusivulle + Poistopyynnöt-nav-linkki</name>
  <files>public/admin/index.php, public/admin/includes/admin_header.php</files>
  <read_first>
    - public/admin/index.php rivit 1-46 (`.admin-stat-row`/`.admin-stat-card`-lohko + olemassa olevat COUNT-kyselyt riveillä 6-9)
    - public/admin/includes/admin_header.php rivit 334-343 ("Sivusto"-nav-osio, `isAdmin()`/rooliarray-gated settings.php-linkki — malli uudelle linkille)
    - .planning/phases/13-poisto-hyv-ksynt-ty-nkulku/13-PATTERNS.md §"index.php (modify — add DEL-04 stat card)" ja §"admin_header.php (modify — add nav link)"
  </read_first>
  <action>
    `index.php` (DEL-04, D-07): Lisää olemassa olevien COUNT-kyselyjen (rivit 6-9) viereen `$pendingDeletionCount = $db->query("SELECT COUNT(*) FROM pending_deletions WHERE status = 'pending'")->fetchColumn();`. Lisää `.admin-stat-row`-lohkoon (rivit 25-46) uusi `.admin-stat-card` joka näyttää `(int)$pendingDeletionCount` ja labelin "Odottavaa poistopyyntöä" (mallina olemassa olevat stat-kortit). Korjaa samalla olemassa olevat `$compCount`/`$showCount`-kyselyt (rivit 8-9) suodattamaan `WHERE is_deleted = 0`, jotta soft-deletetyt eivät vääristä laskureita (kilpailut/näyttelyt saivat is_deleted-sarakkeen Plan 01:ssä).

    `admin_header.php` (D-08): Lisää "Sivusto"-nav-osioon (rivien 334-343 sisään, settings.php-linkin viereen) uusi admin-only nav-linkki `Poistopyynnöt` joka osoittaa `/admin/deletions.php`. Käytä samaa `<?php if (in_array($role, ['admin'], true)): ?>`-ehtoa kuin settings.php-linkki ja aseta aktiivitila `$_activePage === 'deletions'` -vertailulla (sama `.admin-nav-item active`-malli).
  </action>
  <verify>
    <automated>php -l public/admin/index.php && php -l public/admin/includes/admin_header.php && grep -c 'pendingDeletionCount' public/admin/index.php && grep -c 'Odottavaa poistopyyntö' public/admin/index.php && grep -c 'deletions.php' public/admin/includes/admin_header.php && grep -c "is_deleted = 0" public/admin/index.php</automated>
  </verify>
  <acceptance_criteria>
    - Molemmat `php -l` läpäisevät.
    - `index.php` sisältää `pendingDeletionCount`-kyselyn (`WHERE status = 'pending'`) ja uuden stat-kortin labelilla "Odottavaa poistopyyntöä".
    - `index.php`:n competition/show-COUNT-kyselyt suodattavat `is_deleted = 0`.
    - `admin_header.php` sisältää admin-only `Poistopyynnöt`-linkin `/admin/deletions.php`:iin, `in_array($role, ['admin'], true)`-ehdolla.
    - Behavior: mod-/author-roolilla nav ei renderöi Poistopyynnöt-linkkiä; laskuri näyttää `pending`-tilaisten rivien lukumäärän.
  </acceptance_criteria>
  <done>Admin-etusivulla näkyy odottavien poistopyyntöjen laskuri ja admin-navigaatiossa on Poistopyynnöt-linkki (vain adminille).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selain→approve/reject-käsittelijä | Epäluotettu POST (id, csrf_token) |
| entity_type→taulunimi (reject) | Polymorfinen mappaus voi altistaa dynaamiselle SQL:lle |
| ei-admin→deletions.php | Mod/author eivät saa nähdä eivätkä ratkaista pyyntöjä |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-09 | Elevation of Privilege | deletions.php / approve / reject pääsy | high | mitigate | `requireRole('admin')` kaikilla kolmella; mod/author → ei-oikeutta |
| T-13-10 | Injection | deletion_reject.php taulunimen johto | high | mitigate | `entityTypeToTable()`-whitelist, backtick-lainaus, ei syötettä suoraan SQL:ään |
| T-13-11 | Spoofing | CSRF approve/reject-POST | high | mitigate | `validate_csrf_token()`; ei-POST → redirect |
| T-13-12 | Tampering | osittainen kirjoitus hylkäyksessä | medium | mitigate | PDO-transaktio (beginTransaction/commit) atomisoi sisällön palautuksen + jonon tilamuutoksen |
| T-13-13 | Repudiation | kuka ratkaisi pyynnön | low | accept | `reviewed_by`/`reviewed_at` täytetään sessioidentiteetistä; riittää yhden tallin tarpeeseen |

Ei paketinhallinta-asennuksia — toimitusketju-uhkaa (T-13-SC) ei sovelleta.
</threat_model>

<verification>
- deletions.php listaa kaikki viisi sisältötyyppiä yhdessä taulukossa, vain adminille.
- approve muuttaa vain jonon tilaa; reject palauttaa sisällön ja merkitsee rivin hylätyksi transaktiossa.
- index.php-laskuri ja admin_header.php-nav-linkki toimivat admin-only-ehdoin.
</verification>

<success_criteria>
- DEL-01: yksi näkymä kaikista odottavista pyynnöistä.
- DEL-02: hyväksyntä pitää sisällön piilossa, merkitsee approved.
- DEL-03: hylkäys palauttaa sisällön näkyväksi, merkitsee rejected.
- DEL-04: admin-etusivun laskuri näyttää odottavien määrän.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- `public/admin/deletions.php` (new) — unified pending-deletions list view
- `public/admin/deletion_approve.php` (new) — sets `pending_deletions.status='approved'`
- `public/admin/deletion_reject.php` (new) — restores content (`is_deleted=0`) + `status='rejected'` in a PDO transaction via `entityTypeToTable()`
- Modified `public/admin/index.php` — `$pendingDeletionCount` query + stat card; `is_deleted=0` on competition/show counts
- Modified `public/admin/includes/admin_header.php` — admin-only "Poistopyynnöt" nav link
</artifacts_produced>

<output>
Create `.planning/phases/13-poisto-hyv-ksynt-ty-nkulku/13-03-SUMMARY.md` when done
</output>
