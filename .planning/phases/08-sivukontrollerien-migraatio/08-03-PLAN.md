---
phase: 08-sivukontrollerien-migraatio
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/pages/hevonen.php
autonomous: true
requirements: [THEME-08]
must_haves:
  truths:
    - "GET /pages/hevonen.php?slug=<valid> returns HTTP 200 rendering the full horse profile from the default theme template"
    - "GET /pages/hevonen.php with no slug/id returns HTTP 404 with no themed body"
    - "hevonen.php contains no inline HTML, no header.php require, no footer.php require"
    - "hevonen.php contains no pedigreeHorseLink()/pedigreeCell() definitions and no $genderFi array (template-owned)"
    - "hevonen.php ends with a resolveThemePath('pages/hevonen.php') delegation guarded by a 404 fallback"
    - "The existing Model B root-override hook (already canonical) is retained unchanged"
  artifacts:
    - public/pages/hevonen.php
  key_links:
    - "controller data scope ($horse, $pedigree, $competitions, ...) -> require resolveThemePath('pages/hevonen.php') -> template"
    - "hevonen.php missing/not-found horse -> unified http_response_code(404); exit; (theme-page.php convention)"
---

<objective>
Migrate `hevonen.php` — the largest public controller (614 lines, multi-join queries, deep pedigree structure) — to a data-only controller. This is isolated in its own plan because the deletion surface is large (roughly 470 lines of inline HTML plus two embedded helper functions), warranting a dedicated context budget.

`hevonen.php` already has the canonical Model B hook (`resolveThemePath('hevonen.php')`) and already requires `theme.php` — both are RETAINED unchanged (**D-01**/**D-02**). The work is: delete the inline HTML body per **D-04**; remove the template-owned helpers `$genderFi`, `pedigreeHorseLink()`, `pedigreeCell()`, and the `$heroPhoto`/`$heroStyle` presentation locals per **D-05**; unify the two inline-HTML 404 branches to the silent `http_response_code(404); exit;` convention; and append the guarded Malli A delegation. Implements THEME-08 (horse profile).

Purpose: Remove the last and largest inline-HTML controller so all 5 base pages are data-only.
Output: 1 migrated controller; no new persistent symbols.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sivukontrollerien-migraatio/08-CONTEXT.md
@.planning/phases/08-sivukontrollerien-migraatio/08-PATTERNS.md
@.planning/phases/08-sivukontrollerien-migraatio/08-UI-SPEC.md
@public/src/includes/theme.php
@public/pages/theme-page.php
</context>

<artifacts_produced>
No new persistent symbols. Modifies 1 existing file in place:
- `public/pages/hevonen.php` (data-only controller, unified 404, helpers removed)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migrate hevonen.php to data-only + unify 404 + strip template-owned helpers</name>
  <files>public/pages/hevonen.php</files>
  <read_first>
    - public/pages/hevonen.php (current: Model B hook lines 5-11 already canonical, theme.php required line 3; slug/id fetch lines 13-57; no-param 404 branch lines 58-65; not-found 404 branch lines 69-76; `$id`/`$page_title = $horse['name']` lines 78-79; remaining DB queries for competitions/showrecords/photos/foals/horsePosts/pedigree lines 80-143; inline HTML from line 145 through footer include line 614, INCLUDING `$genderFi` line 147, `pedigreeHorseLink()` lines 150-167, `$heroPhoto`/`$heroStyle` lines 170-173, `pedigreeCell()` lines 339-343)
    - public/themes/default/pages/hevonen.php (paired template — owns `$genderFi` line 4, `pedigreeHorseLink()` lines 6-20+, and `pedigreeCell()`; requires its own header.php at line 2 but does NOT set `$page_title`; variable contract: `$horse` assoc array with many keys (name, call_name, breed_name, gender, birth_date, aging_system, discipline_names, level_ko/re/ke, description, height_cm, color_name, genes, vh_id, pkk_id, pedigree_notes, owner/breeder/importer_* fields), `$id`, `$competitions`, `$showrecords`, `$photos`, `$foals`, `$horsePosts`, `$pedigree` nested sire/dam struct)
    - public/pages/theme-page.php (canonical silent `http_response_code(404); exit;`)
    - 08-PATTERNS.md section "hevonen.php" and "404 handling unification"
  </read_first>
  <action>
    Keep lines 1-11 unchanged: the db.php + theme.php requires and the existing canonical Model B hook `resolveThemePath('hevonen.php')`. Do NOT rewrite the hook — it is already the reference form all other controllers copy.

    Replace BOTH inline-HTML 404 branches with the silent unified idiom:
    - The no-param `else` branch (current lines 58-65) becomes `http_response_code(404); exit;` only.
    - The not-found branch (current lines 69-76, `if (!$horse)`) becomes `http_response_code(404); exit;` only.
    Remove the `$page_title = 'Hevosta ei löydy';`, the header include, the echoed body, and the footer include from both branches.

    KEEP `$id = (int)$horse['id'];` (current line 78) and KEEP `$page_title = $horse['name'];` (current line 79) — the hevonen template does NOT self-set `$page_title`; it relies on the controller setting it before the template's header include renders the <title>. Do not remove line 79.

    Keep all remaining DB queries producing `$competitions`, `$showrecords`, `$photos`, `$foals`, `$horsePosts`, and `$pedigree` (current lines 80-143).

    Delete the entire inline HTML region from the header include (current line 145) through the footer include (current line 614) per **D-04**. This region also contains presentation code that the template now owns and MUST be removed per **D-05**: the `$genderFi` array, the `pedigreeHorseLink()` function, the `pedigreeCell()` function, and the `$heroPhoto`/`$heroStyle` locals. None may remain as dead code in the controller — PHP forbids redefining `pedigreeHorseLink()`/`pedigreeCell()` since the template defines them, so leaving them would be a fatal redeclare error.

    Append the guarded Malli A delegation for `resolveThemePath('pages/hevonen.php')` (resolve to variable, `http_response_code(404); exit;` if false, else `require`).
  </action>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' public/pages/hevonen.php | grep -c 'includes/header.php'` returns 0
    - `grep -v '^[[:space:]]*//' public/pages/hevonen.php | grep -c 'includes/footer.php'` returns 0
    - `grep -c "resolveThemePath('hevonen.php')" public/pages/hevonen.php` returns 1 (Model B hook retained)
    - `grep -c "resolveThemePath('pages/hevonen.php')" public/pages/hevonen.php` returns 1 (Malli A delegation)
    - `grep -c 'function pedigreeHorseLink' public/pages/hevonen.php` returns 0 (template owns it)
    - `grep -c 'function pedigreeCell' public/pages/hevonen.php` returns 0
    - `grep -c 'genderFi' public/pages/hevonen.php` returns 0
    - `grep -c "\$page_title = \$horse" public/pages/hevonen.php` returns 1 (dynamic title kept)
    - `docker exec virtuaalitalli-web php -l /var/www/html/pages/hevonen.php` reports "No syntax errors detected"
    - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/pages/hevonen.php` returns 404 (no slug → silent 404)
    - With a valid slug from the DB, `curl` of `?slug=<slug>` returns 200
  </acceptance_criteria>
  <verify>
    <automated>docker exec virtuaalitalli-web php -l /var/www/html/pages/hevonen.php && test "$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/pages/hevonen.php)" = "404" && SLUG=$(docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -N -e "SELECT slug FROM horses WHERE is_deleted=0 AND evm=0 AND ancestor=0 LIMIT 1") && test "$(curl -s -o /dev/null -w '%{http_code}' "http://localhost:8080/pages/hevonen.php?slug=$SLUG")" = "200" && test "$(grep -c 'function pedigreeHorseLink' public/pages/hevonen.php)" = "0" && test "$(grep -c 'genderFi' public/pages/hevonen.php)" = "0"</automated>
  </verify>
  <done>hevonen.php is data-only: retained canonical Model B hook, all DB queries producing the template's variable contract, both 404 branches unified to silent `http_response_code(404); exit;`, dynamic `$page_title` kept, guarded Malli A delegation, and zero inline HTML / `$genderFi` / `pedigreeHorseLink()` / `pedigreeCell()` / `$heroPhoto`. Valid slug renders the full profile at 200; missing slug returns 404.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → controller | HTTP GET with `$_GET['slug']` or `$_GET['id']` |
| controller → filesystem | `resolveThemePath()` maps a subpath to a theme file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Tampering | hevonen.php `$_GET['slug']`/`$_GET['id']` | low | mitigate | Input handling UNCHANGED: slug `preg_replace('/[^a-z0-9\-]/', ...)`, id `(int)`, all queries remain prepared statements. Migration only moves rendering, not input sinks. |
| T-08-07 | Tampering | resolveThemePath() path argument | low | mitigate | Argument is the hardcoded literal `'pages/hevonen.php'`; no user input reaches the path. theme.php preg_match/realpath/prefix-check unchanged. |
| T-08-08 | Information Disclosure | unified 404 branches + Malli A fallback | low | mitigate | Silent `http_response_code(404); exit;` with no body — removes previously-rendered error markup. XSS output escaping stays in the template via `e()`/`htmlspecialchars()`, unchanged. |
| T-08-SC | Tampering | package installs (npm/pip/cargo) | low | accept | No package installs — pure PHP edits. |
</threat_model>

<verification>
- hevonen.php passes `php -l` via the web container.
- No slug → HTTP 404 (silent, no body); valid slug → HTTP 200 full profile.
- No header.php/footer.php include remains (comment-filtered grep 0).
- No `pedigreeHorseLink()`, `pedigreeCell()`, or `$genderFi` remains in the controller (template-owned; would otherwise fatally redeclare).
- Canonical Model B hook retained; guarded Malli A delegation present; dynamic `$page_title` retained.
</verification>

<success_criteria>
hevonen.php is a data-only controller: it runs its multi-join data queries, short-circuits missing horses with a silent 404, and delegates all rendering to `resolveThemePath('pages/hevonen.php')`, with zero inline HTML and zero template-owned helpers remaining.
</success_criteria>

<output>
Create `.planning/phases/08-sivukontrollerien-migraatio/08-03-SUMMARY.md` when done.
</output>
