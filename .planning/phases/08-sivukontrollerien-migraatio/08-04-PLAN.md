---
phase: 08-sivukontrollerien-migraatio
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - public/themes/testitema/
autonomous: true
requirements: [THEME-08, THEME-09]
must_haves:
  truths:
    - "With settings.active_theme='testitema', GET /pages/index.php renders the test theme's distinct marker (appearance changed) at HTTP 200"
    - "Switching the active theme required ZERO edits to any controller in public/pages/ (git diff clean for that path during the switch)"
    - "With settings.active_theme back to 'default', the test marker is absent and pages render normally"
    - "The temporary test theme directory is deleted before phase close and settings.active_theme is 'default'"
  artifacts:
    - "public/themes/testitema/ (TEMPORARY — created in Task 1, deleted in Task 3; net persistent artifact: none)"
  key_links:
    - "settings.active_theme -> theme.php THEME_PATH -> resolveThemePath('pages/X.php') -> testitema template"
---

<objective>
Prove Success Criteria #3 — "changing the theme in the DB changes the site appearance with zero controller changes" — using a throwaway test theme per **D-06**, then remove it per **D-07**.

This plan depends on plans 08-01/08-02/08-03 (all 7 controllers must already delegate via Malli A). It creates a lightweight copy of the `default` theme with one distinctive visual marker, flips `settings.active_theme` to it, curl-verifies the appearance changed while the controllers are untouched, flips back to `default`, and deletes the test theme. The test theme is a developer-only artifact — it is explicitly NOT `oma-talli` (the user's real WIP theme, which is not touched in this phase) and MUST NOT be left installed (**D-07**).

Purpose: End-to-end proof that the migration decouples controllers from presentation.
Output: verification evidence only; net-zero persistent artifacts after cleanup.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sivukontrollerien-migraatio/08-CONTEXT.md
@.planning/phases/08-sivukontrollerien-migraatio/08-UI-SPEC.md
@public/src/includes/theme.php
</context>

<artifacts_produced>
- `public/themes/testitema/` — TEMPORARY test theme (copy of `default` + one marker). Created in Task 1, DELETED in Task 3. No persistent symbol survives this plan.
- No changes to any controller in `public/pages/` (the whole point of the proof).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create the temporary test theme (copy of default + visible marker)</name>
  <files>public/themes/testitema/</files>
  <read_first>
    - public/themes/default/ (full theme to copy: assets/, includes/, pages/, theme.json)
    - public/themes/default/includes/header.php (the shared header rendered on every page — the marker goes here so it appears on all pages)
    - public/themes/default/theme.json (manifest shape: name + version)
    - public/src/includes/theme.php (confirms resolveThemePath resolves active-theme files first, falling back to default)
  </read_first>
  <action>
    Create `public/themes/testitema/` as a full recursive copy of `public/themes/default/` (assets, includes, pages, theme.json). This mirrors the "full custom theme" shape so the active-theme path resolves to testitema's own `pages/*.php` per **D-06**.

    Edit `public/themes/testitema/theme.json` so its `name` field is `testitema` (keep the `version` field).

    Inject one distinctive, curl-detectable marker into `public/themes/testitema/includes/header.php` — an HTML comment on its own line reading exactly `<!-- TESTITEMA-MARKER -->` placed just inside the rendered output (e.g. right after the opening body tag). Because every test-theme page includes this header, the marker appears on all migrated pages when testitema is active, and is ABSENT under the default theme. Do NOT modify `public/themes/default/` — only the testitema copy.

    Do NOT create a root-level `index.php`/`hevonen.php` in testitema — leaving them absent keeps the Model B hook inert and correctly exercises the Malli A delegation path (the actual thing under test).
  </action>
  <acceptance_criteria>
    - `test -d public/themes/testitema/pages && test -f public/themes/testitema/theme.json` (theme copied)
    - `test -f public/themes/testitema/pages/index.php` (Malli A target exists in the test theme)
    - `grep -c 'TESTITEMA-MARKER' public/themes/testitema/includes/header.php` returns 1
    - `grep -c 'TESTITEMA-MARKER' public/themes/default/includes/header.php` returns 0 (default theme untouched)
    - `test ! -f public/themes/testitema/index.php` (no root override, so Malli A is exercised)
    - testitema/theme.json `name` value is `testitema`
  </acceptance_criteria>
  <verify>
    <automated>test -f public/themes/testitema/pages/index.php && test "$(grep -c 'TESTITEMA-MARKER' public/themes/testitema/includes/header.php)" = "1" && test "$(grep -c 'TESTITEMA-MARKER' public/themes/default/includes/header.php)" = "0" && test ! -f public/themes/testitema/index.php</automated>
  </verify>
  <done>A throwaway `testitema` theme exists as a full copy of default with the unique `TESTITEMA-MARKER` in its header include and no root-level override files; the default theme is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Switch active_theme to testitema, prove appearance change with zero controller edits, switch back</name>
  <files>public/themes/testitema/</files>
  <read_first>
    - public/src/includes/theme.php (reads `settings.active_theme`, validates against `/^[a-zA-Z0-9_-]+$/`, builds THEME_PATH)
    - docker-compose.yml (db service `virtuaalitalli-db`, MySQL creds vt_user/vt_password, database `virtuaalitalli`; web service serves localhost:8080)
  </read_first>
  <action>
    Record the pre-switch git state of the controllers so the "zero controller changes" claim is provable: run `git status --porcelain public/pages/` and confirm it reports no modifications introduced by this plan.

    Flip the active theme in the DB (no controller edits): `docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -e "UPDATE settings SET setting_value='testitema' WHERE setting_key='active_theme'"`.

    Verify the appearance changed by fetching a migrated page and asserting the marker is present: `curl -s http://localhost:8080/pages/index.php` contains `TESTITEMA-MARKER`, and the HTTP code is 200. Spot-check a second migrated page (e.g. hevoset.php) also shows the marker — proving the switch affects all Malli A pages, not just one.

    Confirm NO controller was edited: `git status --porcelain public/pages/` is still empty. This is the crux of Success Criteria #3 — the theme switched via DB alone.

    Restore the default theme: `docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -e "UPDATE settings SET setting_value='default' WHERE setting_key='active_theme'"`. Re-fetch `curl -s http://localhost:8080/pages/index.php` and assert the marker is now ABSENT and HTTP is 200 (default rendering restored).
  </action>
  <acceptance_criteria>
    - Before and after the switch, `git status --porcelain public/pages/` is empty (no controller file changed)
    - With active_theme=testitema: `curl -s http://localhost:8080/pages/index.php` contains `TESTITEMA-MARKER` and returns HTTP 200
    - With active_theme=testitema: `curl -s http://localhost:8080/pages/hevoset.php` also contains `TESTITEMA-MARKER` (switch affects all Malli A pages)
    - After restoring active_theme=default: `curl -s http://localhost:8080/pages/index.php` does NOT contain `TESTITEMA-MARKER` and returns HTTP 200
  </acceptance_criteria>
  <verify>
    <automated>docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -e "UPDATE settings SET setting_value='testitema' WHERE setting_key='active_theme'" && curl -s http://localhost:8080/pages/index.php | grep -q 'TESTITEMA-MARKER' && test -z "$(git status --porcelain public/pages/)" && docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -e "UPDATE settings SET setting_value='default' WHERE setting_key='active_theme'" && ! curl -s http://localhost:8080/pages/index.php | grep -q 'TESTITEMA-MARKER'</automated>
  </verify>
  <done>Success Criteria #3 proven: switching `settings.active_theme` to testitema made the marker appear on migrated pages with zero edits to any controller; restoring `default` removed it. Active theme left at `default`.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the temporary test theme and confirm clean state (D-07)</name>
  <files>public/themes/testitema/</files>
  <read_first>
    - public/themes/ (confirm only `default` and `oma-talli` should remain — testitema is the disposable artifact; oma-talli is the user's WIP theme and MUST NOT be touched)
  </read_first>
  <action>
    Delete the entire `public/themes/testitema/` directory recursively per **D-07** — it was a one-shot proof aid and must not be left installed. Do NOT touch `public/themes/default/` or `public/themes/oma-talli/`.

    Confirm `settings.active_theme` is `default` (restored in Task 2) so no page points at the now-deleted theme: query the settings row and assert the value is `default`. (Even if it were stale, theme.php validates and falls back to default, but the DB value must be clean.)

    Confirm the site still renders normally after deletion: `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/pages/index.php` returns 200 and the page no longer contains the marker.
  </action>
  <acceptance_criteria>
    - `test ! -d public/themes/testitema` (test theme deleted)
    - `test -d public/themes/default && test -d public/themes/oma-talli` (real themes untouched)
    - settings.active_theme value is `default`
    - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/pages/index.php` returns 200
    - `curl -s http://localhost:8080/pages/index.php` does NOT contain `TESTITEMA-MARKER`
  </acceptance_criteria>
  <verify>
    <automated>test ! -d public/themes/testitema && test -d public/themes/default && test "$(docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -N -e "SELECT setting_value FROM settings WHERE setting_key='active_theme'")" = "default" && test "$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/pages/index.php)" = "200"</automated>
  </verify>
  <done>The temporary test theme is gone (D-07), `default`/`oma-talli` are intact, `settings.active_theme='default'`, and the site renders at HTTP 200 with no leftover marker. Phase can close with no dangling artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → DB | `settings.active_theme` updated manually via mysql (developer-only, not a user-facing flow) |
| controller → filesystem | `resolveThemePath()` maps to the active theme dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Tampering | settings.active_theme value | low | mitigate | theme.php validates the value against `/^[a-zA-Z0-9_-]+$/` and falls back to `default` on any invalid/missing value; `testitema` passes the allowlist. The value is restored to `default` and the theme deleted, so no dangling reference remains. |
| T-08-10 | Elevation of Privilege | temporary test theme left installed | low | mitigate | **D-07** cleanup (Task 3) deletes the theme; verified by `test ! -d`. No permanent expansion of the theme surface. |
| T-08-SC | Tampering | package installs (npm/pip/cargo) | low | accept | No package installs — filesystem copy + SQL update only. |
</threat_model>

<verification>
- testitema created as a marked copy of default with no root override; default untouched.
- DB theme switch makes the marker appear on migrated pages with zero controller edits (git clean), proving Success Criteria #3; restoring default removes it.
- testitema deleted (D-07); default and oma-talli intact; active_theme='default'; site renders at 200 with no marker.
</verification>

<success_criteria>
Success Criteria #3 is demonstrated and evidence recorded: a DB-only theme switch changed the site appearance without touching any controller, and the disposable test theme is fully removed, leaving the project in a clean `default`-theme state.
</success_criteria>

<output>
Create `.planning/phases/08-sivukontrollerien-migraatio/08-04-SUMMARY.md` when done.
</output>
