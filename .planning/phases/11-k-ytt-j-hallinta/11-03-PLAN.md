---
phase: 11-kayttajahallinta
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - public/admin/user_reset_password.php
  - public/admin/user_toggle_active.php
  - public/admin/user_delete.php
autonomous: true
requirements: [USER-03, USER-04, USER-05, USER-06, USER-07]
must_haves:
  truths:
    - "Admin nollaa toisen käyttäjän salasanan tietämättä vanhaa; järjestelmä generoi uuden salasanan ja näyttää sen kerran (USER-05/D-03/D-04)."
    - "Admin deaktivoi/reaktivoi tunnuksen yhdellä toggle-toiminnolla; deaktivoitu ei voi kirjautua mutta rivi (ja sisältö) säilyy (USER-03/D-06)."
    - "Admin poistaa tunnuksen pysyvästi confirm()-vahvistuksella (USER-04/D-07)."
    - "Järjestelmä estää palvelinpuolella viimeisen aktiivisen admin-tunnuksen sekä oman tunnuksen poiston/deaktivoinnin (USER-06/USER-07)."
  artifacts:
    - public/admin/user_reset_password.php
    - public/admin/user_toggle_active.php
    - public/admin/user_delete.php
  key_links:
    - "Kaikki kolme ovat POST-only ja käynnistyvät users.php:n inline-lomakkeista (plan 11-01)."
    - "toggle + delete tarkistavat last-admin- ja self-guardit ennen mutaatiota ja redirectaavat users.php?err=... jos estetty."
    - "reset_password käyttää generate_password() (plan 11-01) ja näyttää salasanan inline (ei $_GET)."
---

<objective>
Toteuta kolme POST-only-toimintoa: salasanan nollaus toiselle käyttäjälle (USER-05), tila-toggle deaktivointi/reaktivointi (USER-03) ja pysyvä poisto (USER-04) — kaikki suojattuina viimeisen admin (USER-06) ja oman tunnuksen (USER-07) lukituksen esto -tarkistuksin. reset_password generoi ja näyttää uuden salasanan kerran (D-04); toggle ja delete redirectaavat users.php:hen liput/virheet mukanaan.

Purpose: Nämä ovat käyttäjähallinnan toiminnalliset päätepisteet, jotka toteuttavat kriittiset turvatakuut: talli ei koskaan jää ilman toimivaa admin-tiliä eikä admin lukitse itseään ulos.
Output: `public/admin/user_reset_password.php`, `public/admin/user_toggle_active.php`, `public/admin/user_delete.php` (kaikki uusia).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-k-ytt-j-hallinta/11-CONTEXT.md
@.planning/phases/11-k-ytt-j-hallinta/11-PATTERNS.md
</context>

## Artifacts this phase produces

Uudet symbolit joita drift-verifioinnin ei tule merkitä tuntemattomiksi:

- Uusi tiedosto: `public/admin/user_reset_password.php` (POST-only, salasanan nollaus + inline kertanäyttö)
- Uusi tiedosto: `public/admin/user_toggle_active.php` (POST-only, is_active-toggle + last-admin/self-guardit)
- Uusi tiedosto: `public/admin/user_delete.php` (POST-only, DELETE + last-admin/self-guardit)
- Käyttää plan 11-01:n `generate_password()`-funktiota ja olemassa olevia `validate_csrf_token()`/`password_hash()`/`redirect()`/`e()`-apufunktioita.
- Ei uusia DB-sarakkeita — UPDATE/DELETE olemassa olevaan `admin_users`-tauluun.

<tasks>

<task type="auto">
  <name>Task 1: Luo user_reset_password.php — salasanan nollaus + inline kertanäyttö</name>
  <files>public/admin/user_reset_password.php</files>
  <read_first>
    - public/admin/contact_delete.php (POST-only-guard rivit 1-14: method-tarkistus, validate_csrf_token, id-cast)
    - public/admin/change_password.php (password_hash(..., PASSWORD_BCRYPT, ['cost' => 12]) rivi 33, inline-$success/admin-card-näyttö rivit 45-53)
    - public/src/includes/helpers.php (generate_password() plan 11-01:stä, validate_csrf_token(), e(), redirect())
    - .planning/phases/11-k-ytt-j-hallinta/11-PATTERNS.md (user_reset_password.php-osio: D-04 inline-näyttö, ei $_GET)
  </read_first>
  <action>
    Luo `public/admin/user_reset_password.php`. Alku: `require_once __DIR__ . '/../src/includes/db.php';` sitten `requireRole('admin');`. POST-only-guard: jos `$_SERVER['REQUEST_METHOD'] !== 'POST'` `redirect(SITE_URL . '/admin/users.php');`; jos `!validate_csrf_token($_POST['csrf_token'] ?? '')` `redirect(SITE_URL . '/admin/users.php');`. Lue `$id = (int)($_POST['id'] ?? 0);` ja jos `$id <= 0` redirect users.php:hen.

    Hae `$db = getDB();` ja prepared `SELECT id, username FROM admin_users WHERE id = :id`; jos rivi puuttuu, `redirect(SITE_URL . '/admin/users.php');`. Ei vanhan salasanan kyselyä (admin-oikeus, D-03). Ei last-admin/self-guardia tarpeen — salasanan nollaus ei aiheuta lukitusta.

    Generoi `$generatedPassword = generate_password();` (plan 11-01), `$hash = password_hash($generatedPassword, PASSWORD_BCRYPT, ['cost' => 12]);`, prepared `UPDATE admin_users SET password = :hash WHERE id = :id`. ÄLÄ redirectaa — generoitua plaintext-salasanaa ei saa välittää `$_GET`:n kautta. Näytä se kerran inline: aseta `$pageTitle = 'Nollaa salasana';`, `require __DIR__ . '/includes/admin_header.php';`, ja renderöi `<div class="admin-body"><div class="admin-card"><p class="flash-ok">Käyttäjän <?= e($row['username']) ?> uusi salasana (näytetään vain kerran): <code><?= e($generatedPassword) ?></code></p><a href=".../admin/users.php" class="btn">Takaisin</a></div></div>` ja lopuksi `require __DIR__ . '/includes/admin_footer.php';`. Älä logita eikä tallenna plaintext-salasanaa mihinkään muualle.
  </action>
  <verify>
    <automated>rg -q "requireRole\('admin'\)" public/admin/user_reset_password.php && rg -q "REQUEST_METHOD'\] !== 'POST'" public/admin/user_reset_password.php && rg -q "validate_csrf_token" public/admin/user_reset_password.php && rg -q "generate_password\(" public/admin/user_reset_password.php && rg -q "UPDATE admin_users SET password" public/admin/user_reset_password.php</automated>
  </verify>
  <acceptance_criteria>
    - `user_reset_password.php` sisältää `requireRole('admin')` ja POST-only-guardin (`REQUEST_METHOD !== 'POST'` → redirect)
    - Sisältää `validate_csrf_token()`-tarkistuksen ennen mutaatiota
    - EI kysy vanhaa salasanaa (`rg "current_password|password_verify" public/admin/user_reset_password.php` ei osu) — D-03
    - Käyttää `generate_password(` + `password_hash(..., PASSWORD_BCRYPT, ['cost' => 12])` ja `UPDATE admin_users SET password`
    - Näyttää salasanan inline (`$generatedPassword`) eikä välitä sitä `$_GET`:ssä — `rg "users.php\?.*password" public/admin/user_reset_password.php` ei osu
    - `php -l public/admin/user_reset_password.php` palauttaa "No syntax errors" (jos php CLI saatavilla)
  </acceptance_criteria>
  <done>Admin nollaa toisen käyttäjän salasanan tuntematta vanhaa; uusi generoitu salasana näytetään kerran, tallennetaan vain bcrypt-hashina.</done>
</task>

<task type="auto">
  <name>Task 2: Luo user_toggle_active.php — deaktivointi/reaktivointi + last-admin/self-guardit</name>
  <files>public/admin/user_toggle_active.php</files>
  <read_first>
    - public/admin/contact_delete.php (POST-only-guard rivit 1-14; COUNT-pohjainen esto-guard rivit 18-25 joka on rakenteellinen malli last-admin-tarkistukselle)
    - public/src/includes/helpers.php (validate_csrf_token(), redirect())
    - database/schema.sql (rivit 245-256: admin_users.role ENUM, is_active TINYINT)
    - .planning/phases/11-k-ytt-j-hallinta/11-PATTERNS.md (user_toggle_active.php-osio: USER-06/USER-07-guardit)
  </read_first>
  <action>
    Luo `public/admin/user_toggle_active.php`. Alku: `require_once __DIR__ . '/../src/includes/db.php';` sitten `requireRole('admin');`. POST-only-guard: jos `$_SERVER['REQUEST_METHOD'] !== 'POST'` redirect users.php:hen; jos `!validate_csrf_token($_POST['csrf_token'] ?? '')` redirect users.php:hen. Lue `$id = (int)($_POST['id'] ?? 0);` ja jos `$id <= 0` redirect.

    Hae `$db = getDB();` ja kohteen nykytila prepared `SELECT id, role, is_active FROM admin_users WHERE id = :id`; jos rivi puuttuu, redirect users.php:hen. Määritä `$isDeactivating = ((int)$row['is_active'] === 1);` (toggle 1→0 = deaktivointi, 0→1 = reaktivointi).

    Guardit (uusi logiikka — rakenteellinen malli: contact_delete.php:n COUNT-guard, "kysy COUNT, jos ehto täyttyy redirect ilman mutaatiota"). Suorita vain kun `$isDeactivating` on tosi (reaktivointi on aina turvallista):
    - Self-guard (USER-07): jos `$id === (int)$_SESSION['admin_id']` → `redirect(SITE_URL . '/admin/users.php?err=self');`.
    - Last-admin-guard (USER-06): jos `$row['role'] === 'admin'`, laske prepared `SELECT COUNT(*) FROM admin_users WHERE role = 'admin' AND is_active = 1`; jos `(int)$count <= 1` → `redirect(SITE_URL . '/admin/users.php?err=lastadmin');`.

    Jos guardit läpi, tee toggle: prepared `UPDATE admin_users SET is_active = 1 - is_active WHERE id = :id` (tai `is_active = NOT is_active`). Redirect lopputilan mukaan: jos deaktivoitiin `redirect(SITE_URL . '/admin/users.php?deactivated=1');`, muuten `?activated=1`. (Toggle-lopputulos ei sisällä salaisuutta, joten `$_GET`-lippu on turvallinen.)
  </action>
  <verify>
    <automated>rg -q "requireRole\('admin'\)" public/admin/user_toggle_active.php && rg -q "REQUEST_METHOD'\] !== 'POST'" public/admin/user_toggle_active.php && rg -q "admin_id" public/admin/user_toggle_active.php && rg -q "err=self" public/admin/user_toggle_active.php && rg -q "err=lastadmin" public/admin/user_toggle_active.php && rg -q "role = 'admin' AND is_active = 1" public/admin/user_toggle_active.php && rg -q "UPDATE admin_users SET is_active" public/admin/user_toggle_active.php</automated>
  </verify>
  <acceptance_criteria>
    - `user_toggle_active.php` sisältää `requireRole('admin')` + POST-only-guard + `validate_csrf_token()`
    - Self-guard: `$id === (int)$_SESSION['admin_id']` deaktivoitaessa → `redirect(...?err=self)` ilman UPDATE:a
    - Last-admin-guard: kohteen ollessa viimeinen aktiivinen admin deaktivoitaessa → `redirect(...?err=lastadmin)` ilman UPDATE:a (COUNT `role = 'admin' AND is_active = 1` <= 1)
    - Reaktivointi (0→1) sallitaan ilman guardeja
    - Toggle-UPDATE koskee vain `is_active`-saraketta
    - Redirect ?deactivated=1 / ?activated=1 lopputilan mukaan
    - `php -l public/admin/user_toggle_active.php` palauttaa "No syntax errors" (jos php CLI saatavilla)
  </acceptance_criteria>
  <done>Admin toggloi tilan; deaktivoitu tunnus ei kirjaudu mutta rivi säilyy; viimeisen aktiivisen adminin ja oman tunnuksen deaktivointi estetään virheohjauksella.</done>
</task>

<task type="auto">
  <name>Task 3: Luo user_delete.php — pysyvä poisto + last-admin/self-guardit</name>
  <files>public/admin/user_delete.php</files>
  <read_first>
    - public/admin/contact_delete.php (täysi rakenteellinen malli rivit 1-28: POST-only, CSRF, COUNT-guard, DELETE, redirect)
    - public/src/includes/helpers.php (validate_csrf_token(), redirect())
    - database/schema.sql (rivit 245-256: admin_users; huom. ei FK-riippuvuuksia muista tauluista tässä vaiheessa)
    - .planning/phases/11-k-ytt-j-hallinta/11-PATTERNS.md (user_delete.php-osio: USER-06/USER-07-guardit korvaavat contacts.php:n käyttö-COUNT-guardin)
  </read_first>
  <action>
    Luo `public/admin/user_delete.php` kopioimalla `contact_delete.php`:n rakenne mutta admin_users-taululle. Alku: `require_once __DIR__ . '/../src/includes/db.php';` sitten `requireRole('admin');`. POST-only-guard: jos `$_SERVER['REQUEST_METHOD'] !== 'POST'` redirect users.php:hen; jos `!validate_csrf_token($_POST['csrf_token'] ?? '')` redirect users.php:hen. Lue `$id = (int)($_POST['id'] ?? 0);` ja jos `$id <= 0` redirect.

    Hae `$db = getDB();` ja kohteen tiedot prepared `SELECT id, role, is_active FROM admin_users WHERE id = :id`; jos rivi puuttuu, redirect users.php:hen.

    Guardit ennen DELETE:ä (korvaavat contact_delete.php:n hevos-käyttö-COUNT-guardin samalla "COUNT, jos ehto redirect ilman mutaatiota" -muodolla):
    - Self-guard (USER-07): jos `$id === (int)$_SESSION['admin_id']` → `redirect(SITE_URL . '/admin/users.php?err=self');`.
    - Last-admin-guard (USER-06): jos `$row['role'] === 'admin' && (int)$row['is_active'] === 1`, laske prepared `SELECT COUNT(*) FROM admin_users WHERE role = 'admin' AND is_active = 1`; jos `(int)$count <= 1` → `redirect(SITE_URL . '/admin/users.php?err=lastadmin');`.

    Jos guardit läpi, tee todellinen poisto (D-07, ei soft-delete — admin_users:ssa ei ole FK-riippuvuuksia tässä vaiheessa): prepared `DELETE FROM admin_users WHERE id = :id`, sitten `redirect(SITE_URL . '/admin/users.php?deleted=1');`.
  </action>
  <verify>
    <automated>rg -q "requireRole\('admin'\)" public/admin/user_delete.php && rg -q "REQUEST_METHOD'\] !== 'POST'" public/admin/user_delete.php && rg -q "admin_id" public/admin/user_delete.php && rg -q "err=self" public/admin/user_delete.php && rg -q "err=lastadmin" public/admin/user_delete.php && rg -q "DELETE FROM admin_users WHERE id" public/admin/user_delete.php && rg -q "users.php\?deleted=1" public/admin/user_delete.php</automated>
  </verify>
  <acceptance_criteria>
    - `user_delete.php` sisältää `requireRole('admin')` + POST-only-guard + `validate_csrf_token()`
    - Self-guard: `$id === (int)$_SESSION['admin_id']` → `redirect(...?err=self)` ilman DELETE:ä
    - Last-admin-guard: viimeinen aktiivinen admin → `redirect(...?err=lastadmin)` ilman DELETE:ä (COUNT `role = 'admin' AND is_active = 1` <= 1)
    - Poisto on `DELETE FROM admin_users WHERE id = :id` (ei soft-delete)
    - Onnistuessa redirect `users.php?deleted=1`
    - `php -l public/admin/user_delete.php` palauttaa "No syntax errors" (jos php CLI saatavilla)
  </acceptance_criteria>
  <done>Admin poistaa tunnuksen pysyvästi confirm()-vahvistuksen jälkeen; viimeisen aktiivisen adminin ja oman tunnuksen poisto estetään virheohjauksella.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selain → toimintopäätepiste (reset/toggle/delete) | Autentikoitu admin POST; id + csrf_token epäluotettavaa |
| palvelin → HTTP-vastaus (nollattu salasana) | Generoitu plaintext ylittää rajan vain kerran (reset) |
| admin-toiminto → admin_users-mutaatio | Poisto/deaktivointi voi teoriassa lukita tallin ulos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Elevation of Privilege | kaikki 3 tiedostoa | high | mitigate | `requireRole('admin')` jokaisessa — mod/author estetään |
| T-11-02 | Tampering (CSRF) | POST-toiminnot | high | mitigate | `validate_csrf_token()` ennen jokaista UPDATE/DELETE-toimea |
| T-11-04 | Denial of Service (lockout) | toggle + delete | critical | mitigate | Last-admin-guard: viimeisen aktiivisen admin-tunnuksen deaktivointi/poisto estetään (USER-06) |
| T-11-05 | Denial of Service (self-lockout) | toggle + delete | high | mitigate | Self-guard: `$_SESSION['admin_id'] === $id` deaktivointi/poisto estetään (USER-07) |
| T-11-06 | Information Disclosure | reset_password salasana | high | mitigate | Nollattu salasana näytetään vain inline HTTP-vastauksessa; ei `$_GET`-välitystä, ei lokitusta, ei plaintext-tallennusta |
| T-11-09 | Insecure Direct Object Reference | id-parametri | medium | mitigate | Kohde haetaan prepared-lauseella `(int)`-castatulla id:llä; olematon id → turvallinen redirect |
| T-11-SC | Tampering | pakettiasennukset | low | accept | Ei npm/pip/cargo-asennuksia — natiivi PHP, ei toimitusketjupintaa |
</threat_model>

<verification>
- reset_password nollaa salasanan tuntematta vanhaa ja näyttää uuden kerran (ei $_GET).
- toggle deaktivoi/reaktivoi is_active-arvon; last-admin ja self estetään deaktivoitaessa.
- delete poistaa rivin pysyvästi; last-admin ja self estetään.
- CSRF + POST-only kaikissa kolmessa.
</verification>

<success_criteria>
- USER-03: deaktivointi estää kirjautumisen mutta säilyttää rivin/sisällön.
- USER-04: pysyvä poisto onnistuu confirm()-vahvistuksen jälkeen.
- USER-05: admin nollaa toisen salasanan tuntematta vanhaa; uusi näytetään kerran.
- USER-06: viimeisen aktiivisen admin-tunnuksen poisto/deaktivointi estetään.
- USER-07: oman tunnuksen poisto/deaktivointi estetään.
</success_criteria>

<output>
Create `.planning/phases/11-k-ytt-j-hallinta/11-03-SUMMARY.md` when done
</output>
