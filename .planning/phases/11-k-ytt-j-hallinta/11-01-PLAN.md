---
phase: 11-kayttajahallinta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/src/includes/helpers.php
  - public/admin/includes/admin_header.php
  - public/admin/users.php
autonomous: true
requirements: [USER-01, USER-02, USER-03, USER-04, USER-05]
must_haves:
  truths:
    - "Admin näkee /admin/users.php-sivulla taulukon kaikista admin_users-tunnuksista (käyttäjänimi, rooli, tila) ja jokaisella rivillä muokkaa/nollaa salasana/deaktivoi-aktivoi/poista-toiminnot suoraan näkyvissä."
    - "Vain admin-roolilla näkyy Käyttäjähallinta-navigaatiolinkki; mod/author eivät näe sitä."
    - "Palvelinpuolella on generate_password()-funktio joka tuottaa vahvan satunnaissalasanan random_bytes-pohjaisesti."
  artifacts:
    - public/admin/users.php
    - public/src/includes/helpers.php
    - public/admin/includes/admin_header.php
  key_links:
    - "users.php per-rivin inline-lomakkeet POSTaavat user_edit.php / user_reset_password.php / user_toggle_active.php / user_delete.php -tiedostoihin (rakennetaan waveilla 2)."
    - "admin_header.php-linkki osoittaa users.php:hen ja on kääritty isAdmin()/currentRole()-ehtoon."
---

<objective>
Rakenna Phase 11:n käyttäjähallinnan perusta ja hub: (1) uudelleenkäytettävä `generate_password()`-apufunktio salasanan generointiin (D-02/D-04), (2) admin-only navigaatiolinkki "Käyttäjähallinta" (ROLE-04-konventio), ja (3) `users.php`-listasivu perinteisenä `<table>`-näkymänä (D-01) jossa jokaisen käyttäjän kaikki toiminnot ovat suoraan näkyvissä.

Purpose: Tämä on koko käyttäjähallinnan käyttöliittymän ja jaetun apuvälineistön kotipesä. Wave 2:n toimintotiedostot (add/edit/reset/toggle/delete) tarvitsevat `generate_password()`-funktion ja `users.php`:n inline-lomakkeet ovat niiden ainoa käyttöliittymäpiste.
Output: `public/admin/users.php` (uusi), `generate_password()` lisättynä `helpers.php`:hen, admin-only nav-linkki `admin_header.php`:ssä.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-k-ytt-j-hallinta/11-CONTEXT.md
@.planning/phases/11-k-ytt-j-hallinta/11-PATTERNS.md
</context>

## Artifacts this phase produces

Tässä vaiheessa (Phase 11) syntyvät uudet symbolit — drift-verifioinnin tulee jättää nämä uusina luotuina huomiotta:

- Uusi funktio: `generate_password(int $length = 16): string` (`public/src/includes/helpers.php`)
- Uusi tiedosto: `public/admin/users.php` (listanäkymä)
- Uusi tiedosto: `public/admin/user_add.php` (Wave 2 / plan 11-02)
- Uusi tiedosto: `public/admin/user_edit.php` (Wave 2 / plan 11-02)
- Uusi tiedosto: `public/admin/user_reset_password.php` (Wave 2 / plan 11-03)
- Uusi tiedosto: `public/admin/user_toggle_active.php` (Wave 2 / plan 11-03)
- Uusi tiedosto: `public/admin/user_delete.php` (Wave 2 / plan 11-03)
- Muutettu: `public/admin/includes/admin_header.php` (uusi admin-only nav-linkki `users.php`:hen)
- Ei uusia DB-sarakkeita — `admin_users.role` (ENUM) ja `admin_users.is_active` (TINYINT) ovat jo olemassa Phase 10:stä.

<tasks>

<task type="auto">
  <name>Task 1: Lisää generate_password()-apufunktio helpers.php:hen</name>
  <files>public/src/includes/helpers.php</files>
  <read_first>
    - public/src/includes/helpers.php (nykyinen apufunktiokokoelma; katso generate_csrf_token()/validate_string() -tyyli ja funktioiden dokumentaatiokonventio)
    - public/admin/change_password.php (rivit 25-38: validate_string($new, 8, 255) ja password_hash(..., PASSWORD_BCRYPT, ['cost' => 12]) -malli jonka generoidun salasanan tulee läpäistä)
  </read_first>
  <action>
    Lisää `helpers.php`:hen uusi funktio `generate_password(int $length = 16): string` joka tuottaa kryptografisesti vahvan satunnaissalasanan. Käytä `random_bytes()`-pohjaista generointia (EI `rand()`/`mt_rand()`/`uniqid()`). Rakenna merkkijono charsetista joka sisältää isot ja pienet kirjaimet sekä numerot (A-Z, a-z, 0-9); valitse jokainen merkki `random_int(0, strlen($charset) - 1)`-indeksillä silmukassa `$length` kertaa. Oletuspituus 16 (täyttää reilusti D-10:n 8 merkin minimin). Palautettavan arvon tulee läpäistä `validate_string($pw, 8, 255)`. Sijoita funktio olemassa olevien apufunktioiden joukkoon samalla `/** ... */`-docblock-tyylillä kuin naapurifunktiot. Älä muuta olemassa olevia funktioita.
  </action>
  <verify>
    <automated>rg -q "function generate_password\(" public/src/includes/helpers.php && rg -q "random_(bytes|int)" public/src/includes/helpers.php</automated>
  </verify>
  <acceptance_criteria>
    - `public/src/includes/helpers.php` sisältää `function generate_password(`
    - Funktio käyttää `random_bytes` tai `random_int` (ei `rand(`/`mt_rand(`/`uniqid(`) — tarkistettavissa: `rg -n "rand\(|mt_rand\(|uniqid\(" public/src/includes/helpers.php` ei palauta uusia osumia generate_password-funktion rungossa
    - Oletuspituus on ≥ 8 merkkiä (allekirjoituksessa `int $length = 16` tai vastaava ≥ 8)
    - `php -l public/src/includes/helpers.php` palauttaa "No syntax errors" (jos php CLI saatavilla; muuten funktion syntaksi tarkistettu manuaalisesti)
  </acceptance_criteria>
  <done>generate_password() on olemassa, tuottaa random_bytes/random_int-pohjaisen ≥8-merkkisen salasanan, ei riko helpers.php:n syntaksia.</done>
</task>

<task type="auto">
  <name>Task 2: Lisää admin-only "Käyttäjähallinta"-nav-linkki admin_header.php:hen</name>
  <files>public/admin/includes/admin_header.php</files>
  <read_first>
    - public/admin/includes/admin_header.php (rivit 290-340: $role = currentRole(), nav-osiot, ja rivien 334-338 admin-only settings.php-linkkilohko joka on täsmämalli tälle)
    - public/src/includes/helpers.php (currentRole()/isAdmin() rivit 67-76)
  </read_first>
  <action>
    Lisää `admin_header.php`:n "Sivusto"-nav-osioon (rivin 334 `<div class="admin-nav-section">Sivusto</div>` jälkeen, `settings.php`-linkin viereen) uusi admin-only navigaatiolinkki käyttäjähallintaan. Kääri linkki täsmälleen samaan ehtoon kuin settings-linkki: `<?php if (in_array($role, ['admin'], true)): ?> ... <?php endif; ?>`. Linkin href on `<?= e(SITE_URL) ?>/admin/users.php`, teksti esim. "👥 Käyttäjät" (emoji vapaa valinta), ja `active`-luokka lasketaan `in_array($_activePage, ['users','user_add','user_edit'], true)`-ehdolla samaan tapaan kuin muut monisivuiset nav-kohdat (esim. contacts-lohko rivillä 302). Älä muuta olemassa olevia linkkejä. Sijoituspaikka on Claude's Discretion, mutta admin-only-ehto ei ole neuvoteltavissa (per D-05/USER-turvavaatimukset ja MOD-07/AUTHOR-05: mod/author eivät saa nähdä käyttäjähallintaa).
  </action>
  <verify>
    <automated>rg -q "admin/users.php" public/admin/includes/admin_header.php && rg -B2 "admin/users.php" public/admin/includes/admin_header.php | rg -q "in_array\(\\\$role, \['admin'\], true\)"</automated>
  </verify>
  <acceptance_criteria>
    - `admin_header.php` sisältää linkin `href=".../admin/users.php"`
    - Linkkiä välittömästi edeltää `if (in_array($role, ['admin'], true))` -ehto (linkki näkyy vain admin-roolille)
    - `active`-luokka aktivoituu myös user_add/user_edit-sivuilla (`$_activePage`-tarkistus sisältää 'users')
    - Olemassa olevien nav-linkkien määrä ei vähene (vain yksi lisätty)
  </acceptance_criteria>
  <done>Admin näkee sidebarissa käyttäjähallintalinkin, mod/author eivät; linkki osoittaa users.php:hen.</done>
</task>

<task type="auto">
  <name>Task 3: Rakenna users.php-listasivu table-näkymänä kaikin per-rivin toiminnoin</name>
  <files>public/admin/users.php</files>
  <read_first>
    - public/admin/contacts.php (list+flash+admin-page-header-runko, inline delete-lomakkeen CSRF-muoto rivit 66-73)
    - public/admin/change_password.php (admin_header.php/admin_footer.php include-konventio ja .admin-card-käyttö)
    - public/admin/includes/admin_header.php (rivi 7: $_activePage = basename(PHP_SELF); vahvistaa että tämän tiedoston aktiivinen sivu = 'users')
    - database/schema.sql (rivit 245-256: admin_users-sarakkeet id/username/role/is_active)
    - .planning/phases/11-k-ytt-j-hallinta/11-PATTERNS.md (users.php-osio: D-01 table-poikkeama ja per-rivin lomakemuoto)
  </read_first>
  <action>
    Luo `public/admin/users.php`. Alku: `require_once __DIR__ . '/../src/includes/db.php';` sitten `requireRole('admin');` (VAIN admin — koko users.*-perhe on admin-yksinoikeus, toisin kuin contacts.php:n 'admin','mod'). Hae `$db = getDB();` ja `SELECT id, username, role, is_active FROM admin_users ORDER BY role, username`.

    Flash-viestit `$_GET`-parametreista (ei-salaiset lopputulokset, contacts.php rivit 14-17 -malli): `?updated=1` → "Muutokset tallennettu.", `?deleted=1` → "Käyttäjä poistettu.", `?deactivated=1` → "Käyttäjä deaktivoitu.", `?activated=1` → "Käyttäjä aktivoitu.". Virhe-flashit `?err=`-arvoista `.flash-err`-lohkoon: `err=lastadmin` → "Viimeistä aktiivista admin-tunnusta ei voi poistaa tai deaktivoida.", `err=self` → "Et voi poistaa tai deaktivoida omaa tunnustasi.". ÄLÄ lue generoitua salasanaa `$_GET`:stä — sitä ei koskaan välitetä redirectin kautta (se näytetään inline user_add.php/user_reset_password.php:ssä, plan 11-02/11-03).

    Aseta `$pageTitle = 'Käyttäjähallinta';` ja `require __DIR__ . '/includes/admin_header.php';`. Renderöi `admin-page-header`-lohko otsikolla `<h1>Käyttäjähallinta</h1>` ja `page-actions`-diviin `<a href=".../admin/user_add.php" class="btn">+ Lisää käyttäjä</a>`. Sitten `<div class="admin-body"><?= $flash ?>`.

    Rakenna semanttinen HTML `<table>` (thead + tbody) — EI klikkaa-laajenna-ruudukkoa (D-01). Sarakkeet: Käyttäjänimi, Rooli, Tila, Toiminnot. Käytä olemassa olevia luokkia soluissa/napeissa (`.btn-sm`, `.btn-danger`, `.btn-edit`) — ei uutta CSS:ää. Jokaisella `<tbody>`-rivillä:
    - Tila-solu näyttää "Aktiivinen" jos `is_active` == 1, muuten "Deaktivoitu".
    - Toiminnot-solu (kaikki suoraan näkyvissä, ei klikkausta auki — D-01/D-06):
      1. Muokkaa: linkki `<a class="btn-sm btn-edit" href=".../admin/user_edit.php?id=<?= (int)$u['id'] ?>">`.
      2. Nollaa salasana: inline `<form method="post" action=".../admin/user_reset_password.php" style="display:inline">` piilokentin `csrf_token` (`generate_csrf_token()`) + `id`, submit-nappi `.btn-sm`. EI `confirm()`.
      3. Deaktivoi/Aktivoi: inline `<form method="post" action=".../admin/user_toggle_active.php" style="display:inline">` piilokentin `csrf_token` + `id`, nappiteksti "Deaktivoi" jos `is_active`==1 muuten "Aktivoi" (D-06). EI `confirm()`.
      4. Poista: inline `<form method="post" action=".../admin/user_delete.php" style="display:inline">` piilokentin `csrf_token` + `id`, submit-nappi `.btn-sm .btn-danger` jossa `onclick="return confirm('Poistetaanko käyttäjä <?= e(addslashes($u['username'])) ?>?')"` (D-07, sama konventio kuin contact_delete.php).

    Kaikki toiminnot pysyvät näkyvissä myös deaktivoidulle käyttäjälle (D-06 — ei piiloteta). Sulje `</div>` ja lopuksi `require __DIR__ . '/includes/admin_footer.php';`. Käytä `e()`-escapointia kaikessa tulostetussa datassa ja `(int)`-castia id-arvoissa.
  </action>
  <verify>
    <automated>rg -q "requireRole\('admin'\)" public/admin/users.php && rg -q "<table" public/admin/users.php && rg -q "user_add.php" public/admin/users.php && rg -q "user_edit.php" public/admin/users.php && rg -q "user_reset_password.php" public/admin/users.php && rg -q "user_toggle_active.php" public/admin/users.php && rg -q "user_delete.php" public/admin/users.php && rg -c "generate_csrf_token" public/admin/users.php</automated>
  </verify>
  <acceptance_criteria>
    - `public/admin/users.php` sisältää `requireRole('admin')` (ei 'mod'/'author')
    - Sivu sisältää semanttisen `<table>`-elementin (D-01: ei compact-list-ruudukkoa)
    - Jokaisella rivillä on neljä toimintoa: linkki `user_edit.php?id=`, inline-POST `user_reset_password.php`, inline-POST `user_toggle_active.php`, inline-POST `user_delete.php`
    - Jokainen inline-POST-lomake sisältää `generate_csrf_token()`-piilokentän (`rg -c "generate_csrf_token" public/admin/users.php` ≥ 3)
    - Vain poisto-napissa on `confirm(` (D-07); nollaa/toggle-napeissa ei
    - Tila-sarake näyttää aktiivinen/deaktivoitu `is_active`-arvon mukaan
    - `php -l public/admin/users.php` palauttaa "No syntax errors" (jos php CLI saatavilla)
  </acceptance_criteria>
  <done>Admin näkee taulukon kaikista käyttäjistä, kunkin rivin muokkaa/nollaa/toggle/poista-toiminnot suoraan näkyvissä CSRF-suojattuina; sivu on admin-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selain → admin PHP -päätepiste | Autentikoitu admin-sessio; kaikki syöte (GET/POST) epäluotettavaa |
| nav-renderöinti → roolisessio | `currentRole()` päättää mitä valikkokohtia näytetään |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Elevation of Privilege | users.php | high | mitigate | `requireRole('admin')` tiedoston alussa — mod/author ohjautuu ei-oikeutta.php:hen |
| T-11-02 | Information Disclosure | admin_header.php nav | medium | mitigate | Käyttäjähallintalinkki kääritty `in_array($role,['admin'],true)` — mod/author ei näe reittiä |
| T-11-06 | Information Disclosure | users.php flash | high | mitigate | Generoitua salasanaa ei koskaan lueta `$_GET`:stä eikä näytetä listasivulla; vain ei-salaiset liput (updated/deleted/toggled/err) kulkevat redirectissä |
| T-11-08 | Cross-site Scripting | users.php taulukkosolut | medium | mitigate | Kaikki käyttäjädata `e()`-escapoitu, id-arvot `(int)`-castattu, confirm()-teksti `addslashes()`+`e()` |
| T-11-SC | Tampering | pakettiasennukset | low | accept | Ei npm/pip/cargo-asennuksia tässä vaiheessa (natiivi PHP + MySQL); ei toimitusketjupintaa |
</threat_model>

<verification>
- `php -l` läpäisee kaikille kolmelle muutetulle/luodulle tiedostolle (jos php CLI saatavilla).
- users.php on admin-only ja renderöi table-näkymän kaikin per-rivin toiminnoin.
- generate_password() palauttaa random_bytes/random_int-pohjaisen ≥8-merkkisen salasanan.
- Nav-linkki näkyy vain admin-roolille.
</verification>

<success_criteria>
- Admin voi avata /admin/users.php ja nähdä taulukon kaikista admin_users-tunnuksista.
- generate_password() on käytettävissä wave 2:n add/reset-toiminnoille.
- Käyttäjähallintalinkki näkyy vain admin-navigaatiossa.
</success_criteria>

<output>
Create `.planning/phases/11-k-ytt-j-hallinta/11-01-SUMMARY.md` when done
</output>