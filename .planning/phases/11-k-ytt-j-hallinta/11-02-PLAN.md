---
phase: 11-kayttajahallinta
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - public/admin/user_add.php
  - public/admin/user_edit.php
autonomous: true
requirements: [USER-01, USER-02, USER-07]
must_haves:
  truths:
    - "Admin luo uuden tunnuksen antamalla vain käyttäjänimen ja roolin; järjestelmä generoi vahvan salasanan ja näyttää sen kerran onnistumisruudulla, ja uusi käyttäjä voi kirjautua heti."
    - "Admin muokkaa olemassa olevan käyttäjän käyttäjänimeä ja roolia; muutos vaikuttaa oikeuksiin seuraavalla pyynnöllä."
    - "Admin ei voi muuttaa omaa rooliaan pois administilta (D-05/USER-07)."
  artifacts:
    - public/admin/user_add.php
    - public/admin/user_edit.php
  key_links:
    - "user_add.php ja user_edit.php käyttävät generate_password()/validate_string()/validate_csrf_token()/password_hash() -apuvälineitä (plan 11-01 + helpers.php)."
    - "Molemmat POSTaavat itselleen ja user_edit.php redirectaa users.php?updated=1; user_add.php näyttää salasanan inline (ei redirect)."
---

<objective>
Toteuta käyttäjän luonti (USER-01) ja muokkaus (USER-02) sekä oman roolin suoja (USER-07/D-05). `user_add.php` generoi vahvan salasanan palvelinpuolella (D-02) ja näyttää sen kerran kopioitavaksi (ei salasanakenttää lomakkeessa, ei plaintext-tallennusta). `user_edit.php` muokkaa käyttäjänimeä + roolia ja estää adminia alentamasta omaa rooliaan.

Purpose: Nämä ovat käyttäjähallinnan kaksi kirjoittavaa lomaketta. Molemmat mirroroivat olemassa olevaa contact_add/contact_edit + change_password -malleja mutta admin-only-suojauksella ja uudella turvalogiikalla.
Output: `public/admin/user_add.php` (uusi), `public/admin/user_edit.php` (uusi).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-k-ytt-j-hallinta/11-CONTEXT.md
@.planning/phases/11-k-ytt-j-hallinta/11-PATTERNS.md
</context>

## Artifacts this phase produces

Uudet symbolit joita drift-verifioinnin ei tule merkitä tuntemattomiksi:

- Uusi tiedosto: `public/admin/user_add.php` (luontilomake + inline salasanan kertanäyttö)
- Uusi tiedosto: `public/admin/user_edit.php` (muokkauslomake + D-05 oman roolin suoja)
- Käyttää plan 11-01:ssä luotua `generate_password()`-funktiota ja olemassa olevia `validate_string()`/`validate_csrf_token()`/`generate_csrf_token()`/`sanitize()`/`e()`/`redirect()`-apufunktioita.
- Ei uusia DB-sarakkeita — INSERT/UPDATE olemassa olevaan `admin_users`-tauluun (username, password, role, is_active).

<tasks>

<task type="auto">
  <name>Task 1: Luo user_add.php — käyttäjän luonti + generoidun salasanan kertanäyttö</name>
  <files>public/admin/user_add.php</files>
  <read_first>
    - public/admin/contact_add.php (guard + CSRF + validate + form-runko; INSERT-malli rivit 30-44)
    - public/admin/change_password.php (inline-$success/$errors-näyttö rivit 45-53, password_hash(..., PASSWORD_BCRYPT, ['cost' => 12]) rivi 33)
    - public/src/includes/helpers.php (generate_password() plan 11-01:stä, validate_string(), validate_csrf_token(), sanitize(), e(), redirect())
    - database/schema.sql (rivit 245-256: admin_users-sarakkeet ja UNIQUE uk_admin_username)
    - .planning/phases/11-k-ytt-j-hallinta/11-PATTERNS.md (user_add.php-osio)
  </read_first>
  <action>
    Luo `public/admin/user_add.php`. Alku: `require_once __DIR__ . '/../src/includes/db.php';` sitten `requireRole('admin');`. Alusta `$db = getDB();`, `$errors = [];`, `$success = false;`, `$generatedPassword = '';`, ja lomakedata `$f = ['username' => '', 'role' => 'author'];`.

    POST-käsittely (`$_SERVER['REQUEST_METHOD'] === 'POST'`): tarkista `validate_csrf_token($_POST['csrf_token'] ?? '')`, virheellä lisää `$errors[] = 'Virheellinen pyyntö.'`. Muuten `$f['username'] = sanitize($_POST['username'] ?? '')`, `$f['role'] = sanitize($_POST['role'] ?? '')`. Validoi: `validate_string($f['username'], 1, 255)` (lisää palautettu virhe jos ei valid); tarkista `in_array($f['role'], ['admin','mod','author'], true)`, muuten `$errors[] = 'Virheellinen rooli.'`. Uniikkiustarkistus: prepared `SELECT COUNT(*) FROM admin_users WHERE username = :username`; jos > 0, `$errors[] = 'Käyttäjänimi on jo käytössä.'`.

    Jos `empty($errors)`: kutsu `$generatedPassword = generate_password();` (plan 11-01), `$hash = password_hash($generatedPassword, PASSWORD_BCRYPT, ['cost' => 12]);`, ja prepared `INSERT INTO admin_users (username, password, role, is_active) VALUES (:username, :hash, :role, 1)`. ÄLÄ redirectaa — aseta `$success = true;` ja renderöi salasana inline (change_password.php-malli), koska generoitua plaintext-salasanaa ei saa välittää `$_GET`:n kautta (näkyisi URL:ssa/historiassa/Refererissä). Älä logita eikä tallenna plaintext-salasanaa mihinkään muualle kuin tähän kertaluontoiseen HTTP-vastaukseen.

    Aseta `$pageTitle = 'Lisää käyttäjä';` ja `require __DIR__ . '/includes/admin_header.php';`. Renderöi `admin-page-header` back-linkillä users.php:hen ja otsikolla. `admin-body`-lohkoon: jos `$success`, näytä `<div class="admin-card"><p class="flash-ok">Käyttäjä luotu. Salasana (näytetään vain kerran): <code><?= e($generatedPassword) ?></code></p><a href=".../admin/users.php" class="btn">Takaisin</a></div>` EIKÄ näytä lomaketta uudelleen. Jos `$errors`, näytä `.flash-err`-lista. Muutoin renderöi lomake: `csrf_token`-piilokenttä, `username`-tekstikenttä (`.form-group`), `role`-select (vaihtoehdot admin/mod/author), submit `<button class="btn">` + peruuta-linkki. EI salasanakenttää (D-02). Lopuksi `require __DIR__ . '/includes/admin_footer.php';`.
  </action>
  <verify>
    <automated>rg -q "requireRole\('admin'\)" public/admin/user_add.php && rg -q "generate_password\(" public/admin/user_add.php && rg -q "PASSWORD_BCRYPT" public/admin/user_add.php && rg -q "INSERT INTO admin_users" public/admin/user_add.php && rg -q "SELECT COUNT\(\*\) FROM admin_users WHERE username" public/admin/user_add.php && ! rg -q "type=\"password\"" public/admin/user_add.php</automated>
  </verify>
  <acceptance_criteria>
    - `user_add.php` sisältää `requireRole('admin')`
    - Lomake EI sisällä salasanakenttää (`rg "type=\"password\"" public/admin/user_add.php` ei osu) — D-02
    - Käyttää `generate_password(` + `password_hash(..., PASSWORD_BCRYPT, ['cost' => 12])`
    - Sisältää käyttäjänimen uniikkiustarkistuksen `SELECT COUNT(*) FROM admin_users WHERE username`
    - INSERT sisältää `is_active` arvolla 1 (uusi käyttäjä voi kirjautua heti)
    - Onnistumispolku näyttää salasanan inline (`$success`/`$generatedPassword`) eikä käytä `redirect()` salasanan välittämiseen — `rg "added=1|password=" public/admin/user_add.php` ei paljasta salasanaa GET-parametrissa
    - `php -l public/admin/user_add.php` palauttaa "No syntax errors" (jos php CLI saatavilla)
  </acceptance_criteria>
  <done>Admin luo tunnuksen käyttäjänimellä+roolilla, saa generoidun salasanan kertanäytöllä, ja uusi tunnus (is_active=1) voi kirjautua heti annetulla roolilla.</done>
</task>

<task type="auto">
  <name>Task 2: Luo user_edit.php — käyttäjänimen + roolin muokkaus ja oman roolin suoja (D-05)</name>
  <files>public/admin/user_edit.php</files>
  <read_first>
    - public/admin/contact_edit.php (fetch-by-id-or-redirect rivit 5-12; UPDATE + CSRF + validointi rivit 17-56; validointien sijoituspaikka ennen if(empty($errors)))
    - public/src/includes/helpers.php (validate_string(), validate_csrf_token(), sanitize(), e(), redirect())
    - database/schema.sql (rivit 245-256: admin_users)
    - .planning/phases/11-k-ytt-j-hallinta/11-PATTERNS.md (user_edit.php-osio, erityisesti D-05 self-protection -lisälogiikka)
  </read_first>
  <action>
    Luo `public/admin/user_edit.php`. Alku: `require_once __DIR__ . '/../src/includes/db.php';` sitten `requireRole('admin');`. Lue `$id = (int)($_GET['id'] ?? 0);` ja jos `$id <= 0` `redirect(SITE_URL . '/admin/users.php');`. Hae prepared `SELECT * FROM admin_users WHERE id = :id`; jos rivi puuttuu, `redirect(SITE_URL . '/admin/users.php');`. Alusta `$errors = [];` ja `$f = $row;` (esitäytä lomake nykyarvoilla).

    POST-käsittely: tarkista `validate_csrf_token($_POST['csrf_token'] ?? '')`. Muuten `$f['username'] = sanitize($_POST['username'] ?? '')`, `$f['role'] = sanitize($_POST['role'] ?? '')`. Validoi: `validate_string($f['username'], 1, 255)`; `in_array($f['role'], ['admin','mod','author'], true)` muuten virhe. Uniikkiustarkistus muut kuin oma rivi: prepared `SELECT COUNT(*) FROM admin_users WHERE username = :username AND id != :id`; jos > 0, `$errors[] = 'Käyttäjänimi on jo käytössä.'`.

    D-05 oman roolin suoja (USER-07, uusi logiikka — ei olemassa olevaa analogia): jos `$id === (int)$_SESSION['admin_id'] && $f['role'] !== 'admin'`, lisää `$errors[] = 'Et voi muuttaa omaa rooliasi pois administilta.'`. Sijoita tämä muiden validointien joukkoon ennen `if (empty($errors))`-lohkoa.

    Jos `empty($errors)`: prepared `UPDATE admin_users SET username = :username, role = :role WHERE id = :id`, sitten `redirect(SITE_URL . '/admin/users.php?updated=1');`. ÄLÄ muuta password-saraketta tässä lomakkeessa (salasana hoidetaan erillisellä nollaus-toiminnolla, plan 11-03).

    Aseta `$pageTitle = 'Muokkaa käyttäjää';` ja `require __DIR__ . '/includes/admin_header.php';`. Renderöi `admin-page-header` back-linkillä + otsikolla, `.flash-err`-lohko jos `$errors`, ja lomake (`csrf_token`-piilokenttä, `username`-tekstikenttä esitäytettynä `e($f['username'])`, `role`-select nykyinen valittuna, submit + peruuta). Käytä `e()`-escapointia. Lopuksi `require __DIR__ . '/includes/admin_footer.php';`.
  </action>
  <verify>
    <automated>rg -q "requireRole\('admin'\)" public/admin/user_edit.php && rg -q "UPDATE admin_users SET username" public/admin/user_edit.php && rg -q "admin_id" public/admin/user_edit.php && rg -q "users.php\?updated=1" public/admin/user_edit.php && ! rg -q "UPDATE admin_users SET password" public/admin/user_edit.php</automated>
  </verify>
  <acceptance_criteria>
    - `user_edit.php` sisältää `requireRole('admin')`
    - Hakee kohteen `SELECT * FROM admin_users WHERE id` ja redirectaa users.php:hen jos id puuttuu/virheellinen
    - UPDATE koskee vain `username` + `role` -sarakkeita — EI `password`-saraketta (`rg "UPDATE admin_users SET password" public/admin/user_edit.php` ei osu)
    - Sisältää D-05 oman roolin suojan: tarkistus `$_SESSION['admin_id']` vs `$id` ja `role !== 'admin'` → virhe eikä UPDATE:a suoriteta
    - Käyttäjänimen uniikkiustarkistus sulkee pois oman rivin (`AND id != :id`)
    - Onnistuessa redirect `users.php?updated=1`
    - `php -l public/admin/user_edit.php` palauttaa "No syntax errors" (jos php CLI saatavilla)
  </acceptance_criteria>
  <done>Admin muokkaa käyttäjänimeä+roolia; muutos tallentuu ja vaikuttaa oikeuksiin seuraavalla pyynnöllä; admin ei voi alentaa omaa rooliaan pois administilta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selain → user_add.php / user_edit.php | Autentikoitu admin POST; syöte (username, role, id) epäluotettavaa |
| palvelin → HTTP-vastaus (kertaluontoinen salasana) | Generoitu plaintext-salasana ylittää tämän rajan vain kerran |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Elevation of Privilege | molemmat tiedostot | high | mitigate | `requireRole('admin')` — mod/author estetään |
| T-11-02 | Tampering (CSRF) | POST-lomakkeet | high | mitigate | `validate_csrf_token()` ennen kaikkia INSERT/UPDATE-toimia |
| T-11-03 | Elevation of Privilege | user_edit.php roolin muokkaus | high | mitigate | D-05: admin ei voi alentaa omaa rooliaan pois administilta → estää itselukituksen |
| T-11-06 | Information Disclosure | user_add.php salasana | high | mitigate | Generoitu salasana näytetään vain inline HTTP-vastauksessa; ei `$_GET`-välitystä, ei lokitusta, ei plaintext-tallennusta (vain bcrypt DB:hen) |
| T-11-07 | Spoofing | uniikki käyttäjänimi | medium | mitigate | Uniikkiustarkistus + DB UNIQUE uk_admin_username estävät duplikaattitunnukset |
| T-11-08 | Cross-site Scripting | lomakkeiden esitäyttö | medium | mitigate | `e()`-escapointi kaikessa tulostetussa datassa |
| T-11-SC | Tampering | pakettiasennukset | low | accept | Ei npm/pip/cargo-asennuksia — natiivi PHP, ei toimitusketjupintaa |
</threat_model>

<verification>
- Uusi käyttäjä luodaan käyttäjänimellä+roolilla, generoitu salasana näytetään kerran, tunnus voi kirjautua heti.
- Muokkaus tallentaa käyttäjänimen+roolin eikä koske salasanaan.
- Oman roolin alennus estetään D-05-tarkistuksella.
- CSRF-suojaus ja e()-escapointi molemmissa tiedostoissa.
</verification>

<success_criteria>
- USER-01: admin luo tunnuksen; uusi käyttäjä kirjautuu heti annetulla roolilla.
- USER-02: admin muokkaa roolia ja käyttäjänimeä; muutos vaikuttaa oikeuksiin.
- USER-07/D-05: admin ei voi muuttaa omaa rooliaan pois administilta.
</success_criteria>

<output>
Create `.planning/phases/11-k-ytt-j-hallinta/11-02-SUMMARY.md` when done
</output>
