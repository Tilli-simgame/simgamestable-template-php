---
phase: 12-sis-lt-tyyppien-roolirajaus
plan: 2
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - public/admin/posts.php
  - public/src/includes/helpers.php
autonomous: true
requirements: [MOD-01, MOD-02, MOD-03, MOD-04, MOD-05, MOD-07, AUTHOR-01, AUTHOR-02, AUTHOR-04, AUTHOR-05]
must_haves:
  truths:
    - "Author näkee posts.php-listassa VAIN omat postauksensa (WHERE author_id = kirjautuneen id kun currentRole()==='author', D-03)"
    - "Author joka avaa toisen postauksen suoralla osoitteella action=edit&id=X ohjautuu ei-oikeutta.php:hen (SC3)"
    - "Crafted POST toisen postauksen edit_id:llä EI päivitä postausta — ohjautuu ei-oikeutta.php:hen ennen UPDATE-lausetta (IDOR defense-in-depth, Pitfall 2)"
    - "Jokainen uusi postaus (mikä tahansa rooli) tallentuu author_id = $_SESSION['admin_id'] (D-02)"
    - "Admin ja mod näkevät ja voivat muokata KAIKKIA postauksia ilman omistajuusrajausta"
    - "Author voi linkittää olemassa olevia hevosia postaukseensa muttei muokata hevostietoja (AUTHOR-04, hevoslinkitys-widget säilyy ennallaan)"
    - "Mod ja author eivät pääse users.php/settings.php-sivuille edes suoralla osoitteella (MOD-07/AUTHOR-05, Phase 10:n requireRole('admin') voimassa)"
  artifacts:
    - "public/admin/posts.php — omistajuuslogiikka (INSERT author_id, listasuodatus, GET/POST ownership-tarkistus)"
    - "public/src/includes/helpers.php — requireOwnResourceOrAdmin() apufunktio"
  key_links:
    - "posts.php ownership check → redirect(SITE_URL . '/admin/ei-oikeutta.php') (sama kohde kuin requireRole())"
    - "currentRole() / $_SESSION['admin_id'] (helpers.php) → author_id-vertailu"
    - "posts.author_id (plan 12-01) → INSERT/SELECT/UPDATE posts.php:ssä"
---

<objective>
Toteuta postausten omistajuuslogiikka `public/admin/posts.php`:ssä ja lisää `requireOwnResourceOrAdmin()`-apufunktio `helpers.php`:hen: author näkee/muokkaa vain omia postauksiaan (AUTHOR-02, D-03), uusi postaus saa author_id:n (D-02), suora-URL- ja crafted-POST-omistajuusyritykset estetään (SC3 + IDOR defense-in-depth). Lisäksi verifioi grep-passilla ettei Phase 10:n mod/author-sivutason roolisuojaus ole muuttunut (D-04, MOD-01..05/MOD-07/AUTHOR-01/AUTHOR-05).

Purpose: Tämä on Phase 12:n varsinainen uusi toiminnallisuus. Mod/author-sisältöoikeudet (hevoset/varsat/kilpailut/näyttelyt/kuvat = admin+mod, postaukset = kaikki roolit) ovat jo rakenteellisesti valmiit Phase 10:stä — tämä plan lisää VAIN puuttuvan omistajuuskerroksen postauksiin ja vahvistaa muun rajauksen ennallaan pysymisen.
Output: Muokattu posts.php + helpers.php, ja verifiointi-evidenssi roolirajauksen ennallaan pysymisestä.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sis-lt-tyyppien-roolirajaus/12-CONTEXT.md
@.planning/phases/12-sis-lt-tyyppien-roolirajaus/12-PATTERNS.md
@public/admin/posts.php
@public/src/includes/helpers.php
@public/admin/ei-oikeutta.php
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Lisää requireOwnResourceOrAdmin()-apufunktio helpers.php:hen</name>
  <files>public/src/includes/helpers.php</files>
  <read_first>
    - public/src/includes/helpers.php rivit 64-102 — currentRole() (rivit 67-69), isAdmin() (rivit 74-76), requireRole() (rivit 84-102): konventio — ei paluuarvoa, redirect(SITE_URL . '/admin/ei-oikeutta.php') epäonnistuessa, pieni currentRole()-pohjainen check.
    - public/admin/ei-oikeutta.php — redirectin kohdesivu (olemassa oleva, Phase 10, ei uutta sivua/viestiä).
  </read_first>
  <action>
    Lisää `helpers.php`:hen (isAdmin():n / requireRole():n viereen) uusi funktio `requireOwnResourceOrAdmin(int $resourceAuthorId): void`. Toteutus Claude's Discretion -päätöksen mukaan (12-CONTEXT.md): jos `currentRole() === 'author'` JA `$resourceAuthorId !== (int)($_SESSION['admin_id'] ?? 0)`, kutsu `redirect(SITE_URL . '/admin/ei-oikeutta.php')`. Muut roolit (admin, mod) läpäisevät aina — he EIVÄT ole omistajuusrajattuja (D-02/D-03-perustelu). Funktio ei palauta arvoa ja seuraa täsmälleen requireRole():n redirect-kohdetta ja käyttäytymismallia. Tunnistus perustuu AINA $_SESSION['admin_id']:hen, ei koskaan POST-syötettyyn käyttäjä-ID:hen (Spoofing-suoja).
  </action>
  <acceptance_criteria>
    - `grep -n "function requireOwnResourceOrAdmin" public/src/includes/helpers.php` palauttaa osuman.
    - Funktion runko sisältää `currentRole() === 'author'` -ehdon ja vertailun `$_SESSION['admin_id']`:hen (grep löytää molemmat funktion alueelta).
    - Funktio redirectaa ei-oikeutta.php:hen: grep löytää `ei-oikeutta.php` funktion rungosta.
    - PHP-syntaksi kelvollinen: `docker exec virtuaalitalli-web php -l /var/www/html/src/includes/helpers.php` tulostaa "No syntax errors detected".
  </acceptance_criteria>
  <verify>
    <automated>docker exec virtuaalitalli-web php -l /var/www/html/src/includes/helpers.php 2>&1 | grep -q "No syntax errors" && grep -q "function requireOwnResourceOrAdmin" public/src/includes/helpers.php && echo OK</automated>
  </verify>
  <done>requireOwnResourceOrAdmin() lisätty helpers.php:hen, syntaksi kelvollinen, redirectaa ei-oikeutta.php:hen vain author-roolille vieraan resurssin kohdalla.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Omistajuuslogiikka posts.php:hen (INSERT author_id, listasuodatus, GET/POST ownership-esto)</name>
  <files>public/admin/posts.php</files>
  <read_first>
    - public/admin/posts.php koko tiedosto (1-408) — erityisesti: rivi 3 requireRole('admin','mod','author') (EI muuteta), rivit 38-48 POST UPDATE/INSERT-haara, rivit 70-87 GET action=edit-haara, rivi 105 listakysely, rivit 107-131 + 254-407 hevoslinkitys-widget (säilyy ennallaan, AUTHOR-04 — jo roolineutraali).
    - public/src/includes/helpers.php — requireOwnResourceOrAdmin() (Task 1), currentRole(), $_SESSION['admin_id'].
    - .planning/phases/12-sis-lt-tyyppien-roolirajaus/12-PATTERNS.md rivit 30-95 — täsmälliset muutoskohdat ja mallit kullekin haaralle.
  </read_first>
  <action>
    Neljä muutosta posts.php:hen, kaikki AFTER CSRF-validoinnin (ei ohita sitä):

    (1) INSERT (rivit 43-47, D-02): lisää `author_id`-sarake INSERT-lauseeseen ja sido arvoksi `$_SESSION['admin_id']`. Lause muotoon INSERT INTO posts (title, slug, content, author_id) VALUES (:t,:s,:c,:aid). JOKAINEN uusi postaus rooliin katsomatta saa author_id:n = kirjautuneen id (ei roolikohtaista haaraa).

    (2) POST UPDATE ownership (rivit 38-42, IDOR defense-in-depth / Pitfall 2): ENNEN UPDATE-lauseen ajoa, kun $edit_id > 0, hae kohdepostauksen author_id (SELECT author_id FROM posts WHERE id = :id) ja kutsu Task 1:n `requireOwnResourceOrAdmin((int)$row['author_id'])`. Tämä estää crafted-POST-pyynnön toisen author-käyttäjän postauksen ID:llä — admin/mod läpäisevät, author vain omansa. Suojaus on erillinen GET-side-tarkistuksesta (crafted POST ei kulje GET-näkymän kautta).

    (3) GET action=edit ownership (rivit 70-87, SC3): kun $editPost on haettu ja se on olemassa, kutsu `requireOwnResourceOrAdmin((int)$editPost['author_id'])` ennen lomakkeen esitäyttöä. Suora-URL-yritys action=edit&id=X toisen postaukseen ohjautuu ei-oikeutta.php:hen (author kirjoittaa/arvaa vieraan ID:n URL:iin).

    (4) Listakysely (rivi 105, D-03): kun `currentRole() === 'author'`, aja parametrisoitu kysely SELECT id,title,slug,created_at FROM posts WHERE author_id = :aid ORDER BY created_at DESC sidonnalla :aid => $_SESSION['admin_id']; muutoin (admin/mod) säilytä nykyinen suodattamaton kysely. Author ei näe edes että muita postauksia on — siksi rivikohtaista Muokkaa-napin piilotusta EI tarvita.

    Hevoslinkitys-widget (rivit 107-131, 183-192, 254-407) EI muutu — se on jo roolineutraali ja tukee AUTHOR-04:ää sellaisenaan (author valitsee olemassa olevia hevosia listalta, ei muokkaa hevostietoja). Sivutason requireRole('admin','mod','author') (rivi 3) EI muutu.
  </action>
  <acceptance_criteria>
    - INSERT sisältää author_id:n: `grep -n "author_id" public/admin/posts.php` osuu INSERT-lauseeseen ja sidonta käyttää $_SESSION['admin_id'] (grep löytää molemmat).
    - POST UPDATE -haara kutsuu requireOwnResourceOrAdmin ennen UPDATE-lausetta: grep löytää `requireOwnResourceOrAdmin` posts.php:stä (vähintään 2 kutsua — POST- ja GET-haara).
    - Listakysely suodattaa author-roolille: grep löytää `currentRole() === 'author'` ja `WHERE author_id` posts.php:stä.
    - GET action=edit -haara kutsuu requireOwnResourceOrAdmin editPost-haun jälkeen (source-tarkistus: kutsu esiintyy $editPost-fetchin jälkeen, ennen $f-esitäyttöä).
    - PHP-syntaksi kelvollinen: `docker exec virtuaalitalli-web php -l /var/www/html/admin/posts.php` tulostaa "No syntax errors detected".
  </acceptance_criteria>
  <verify>
    <automated>docker exec virtuaalitalli-web php -l /var/www/html/admin/posts.php 2>&1 | grep -q "No syntax errors" && grep -q "WHERE author_id" public/admin/posts.php && [ "$(grep -c "requireOwnResourceOrAdmin" public/admin/posts.php)" -ge 2 ] && echo OK</automated>
  </verify>
  <done>posts.php: INSERT asettaa author_id:n (D-02), lista suodattuu author-roolille (D-03), GET edit + POST update estävät vieraan postauksen omistajuustarkistuksella (SC3 + IDOR), hevoswidget ja sivugate ennallaan. Syntaksi kelvollinen.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verifioi mod/author-sivutason roolirajaus ennallaan (D-04) + omistajuuskäyttäytymisen UAT</name>
  <files>public/admin/posts.php</files>
  <read_first>
    - .planning/phases/10-roolit-ja-autentikaation-perusta/10-CONTEXT.md — D-01 täysi sivukartoitus: mitkä sivut sallivat mod:n (admin+mod) ja mitkä ovat admin-only (users/settings).
    - public/admin/posts.php, public/src/includes/helpers.php — Task 1-2:n tulos verifioinnin kohteena.
  </read_first>
  <action>
    Aja D-04:n verifiointipassi: grep `requireRole` koko `public/admin/`-hakemistosta ja vahvista Phase 10:n rajaus on ennallaan — sisältösivut (horses.php/horse_add.php/horse_edit.php/foals.php/foal_add.php/foal_edit.php/competitions.php/showrecords.php/photos.php/photo_update.php/horse_import_vrl.php) sallivat 'admin','mod' (MOD-01..04), posts.php sallii 'admin','mod','author' (MOD-05/AUTHOR-01), ja users.php/settings.php sekä user_*.php-perhe ovat requireRole('admin') (MOD-07/AUTHOR-05). Kirjaa grep-tulos SUMMARY:yn evidenssiksi. Uutta koodia EI odoteta tälle alueelle — jos jokin gate on muuttunut Phase 10/11:n jälkeen, se on regressio joka nostetaan esiin.

    Kirjaa lisäksi SUMMARY:yn manuaalisen UAT:n ohjeet (ihmisen ajettavaksi gsd-verify-work-vaiheessa, session-pohjaista käyttäytymistä ei voi luotettavasti automatisoida): (a) kirjaudu author-tunnuksella → posts.php-lista näyttää vain omat postaukset; (b) avaa toisen author-käyttäjän postaus osoitteella posts.php?action=edit&id=X → ohjautuu ei-oikeutta.php:hen; (c) lähetä crafted POST toisen postauksen edit_id:llä → EI päivity, ohjautuu ei-oikeutta.php:hen; (d) admin ja mod näkevät ja muokkaavat kaikkia postauksia; (e) mod ja author avaavat users.php/settings.php suoralla osoitteella → ei-oikeutta.php.
  </action>
  <acceptance_criteria>
    - Sisältösivujen mod-pääsy ennallaan: `grep -rn "requireRole('admin', 'mod')" public/admin/horses.php public/admin/foals.php public/admin/competitions.php public/admin/showrecords.php` palauttaa osuman kussakin (MOD-01..04).
    - posts.php sallii kaikki kolme roolia: `grep -n "requireRole('admin', 'mod', 'author')" public/admin/posts.php` palauttaa osuman (MOD-05/AUTHOR-01).
    - Käyttäjähallinta + asetukset admin-only: `grep -n "requireRole('admin')" public/admin/users.php public/admin/settings.php` palauttaa osuman kummassakin (MOD-07/AUTHOR-05).
    - SUMMARY sisältää grep-evidenssin roolirajauksesta JA manuaalisen UAT-tarkistuslistan (a)-(e).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "requireRole('admin', 'mod', 'author')" public/admin/posts.php && grep -q "requireRole('admin')" public/admin/users.php && grep -q "requireRole('admin')" public/admin/settings.php && echo GATES_OK</automated>
    <human-check>Aja UAT-tarkistuslista (a)-(e): author näkee vain omat postaukset; vieraan postauksen edit-URL ja crafted-POST → ei-oikeutta.php; admin/mod näkevät kaikki; mod/author eivät pääse users.php/settings.php:hen.</human-check>
  </verify>
  <done>D-04-grep-passi vahvistaa Phase 10:n roolirajauksen ennallaan (mod-sisältösivut, all-roles-posts, admin-only-users/settings) ilman regressiota; manuaalinen UAT-tarkistuslista dokumentoitu SUMMARY:yn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selain (author, epäluotettu) → admin/posts.php | Kirjautunut author-rooli; rooli- ja omistajuusrajaus PAKKO tehdä palvelinpuolella, ei piilottamalla linkkejä |
| crafted POST → posts.php UPDATE-käsittelijä | Hyökkääjä voi lähettää mielivaltaisen edit_id:n POST-rungossa GET-näkymän ohi |
| POST-runko (horse_ids, edit_id) → tietokanta | Käyttäjän toimittamat arvot; identiteetti johdettava aina sessiosta, ei POST:sta |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02-01 | Elevation of Privilege | posts.php GET action=edit -haara | high | mitigate | requireOwnResourceOrAdmin($editPost['author_id']) $editPost-fetchin jälkeen → author vieraan postauksen edit-URL:iin ohjautuu ei-oikeutta.php:hen (SC3, Task 2 muutos 3) |
| T-12-02-02 | Tampering / Elevation | posts.php POST UPDATE -haara | high | mitigate | Hae kohteen author_id ja kutsu requireOwnResourceOrAdmin ENNEN UPDATE-lausetta — crafted POST toisen postauksen ID:llä estyy (IDOR defense-in-depth, Pitfall 2, Task 2 muutos 2) |
| T-12-02-03 | Information Disclosure | posts.php listakysely | medium | mitigate | WHERE author_id = :aid kun currentRole()==='author' — author ei näe muiden postausten olemassaoloa (D-03, Task 2 muutos 4) |
| T-12-02-04 | Spoofing | author_id-lähde INSERT/omistajuustarkistuksissa | high | mitigate | author_id ja omistajuusvertailu johdetaan AINA $_SESSION['admin_id']:stä, ei koskaan POST-syötteestä (Task 1 + Task 2) |
| T-12-02-05 | Elevation of Privilege | users.php/settings.php suora-URL mod/author | high | mitigate | Phase 10:n requireRole('admin') voimassa; verifioidaan grep-passilla (Task 3, MOD-07/AUTHOR-05) |
| T-12-02-06 | Bypass | CSRF-validoinnin ohitus uudessa ownership-haarassa | medium | mitigate | Ownership-tarkistus sijoitetaan CSRF-validoinnin JÄLKEEN POST-haarassa, ei sen ohi (Task 2 action-huomautus) |
| T-12-SC | Tampering | npm/pip/cargo-asennukset | low | accept | Tässä vaiheessa ei asenneta paketteja (pelkkä PHP-muokkaus). Ei package-legitimacy-riskiä. |
</threat_model>

<verification>
- php -l läpäisee posts.php:lle ja helpers.php:lle.
- grep vahvistaa: author_id INSERT:issä, WHERE author_id -listasuodatus, ≥2 requireOwnResourceOrAdmin-kutsua posts.php:ssä, requireOwnResourceOrAdmin helpers.php:ssä.
- grep vahvistaa Phase 10 -roolirajaus ennallaan (mod-sisältösivut, all-roles-posts, admin-only users/settings).
- Manuaalinen UAT (a)-(e) dokumentoitu ja ajettavissa gsd-verify-work-vaiheessa.
</verification>

<success_criteria>
- Author näkee/muokkaa vain omia postauksiaan; vieraan postauksen GET-edit ja crafted-POST estyvät ei-oikeutta.php:hen (AUTHOR-02, SC3, IDOR).
- Uusi postaus tallentuu author_id = kirjautuneen id (D-02).
- Author voi linkittää hevosia postaukseen muokkaamatta hevostietoja (AUTHOR-04, widget ennallaan).
- Mod/admin toimivat rajoituksetta postauksissa ja mod hallinnoi sisältöä; mod/author eivät pääse käyttäjähallintaan/teema-asetuksiin (MOD-01..05/MOD-07/AUTHOR-05, verifioitu).
</success_criteria>

## Artifacts this phase produces

Tämän suunnitelman (12-02) tuottamat uudet/muutetut symbolit:

- **`requireOwnResourceOrAdmin(int $resourceAuthorId): void`** (uusi funktio) — `public/src/includes/helpers.php`; omistajuustarkistus joka ohjaa authorin vieraan resurssin kohdalla ei-oikeutta.php:hen.
- **`public/admin/posts.php`** (muokattu) — omistajuuslogiikka: INSERT asettaa author_id = $_SESSION['admin_id'] (D-02); listakysely suodattuu author-roolille (WHERE author_id, D-03); GET action=edit + POST UPDATE -haarat kutsuvat requireOwnResourceOrAdmin (SC3 + IDOR defense-in-depth).

Kuluttaa 12-01:n tuottaman `posts.author_id`-sarakkeen. (Migraatio ja sarake tuotetaan suunnitelmassa 12-01.)

<output>
Create `.planning/phases/12-sis-lt-tyyppien-roolirajaus/12-02-SUMMARY.md` when done
</output>
