---
phase: 12-sis-lt-tyyppien-roolirajaus
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrate_posts_author.sql
  - database/schema.sql
autonomous: true
requirements: [AUTHOR-02, AUTHOR-04]
must_haves:
  truths:
    - "posts-taulussa on author_id-sarake, joka viittaa admin_users.id:hen (FK fk_posts_author)"
    - "Kaikki olemassa olevat postaukset on backfillattu admin-tunnukselle — ei yhtään NULL author_id -riviä jää olemassa olevista postauksista (D-01)"
    - "database/schema.sql:n posts-lohko sisältää author_id-sarakkeen fresh-install-polkua varten (sama konventio kuin migrate_roles.sql → schema.sql admin_users-lohko)"
  artifacts:
    - database/migrate_posts_author.sql
    - "posts.author_id -sarake dev-tietokannassa (virtuaalitalli-db)"
  key_links:
    - "posts.author_id → admin_users.id (FK fk_posts_author, ON DELETE SET NULL)"
---

<objective>
Lisää `posts.author_id`-sarake postausten omistajuuden perustaksi: uusi migraatiotiedosto (`database/migrate_posts_author.sql`) migrate_roles.sql-konvention mukaan, taannehtiva backfill kaikille olemassa oleville postauksille (D-01), `database/schema.sql`:n posts-lohkon peilaus fresh-install-polkua varten, ja migraation ajaminen dev-tietokantaan.

Purpose: `author_id` on koko Phase 12:n omistajuuslogiikan (AUTHOR-02 muokkausrajaus, AUTHOR-04 hevoslinkitys omistajuuden alla) enabling-artefakti. Ilman saraketta ja backfillia posts.php:n omistajuussuodatus ei voi toimia. Tämä on [BLOCKING]-perusta plan 12-02:lle — sarakkeen on oltava olemassa ja backfillattu ENNEN kuin omistajuuskäyttäytymistä voi verifioida.
Output: Migraatiotiedosto, päivitetty schema.sql, ja dev-DB:hen ajettu sarake + backfill.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sis-lt-tyyppien-roolirajaus/12-CONTEXT.md
@.planning/phases/12-sis-lt-tyyppien-roolirajaus/12-PATTERNS.md
@database/migrate_roles.sql
@database/schema.sql
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Luo migraatiotiedosto ja peilaa muutos schema.sql:ään</name>
  <files>database/migrate_posts_author.sql, database/schema.sql</files>
  <read_first>
    - database/migrate_roles.sql — migraatiokonventio: kommenttiheader-lohko (===-viivat), suomenkielinen selitys, "Aja phpMyAdminissa: Import → valitse tämä tiedosto" -ohje, eksplisiittinen ALTER + eksplisiittinen UPDATE.
    - database/schema.sql rivit 244-282 — admin_users-lohko (rivi 245, sisältää id INT UNSIGNED AUTO_INCREMENT PK), posts-lohko (rivit 261-270, EI vielä author_id-saraketta), post_horses-lohko (rivit 275-282) FK-konventioesimerkkinä (CONSTRAINT fk_ph_post ... REFERENCES posts(id) ON DELETE CASCADE).
    - database/schema.sql rivit 1-9 — tiedoston header ja SET FOREIGN_KEY_CHECKS = 0.
  </read_first>
  <action>
    Luo `database/migrate_posts_author.sql` migrate_roles.sql:n rakennemallilla: (1) kommenttiheader-lohko ===-viivoin joka selittää tarkoituksen (posts.author_id lisäys + backfill) ja sisältää phpMyAdmin-import-ohjeen samalla sanamuodolla kuin migrate_roles.sql; (2) `ALTER TABLE \`posts\` ADD COLUMN \`author_id\` INT UNSIGNED DEFAULT NULL COMMENT 'Postauksen tekijä (admin_users.id)' AFTER \`content\`, ADD CONSTRAINT \`fk_posts_author\` FOREIGN KEY (\`author_id\`) REFERENCES \`admin_users\`(\`id\`) ON DELETE SET NULL;` — sarake on nullable ja FK ON DELETE SET NULL, jotta käyttäjän pysyvä poisto (USER-04) ei poista hänen postauksiaan vaan nollaa niiden author_id:n; (3) backfill D-01:n mukaan: `UPDATE \`posts\` SET \`author_id\` = (SELECT \`id\` FROM \`admin_users\` WHERE \`username\` = 'admin') WHERE \`author_id\` IS NULL;` — kaikki olemassa olevat postaukset admin-tunnukselle.

    Peilaa sama sarake `database/schema.sql`:n posts-lohkoon (rivit 261-270): lisää `\`author_id\` INT UNSIGNED DEFAULT NULL COMMENT 'Postauksen tekijä (admin_users.id)'` content-sarakkeen jälkeen, ja lisää CONSTRAINT-rivi `CONSTRAINT \`fk_posts_author\` FOREIGN KEY (\`author_id\`) REFERENCES \`admin_users\` (\`id\`) ON DELETE SET NULL` PRIMARY KEY / UNIQUE KEY -rivien joukkoon post_horses-lohkon fk-konvention mukaisesti. Tämä seuraa projektin vahvistettua konventiota, jossa migrate_roles.sql:n role/is_active-muutokset on jo peilattu schema.sql:n admin_users-lohkoon (rivit 248-251) — migraatiot EIVÄT ole vain migration-only tässä projektissa. FK on kelvollinen koska admin_users (rivi 245) määritellään ENNEN posts-taulua (rivi 261) ja SET FOREIGN_KEY_CHECKS = 0 on voimassa.
  </action>
  <acceptance_criteria>
    - Tiedosto `database/migrate_posts_author.sql` on olemassa ja sisältää stringin `ADD COLUMN` sekä `author_id`.
    - Migraatio sisältää FK-määrittelyn: grep löytää `fk_posts_author` ja `REFERENCES` + `admin_users` migraatiotiedostosta.
    - Migraatio sisältää backfill-UPDATE:n: grep löytää `UPDATE` + `posts` + `SET` + `author_id` ja alikyselyn `username` = 'admin' migraatiotiedostosta.
    - `database/schema.sql`:n posts-lohko sisältää nyt `author_id`-sarakkeen: `grep -n author_id database/schema.sql` palauttaa vähintään yhden osuman posts-lohkon alueelta (rivit ~261-272).
    - Migraatiotiedoston headerissa on phpMyAdmin-import-ohje (grep löytää `phpMyAdmin`).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "ADD COLUMN" database/migrate_posts_author.sql && grep -q "fk_posts_author" database/migrate_posts_author.sql && grep -q "author_id" database/schema.sql && echo OK</automated>
  </verify>
  <done>Migraatiotiedosto ja schema.sql-peilaus valmiit; kumpikin sisältää author_id-sarakkeen, FK:n admin_users.id:hen ja backfill-lauseen.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aja migraatio dev-tietokantaan ja verifioi sarake + backfill</name>
  <files>database/migrate_posts_author.sql</files>
  <read_first>
    - docker-compose.yml rivit 23-46 — db-palvelu: kontin nimi `virtuaalitalli-db`, MYSQL_DATABASE=${DB_NAME:-virtuaalitalli}, MYSQL_USER=${DB_USER:-vt_user}, MYSQL_PASSWORD=${DB_PASS:-vt_password}.
    - database/migrate_posts_author.sql — Task 1:ssä luotu tiedosto joka ajetaan.
  </read_first>
  <action>
    Aja Task 1:n migraatio käynnissä olevaan dev-DB-konttiin (`virtuaalitalli-db`, todettu käynnissä ja healthy) ei-interaktiivisesti docker exec -stdin-putkella: syötä `database/migrate_posts_author.sql` mysql-clientille tunnuksilla vt_user / vt_password tietokantaan `virtuaalitalli`. Tämä on [BLOCKING]-vaihe — plan 12-02:n omistajuussuodatuksen verifiointi riippuu siitä, että sarake on olemassa JA backfillattu, ei pelkästä SQL-tiedoston olemassaolosta levyllä.

    Jos docker exec -ajo epäonnistuu (kontti ei käytössä ympäristössä jossa executor ajaa), älä jää jumiin: kirjaa SUMMARY:yn eksplisiittinen manuaaliohje operaattorille (phpMyAdmin localhost:8081 → Import → valitse database/migrate_posts_author.sql) ja merkitse migraation ajaminen odottavaksi. Migraatiotiedosto on additiivinen (ADD COLUMN + UPDATE), ei tuhoa dataa.

    Verifioi ajon jälkeen: (a) sarake on olemassa — `SHOW COLUMNS FROM posts LIKE 'author_id'` palauttaa rivin; (b) backfill onnistui — olemassa olevista postauksista ei jää yhtään NULL author_id -riviä.
  </action>
  <acceptance_criteria>
    - Komento `docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -N -e "SHOW COLUMNS FROM posts LIKE 'author_id'"` tulostaa rivin jossa on `author_id` (sarake on tietokannassa).
    - Komento `docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -N -e "SELECT COUNT(*) FROM posts WHERE author_id IS NULL"` tulostaa `0` (kaikki olemassa olevat postaukset backfillattu, D-01) — HUOM jos posts-taulu on tyhjä dev-DB:ssä, tulos on myös 0, mikä täyttää kriteerin.
    - FK on voimassa — `docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -N -e "SELECT COUNT(*) FROM information_schema.KEY_COLUMN_USAGE WHERE TABLE_NAME='posts' AND CONSTRAINT_NAME='fk_posts_author'"` tulostaa vähintään `1`.
    - Jos ajo ei ollut mahdollista, SUMMARY sisältää eksplisiittisen manuaalisen ajo-ohjeen ja migraation tila on merkitty odottavaksi (ei hiljaista ohitusta).
  </acceptance_criteria>
  <verify>
    <automated>docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -N -e "SELECT COUNT(*) FROM posts WHERE author_id IS NULL" 2>/dev/null | grep -qx 0 && echo BACKFILL_OK</automated>
  </verify>
  <done>Sarake posts.author_id on dev-DB:ssä, backfill ajettu (0 NULL-riviä olemassa olevista postauksista), FK fk_posts_author voimassa. Tai: ajo dokumentoitu odottavaksi eksplisiittisin manuaaliohjein jos kontti ei ollut käytettävissä.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operaattori/CLI → dev-tietokanta | DDL-migraatio ajetaan tietokantaan; virheellinen kohde tai tuhoava lause voisi menettää dataa |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01-01 | Tampering | migrate_posts_author.sql (ALTER/UPDATE) | low | mitigate | Vain additiivinen ADD COLUMN + backfill-UPDATE; ei DROP/DELETE. FK ON DELETE SET NULL suojaa postaukset käyttäjän poistolta. Verifioidaan Task 2:n COUNT-tarkistuksin ennen 12-02:ta. |
| T-12-01-02 | Denial of Service | FK fk_posts_author | low | accept | FK viittaa admin_users(id):hen joka on jo olemassa ennen posts-taulua; SET FOREIGN_KEY_CHECKS=0 fresh-installissa estää järjestysongelmat. |
| T-12-SC | Tampering | npm/pip/cargo-asennukset | low | accept | Tässä vaiheessa ei asenneta paketteja (pelkkä SQL + PHP-muokkaus). Ei package-legitimacy-riskiä. |
</threat_model>

<verification>
- database/migrate_posts_author.sql on olemassa, sisältää ADD COLUMN author_id + FK fk_posts_author + backfill-UPDATE.
- database/schema.sql posts-lohko peilaa author_id-sarakkeen.
- Dev-DB:ssä posts.author_id-sarake on olemassa ja 0 NULL-riviä olemassa olevista postauksista (tai manuaaliajo dokumentoitu).
</verification>

<success_criteria>
- posts.author_id -sarake olemassa dev-DB:ssä FK:lla admin_users.id:hen.
- Kaikki olemassa olevat postaukset backfillattu admin-tunnukselle (0 NULL author_id -riviä).
- schema.sql päivitetty fresh-install-polkua varten.
</success_criteria>

## Artifacts this phase produces

Tämän suunnitelman (12-01) tuottamat uudet/muutetut symbolit:

- **`database/migrate_posts_author.sql`** (uusi tiedosto) — migraatio joka lisää author_id-sarakkeen ja backfillaa olemassa olevat postaukset.
- **`posts.author_id`** (uusi sarake) — INT UNSIGNED DEFAULT NULL dev-tietokannassa ja schema.sql:ssä.
- **`fk_posts_author`** (uusi FK) — posts.author_id → admin_users(id) ON DELETE SET NULL.
- **`database/schema.sql`** (muokattu) — posts-lohkoon peilattu author_id-sarake + FK fresh-install-polkua varten.

(Koko Phase 12:n muut artefaktit — posts.php-omistajuuslogiikka ja requireOwnResourceOrAdmin() — tuotetaan suunnitelmassa 12-02.)

<output>
Create `.planning/phases/12-sis-lt-tyyppien-roolirajaus/12-01-SUMMARY.md` when done
</output>
