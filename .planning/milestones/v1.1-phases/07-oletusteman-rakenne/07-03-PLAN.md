---
phase: 07-oletusteman-rakenne
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - public/themes/default/pages/hevonen.php
autonomous: true
requirements: [THEME-06]
must_haves:
  truths:
    - "public/themes/default/pages/hevonen.php on olemassa puhtaana yksittäisen hevosen profiilisivupohjana"
    - "Sivupohja sisältää vain HTML:n + muuttujien tulostuksen + presentaatioapufunktiot — ei SQL-kyselyitä, ei getDB()/getHorsePedigree()-datanhakua, ei resolveThemePath()-teema-override-hookia, ei 404-early-exit-haaroja (D-01)"
    - "404-early-exit-logiikka (lähteen rivit 58-65 ja 69-76) jää kokonaan kontrolleriin public/pages/hevonen.php:hen — sitä EI kopioida sivupohjaan"
    - "Jokainen dynaaminen tuloste käyttää e()-helperia identtisesti lähteen kanssa (XSS-suojaus säilyy)"
    - "Alkuperäinen public/pages/hevonen.php pysyy muuttumattomana (kontrolleri toimii edelleen live-käytössä)"
  artifacts:
    - path: "public/themes/default/pages/hevonen.php"
      provides: "Yksittäisen hevosen profiilisivupohja (hero-banneri, perustiedot, kilpailut, näyttelyt, kuvagalleria+lightbox, varsat, postaukset, sukutaulu)"
      contains: "hero-banner"
      min_lines: 400
  key_links:
    - from: "public/themes/default/pages/hevonen.php"
      to: "public/themes/default/includes/header.php"
      via: "require __DIR__ . '/../includes/header.php'"
      pattern: "require __DIR__ \\. '/\\.\\./includes/header\\.php'"
    - from: "public/themes/default/pages/hevonen.php"
      to: "e() helper"
      via: "XSS-escape jokaisessa dynaamisessa tulosteessa"
      pattern: "e\\("
---

<objective>
Erottele yksittäisen hevosen profiilisivun (`hevonen.php`, 7 sivupohjasta monimutkaisin, 614 riviä lähteessä) HTML-osuus puhtaaksi sivupohjaksi `public/themes/default/pages/hevonen.php`-tiedostoon (THEME-06). Sivupohja sisältää vain HTML:n + muuttujien tulostuksen + kaksi presentaatioapufunktiota; kaikki datanhaku (SQL, `getDB()`, `getHorsePedigree()`), teema-override-hook ja 404-early-exit-haarat jäävät alkuperäiseen `public/pages/hevonen.php`-kontrolleriin ennallaan (D-01/D-02).

Purpose: Oletusteeman sivupohjajoukko täydentyy 5/7 → sivustolla on sisällöllisesti täydellinen hevosprofiilinäkymä teemakansiossa. Phase 8:ssa kontrolleri muutetaan data-only-muotoon, joka delegoi renderöinnin tälle sivupohjalle `resolveThemePath()`:n kautta. Phase 7:ssä sivupohja on rinnakkainen eikä vielä ajettavissa itsenäisesti (odottaa kontrollerin muuttujia — odotettu välitila, D-02).

Output: 1 puhdas HTML-sivupohja, joka viittaa teeman omiin includeihin ja säilyttää lähteen e()-escapeuksen identtisenä.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-oletusteman-rakenne/07-CONTEXT.md
@.planning/phases/07-oletusteman-rakenne/07-PATTERNS.md
@.planning/phases/07-oletusteman-rakenne/07-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Uudet tiedostot jotka TÄMÄ plan luo (drift-verifioinnin ulkopuolelle):

**Uusi tiedosto:**
- `public/themes/default/pages/hevonen.php` (HTML-osuus lähteestä `public/pages/hevonen.php`, rivit 145-614)

**Uudet presentaatioapufunktiot sivupohjassa (kopioitu lähteestä, EI uusia symboleja koodikannassa):** `pedigreeHorseLink()` (lähteen rivit 150-167) ja `pedigreeCell()` (lähteen rivit 339-343) määritellään sivupohjan sisällä kuten lähteessäkin — PHP ei salli funktioiden kaksoismäärittelyä, joten ne kuuluvat template-tiedostoon, eivät jaettuun helpers.php:hen.

**Kontrollerin toimittamat muuttujat, joita template kuluttaa (jo olemassa lähdekontrollerissa):** `$horse` (koko rivi liitostietoineen), `$id`, `$competitions`, `$showrecords`, `$photos`, `$foals`, `$horsePosts`, `$pedigree`. Template-paikalliset johdannaiset: `$genderFi`, `$heroPhoto`, `$heroStyle` sekä sukutaulun purkumuuttujat.
</artifacts_this_phase_produces>

<split_convention>
Tämä plan noudattaa 07-02-PLAN.md:n `<split_convention>`-lohkon sääntöjä identtisesti. Tiivistelmä + tämän sivun erityispiirteet:

**Datanhaku (JÄÄ kontrolleriin `public/pages/hevonen.php`, EI kopioida sivupohjaan):** lähteen rivit 1-143 — `require db.php`/`theme.php`, teema-override-hook (rivit 5-11, `resolveThemePath('hevonen.php')`), `getDB()`, `$contactCols`/`$contactJoins`, hevoshaku slug/id:llä (rivit 23-57), kilpailut/näyttelyt/kuvat/varsat/postaukset-kyselyt (rivit 81-140), `$pedigree = getHorsePedigree($id)` (rivi 143), sekä `$id`/`$page_title`-asetukset (rivit 78-79).

**404-early-exit (JÄÄ kontrolleriin, ERITYISHUOMIO):** lähteen rivit 58-65 (`else`-haara, slug/id puuttuu) ja rivit 69-76 (`if (!$horse)`) tekevät oman mini-header/footer-renderin ja `exit`. Nämä renderöityvät ENNEN normaalia split-rajaa (rivi 145) → jäävät kokonaan kontrolleriin, EI kopioida sivupohjaan.

**Sivupohjaan (`themes/default/pages/hevonen.php`) kopioidaan:** lähteen rivi 145 (`require header.php`) alkaen EOF:iin (rivi 614, footer-include). Tähän kuuluu: header-include, `$genderFi`-lookup (rivi 147), `pedigreeHorseLink()`-funktio (150-167), `$heroPhoto`/`$heroStyle` (170-173), koko HTML-body (hero-banneri, perustiedot, kilpailut, näyttelyt, kuvagalleria + lightbox, varsat, postaukset), sukutaulun purkulohko + `pedigreeCell()`-funktio (n. rivit 330-343), inline-`<script>`-lohkot (lightbox, rivit ~595-612) ja footer-include.

**Header/footer-include-polut sivupohjassa:** muuta lähteen `require __DIR__ . '/../src/includes/header.php'` (rivi 145) muotoon `require __DIR__ . '/../includes/header.php'` ja `require __DIR__ . '/../src/includes/footer.php'` (rivi 614) muotoon `require __DIR__ . '/../includes/footer.php'`. Tämä on ainoa sallittu poikkeama lähteen tekstistä.

**XSS-suojaus (ei-neuvoteltava, CONTEXT rivi 74):** jokainen dynaaminen tuloste säilyttää lähteen `e()`-kääreen identtisenä, mukaan lukien `e()`-kutsut `pedigreeHorseLink()`-funktion sisällä (rivit 154, 159, 162-164) ja `e(UPLOADS_URL . $heroPhoto)` (rivi 172). ÄLÄ poista, lisää tai muuta yhtään `e()`-kutsua. `filter_var(...)`-URL-validointi (rivi 153) säilytetään myös identtisenä.

**`$page_title`:** lähteessä `$page_title = $horse['name']` (rivi 79) on dynaaminen (dataan sidottu) → jää kontrolleriin. ÄLÄ aseta staattista literaalia sivupohjaan tälle sivulle.
</split_convention>

<tasks>

<task type="auto">
  <name>Task 1: hevonen.php-sivupohja (yksittäisen hevosen profiili)</name>
  <files>public/themes/default/pages/hevonen.php</files>
  <read_first>
    - public/pages/hevonen.php (lähde, 614 riviä — data + 404 + teema-hook rivit 1-143, template rivit 145-614; `$genderFi` rivi 147; `pedigreeHorseLink()` rivit 150-167; `$heroPhoto`/`$heroStyle` rivit 170-173; `pedigreeCell()` rivit 339-343; footer rivi 614)
    - public/themes/default/includes/header.php (teeman header, Plan 01)
    - .planning/phases/07-oletusteman-rakenne/07-PATTERNS.md (`public/themes/default/pages/hevonen.php`-osio, rivit 89-97 — split boundary, in-template helper -maininta, 404-pattern)
    - .planning/phases/07-oletusteman-rakenne/07-02-PLAN.md (`<split_convention>`-lohko, rivit 77-89 — yhteinen erottelusääntö)
  </read_first>
  <action>
    Luo tiedosto `public/themes/default/pages/hevonen.php` (hakemisto `public/themes/default/pages/` on jo luotu Plan 07-02:ssa; jos ei, luo se).
    Kopioi lähteen `public/pages/hevonen.php` rivi 145 (`require ... header.php`) alkaen EOF:iin (rivi 614, footer-include) sivupohjaan sellaisenaan.
    ÄLÄ kopioi rivejä 1-143: teema-override-hook (rivit 5-11), `getDB()`, `$contactCols`/`$contactJoins`, hevos-/kilpailu-/näyttely-/kuva-/varsa-/postauskyselyt, `$pedigree = getHorsePedigree($id)` (rivi 143) sekä molemmat 404-early-exit-haarat (rivit 58-65 ja 69-76) — kaikki nämä jäävät kontrolleriin.
    Muuta header-include (lähteen rivi 145) muotoon `require __DIR__ . '/../includes/header.php'` ja footer-include (lähteen rivi 614) muotoon `require __DIR__ . '/../includes/footer.php'`.
    Säilytä sivupohjassa presentaatiolohkot identtisinä: `$genderFi`-lookup (rivi 147), `pedigreeHorseLink()`-funktio (rivit 150-167), `$heroPhoto`/`$heroStyle` (rivit 170-173), sukutaulun purkumuuttujat + `pedigreeCell()`-funktio (n. rivit 330-343). Nämä ovat esitysmuotoilua, eivät datanhakua, ja ne on lähteessä määritelty header-includen jälkeen → kuuluvat templateen.
    Säilytä koko HTML-body (hero-banneri, perustiedot, kilpailu-/näyttelytaulukot, kuvagalleria + lightbox-modal, varsalista, postauslista, sukutaulu `ped-tree`) ja inline-`<script>`-lohkot (lightbox openLightbox/closeLightbox, rivit ~595-612) sellaisenaan mukaan lukien jokainen `e()`-kääre ja `filter_var()`-URL-validointi.
    ÄLÄ aseta staattista `$page_title`-literaalia — lähteessä se on dynaaminen (`$horse['name']`, rivi 79) ja jää kontrolleriin.
    Sivupohja kuluttaa kontrollerin muuttujat: `$horse`, `$id`, `$competitions`, `$showrecords`, `$photos`, `$foals`, `$horsePosts`, `$pedigree`.
    ÄLÄ koske alkuperäiseen `public/pages/hevonen.php`:ään.
  </action>
  <acceptance_criteria>
    - `public/themes/default/pages/hevonen.php` on olemassa ja vähintään 400 riviä pitkä.
    - Sivupohja sisältää merkkijonot `hero-banner`, `ped-tree`, `pedigreeHorseLink` ja `pedigreeCell` (HTML-body + apufunktiot kopioitu).
    - Sivupohja EI sisällä datanhakua eikä teema-override-hookia: Grep `getDB\(|->query\(|->prepare\(|->execute\(|->fetchAll\(|getHorsePedigree\(|resolveThemePath\(` sivupohjaan palauttaa 0 osumaa.
    - Sivupohja EI sisällä 404-early-exit-logiikkaa: Grep `http_response_code` palauttaa 0 osumaa.
    - Sivupohja sisältää `require __DIR__ . '/../includes/header.php'` ja `require __DIR__ . '/../includes/footer.php'` (teeman omat includet).
    - Sivupohja EI sisällä merkkijonoa `/../src/includes/`.
    - e()-escapeus säilytetty: sivupohja sisältää `e($horse['name'])` ja `e(horseUrl($h))` (pedigreeHorseLink-funktion sisällä).
    - Alkuperäinen `public/pages/hevonen.php` on muuttumaton (git status ei näytä muutosta sille tiedostolle).
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "$f='public/themes/default/pages/hevonen.php'; if (-not (Test-Path $f)) { exit 1 }; $c=Get-Content $f -Raw; $lines=(Get-Content $f).Count; if ($lines -ge 400 -and $c -match 'hero-banner' -and $c -match 'pedigreeHorseLink' -and $c -match 'pedigreeCell' -and $c -notmatch 'getDB\(|->query\(|->prepare\(|->fetchAll\(|getHorsePedigree\(|resolveThemePath\(' -and $c -notmatch 'http_response_code' -and $c -match [regex]::Escape('/../includes/header.php') -and $c -match [regex]::Escape('/../includes/footer.php') -and $c -notmatch [regex]::Escape('/../src/includes/') -and $c -match [regex]::Escape(\"e(\$horse['name'])\")) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>hevonen.php-sivupohja sisältää hevosprofiilin koko HTML:n + presentaatioapufunktiot (pedigreeHorseLink/pedigreeCell), viittaa teeman includeihin, ei sisällä SQL:ää/teema-hookia/404-haaroja, e()-escapeus säilytetty identtisenä.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB-data → renderöity HTML | Sivupohja tulostaa tietokannasta haettuja arvoja (`$horse`, `$competitions`, `$showrecords`, `$foals`, `$pedigree`) selaimeen |
| kontrolleri → sivupohja | Muuttujat ylittävät data/template-rajan (Phase 8 kytkee; Phase 7:ssä latentti) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Injection (XSS) | hevonen.php-sivupohja | mitigate | Jokainen dynaaminen tuloste säilyttää lähteen `e()`-kääreen identtisenä (helpers.php rivi 9), mukaan lukien `pedigreeHorseLink()`-funktion sisäiset `e()`-kutsut ja `filter_var(...FILTER_VALIDATE_URL)`-tarkistus ulkoisille profiililinkeille (rivi 153); acceptance-Grep varmentaa `e($horse['name'])` ja `e(horseUrl($h))`; e()-kutsun poisto kopioinnissa palauttaisi XSS:n |
| T-07-06 | Tampering | data/template-erottelu | mitigate | Grep-gate varmentaa ettei sivupohjaan jää SQL/`getDB()`/`getHorsePedigree()`-datanhakua, `resolveThemePath()`-teema-hookia eikä `http_response_code`-404-logiikkaa (D-01); estää tahattoman logiikkavuodon templateen |
| T-07-SC | Tampering | pakettiasennukset | N/A | Ei pakettiasennuksia (pelkkä tiedosto-erottelu) |
</threat_model>

<verification>
- `public/themes/default/pages/hevonen.php` on olemassa (≥400 riviä).
- 0 osumaa datanhaku-/teema-hook-/404-grepille (`getDB(`, `->prepare(`, `->fetchAll(`, `getHorsePedigree(`, `resolveThemePath(`, `http_response_code`).
- Viittaa teeman includeihin (`/../includes/`), ei vanhoihin (`/../src/includes/`).
- e()-avainkentät läsnä (`e($horse['name'])`, `e(horseUrl($h))`).
- git status: alkuperäinen `public/pages/hevonen.php` muuttumaton.
</verification>

<success_criteria>
- `public/themes/default/pages/hevonen.php` on olemassa puhtaana HTML+muuttuja-templatena presentaatioapufunktioineen (THEME-06, 5/7 sivua).
- Sivupohja ei sisällä datanhakua, teema-override-hookia eikä 404-early-exit-logiikkaa (D-01) — nämä jäävät kontrolleriin.
- XSS-suojaus (e()) säilyy identtisenä lähteen kanssa jokaisessa tulosteessa.
- Alkuperäinen kontrolleri toimii edelleen live-käytössä muuttumattomana.
</success_criteria>

<output>
Create `.planning/phases/07-oletusteman-rakenne/07-03-SUMMARY.md` when done
</output>
