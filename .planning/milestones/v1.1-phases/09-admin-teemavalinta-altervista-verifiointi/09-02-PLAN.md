---
phase: 09-admin-teemavalinta-altervista-verifiointi
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: []
autonomous: false
requirements: [THEME-12]

must_haves:
  truths:
    - "In production (Altervista, path /demotalli-02/), the default theme renders correctly on all public pages after the FTP deploy"
    - "In production, the default theme style.css is served with Content-Type text/css (verified via browser DevTools Network tab, not curl — D-07)"
    - "In production, a direct URL to a theme template (themes/default/pages/index.php) does NOT execute — it is denied (403/forbidden) by the deployed pages/.htaccess"
    - "In production, browsing the themes/ directory URL does NOT list its contents (Options -Indexes inherited from root .htaccess, D-02)"
  artifacts: []
  key_links:
    - "The FTP deploy (deploy.yml, SamKirkland/FTP-Deploy-Action) publishes the whole public/ tree on push to main, so the new pages/.htaccess reaches production with no separate deploy step"
    - "THEME_URL is built on SITE_URL which carries the /demotalli-02/ production prefix — theme CSS/asset URLs must resolve under that prefix"
---

<objective>
Verify (THEME-12) that the theme system works end-to-end in the Altervista production environment after the Phase 9 `.htaccess` from plan 09-01 is deployed: CSS MIME type, theme rendering under the `/demotalli-02/` URL prefix, the `themes/` directory not being browsable, and direct theme-template access being denied.

Purpose: Everything upstream (theme shim, default theme, data-only controllers, admin selection, pages/.htaccess) has only ever been exercised locally on Docker. Altervista's Apache config, MIME handling for `themes/*/assets/css/`, and subdirectory URL prefixing are the one unverified surface (STATE.md blocker). This is a one-time manual check per D-06/D-07/D-08 — verify default theme only, use browser DevTools for MIME, and produce no separate verification-document artifact.

Output: A human-confirmed production verification (recorded only in this plan's SUMMARY, per D-08 — no VERIFICATION.md-style checklist file).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-teemavalinta-altervista-verifiointi/09-CONTEXT.md
@.github/workflows/deploy.yml
</context>

## Artifacts this phase produces

This plan produces no code or file artifacts (D-08: no separate verification document). Its only output is the human-verify confirmation recorded in `09-02-SUMMARY.md`. No new symbols, functions, classes, or files are created.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify theme system in Altervista production (THEME-12, D-06/D-07/D-08)</name>
  <files>(none — verification only, no files modified per D-08)</files>
  <action>
    Confirm the latest main (including plan 09-01's pages/.htaccess) is pushed and the GitHub Actions FTP deploy has completed green, then pause and hand off to the human to run the four production checks in `<how-to-verify>` against the Altervista `/demotalli-02/` site. This is a blocking human-verify checkpoint per D-06/D-07/D-08 — verify the default theme only, use browser DevTools Network for the CSS MIME check (not curl), and record the outcome only in the plan SUMMARY (no separate verification document).
  </action>
  <read_first>
    - .planning/phases/09-admin-teemavalinta-altervista-verifiointi/09-CONTEXT.md — decisions D-02 (root Options -Indexes inherited), D-06 (verify default theme only), D-07 (CSS MIME via DevTools Network, not curl), D-08 (no separate verification artifact)
    - .github/workflows/deploy.yml — FTP deploy config (SamKirkland/FTP-Deploy-Action, push main trigger, server-dir /demotalli-02/); confirms the new pages/.htaccess ships automatically
    - public/.htaccess — root config confirming Options -Indexes and the /demotalli-02/ rewrite prefix used in production
  </read_first>
  <what-built>
    Plan 09-01 added `public/themes/default/pages/.htaccess` (Deny direct HTTP to theme templates). Combined with the pre-existing default theme, data-only controllers, and the admin theme selector in settings.php, the full v1.1 theme system is now complete. The FTP deploy in `deploy.yml` publishes `public/` to Altervista (`/demotalli-02/`) on push to main, so the new `.htaccess` reaches production automatically. This checkpoint confirms the system works in the real Altervista environment — the one surface never testable on local Docker.
  </what-built>
  <how-to-verify>
    Prerequisite: ensure the latest `main` (including plan 09-01's commit) has been pushed and the GitHub Actions FTP deploy has completed successfully (green run). Then, against the production Altervista URL (the SITE_URL site under the `/demotalli-02/` path):

    1. CSS MIME (D-07): Open the production home page in a browser. Open DevTools → Network tab, reload, and click the default theme's `style.css` request (under `themes/default/assets/css/`). Confirm its response `Content-Type` header is `text/css`. Do NOT use `curl -I` for this — DevTools Network is the required method (D-07).
    2. Theme rendering (D-06, default theme only): Click through the public pages — home, hevoset (horse list), a single horse profile, kasvatus, yhteystiedot, ajankohtaista (blog list), a single blog post. Confirm each renders with correct styling and no broken asset/URL paths under the `/demotalli-02/` prefix.
    3. Directory listing (D-02): Visit the `themes/` directory URL directly (e.g. `.../demotalli-02/themes/`). Confirm the browser does NOT show a file/directory listing (403 or the site's default response) — Options -Indexes is inherited from the root `.htaccess`.
    4. Direct template access (verifies 09-01's .htaccess in production): Visit a theme template directly (e.g. `.../demotalli-02/themes/default/pages/index.php`). Confirm it is DENIED (403 Forbidden) and does NOT execute or dump PHP notices.

    Per D-08, do not create a separate checklist/VERIFICATION artifact — a single pass through these steps is sufficient. If all four pass, approve. If any fails, describe which step and the observed behavior so a follow-up gap-closure plan can be scoped.
  </how-to-verify>
  <acceptance_criteria>
    - Production default theme `style.css` response `Content-Type` is `text/css` (observed in DevTools Network)
    - All 7 default-theme public page types render correctly under `/demotalli-02/` with no broken asset paths
    - Direct request to `themes/` directory URL returns no directory listing (403 or default response)
    - Direct request to `themes/default/pages/index.php` returns 403 Forbidden (template not executed)
  </acceptance_criteria>
  <resume-signal>Type "approved" if all four checks pass, or describe which step failed and the observed behavior.</resume-signal>
  <verify>
    <human-check>All four production checks (CSS text/css MIME via DevTools, default theme pages render under /demotalli-02/, themes/ not browsable, direct template access 403) confirmed passing by the human.</human-check>
  </verify>
  <done>Human confirms all four production checks pass; result recorded in 09-02-SUMMARY.md; no separate verification artifact created (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → Altervista themes/ directory | Direct URL browse attempt against the deployed theme tree |
| public internet → deployed theme templates | Direct HTTP request to production `themes/default/pages/*.php` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Information Disclosure | Altervista themes/ directory listing | low | mitigate | Root `.htaccess` `Options -Indexes` inherited into themes/ (D-02); verified in production (step 3) |
| T-09-01P | Information Disclosure | production themes/default/pages/*.php direct access | medium | mitigate | pages/.htaccess (plan 09-01) deployed via FTP; verified returns 403 in production (step 4) |
</threat_model>

<verification>
- FTP deploy of the latest main (with 09-01's .htaccess) completed successfully.
- Production checks pass: CSS Content-Type text/css (DevTools), default theme pages render under /demotalli-02/, themes/ not browsable, direct template access denied (403).
- No separate verification artifact created (D-08) — result recorded only in the plan SUMMARY.
</verification>

<success_criteria>
- THEME-12 satisfied: the theme system is confirmed working in Altervista production (MIME, URL paths, directory protection, template-access denial) for the default theme.
</success_criteria>

<output>
Create `.planning/phases/09-admin-teemavalinta-altervista-verifiointi/09-02-SUMMARY.md` when done (record the human-verify result; no separate verification document per D-08).
</output>
