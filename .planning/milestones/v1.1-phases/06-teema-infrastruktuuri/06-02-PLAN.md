---
phase: 06-teema-infrastruktuuri
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - public/src/includes/theme.php
  - public/pages/index.php
autonomous: true
requirements: [THEME-01, THEME-03]
must_haves:
  truths:
    - "Julkinen sivu (index.php) saa THEME_PATH- ja THEME_URL-vakiot theme.php-shimistä"
    - "resolveThemePath('pages/index.php') tai vastaava palauttaa absoluuttisen palvelinpolun default-teemaan (fallback) tai false"
    - "resolveThemePath('../../etc/passwd') palauttaa false (path-traversal estetty)"
    - "Admin-sivu ei lataa theme.php:tä — THEME_PATH ei ole määritelty admin-kontekstissa"
    - "D-04: resolveThemePath()-turvallisuus toteutetaan preg_match allowlist → realpath() → str_starts_with prefix-check järjestyksessä (ei pelkkä regex, ei strpos)"
  artifacts:
    - path: "public/src/includes/theme.php"
      provides: "THEME_PATH/THEME_URL/THEMES_ROOT-vakiot ja resolveThemePath()-funktio"
      contains: "function resolveThemePath"
      min_lines: 30
    - path: "public/pages/index.php"
      provides: "Shimin integraatiotodistus (yksi require_once-rivi)"
      contains: "theme.php"
  key_links:
    - from: "public/pages/index.php"
      to: "public/src/includes/theme.php"
      via: "require_once db.php-requireen jälkeen"
      pattern: "require_once.*theme\\.php"
    - from: "public/src/includes/theme.php"
      to: "settings-taulu (active_theme)"
      via: "getDB() + prepared SELECT"
      pattern: "active_theme"
    - from: "public/src/includes/theme.php"
      to: "public/themes/{teema}/"
      via: "realpath() + str_starts_with prefix-check"
      pattern: "realpath"
---

<objective>
Luo `public/src/includes/theme.php`-shim joka (1) lukee aktiivisen teeman `settings`-taulusta, (2) validoi teemanimen ja määrittelee `THEME_PATH`-, `THEME_URL`- ja `THEMES_ROOT`-vakiot, ja (3) tarjoaa `resolveThemePath(string $subPath): string|false` -funktion path-traversal-suojauksella ja default-fallbackilla. Lisää sitten `public/pages/index.php`:hen yhden `require_once`-rivin integraatiotodistukseksi.

Purpose: Tämä on teemajärjestelmän ydin. Shim on ainoa paikka joka tietää teemoista (helpers.php pysyy teema-agnostisena), ja sen erillisyys takaa admin-eristyksen (D-09): admin-sivut lataavat db.php:n mutta EIVÄT theme.php:tä. resolveThemePath() on path-traversal-suojattu kontrollipiste jota Phase 8 käyttää kaikkien julkisten sivujen renderöintiin.

Output: `public/src/includes/theme.php` (uusi), `public/pages/index.php` (yksi rivi lisätty).

Toteuttaa: THEME-01 (resolveThemePath path-traversal-suojauksella + fallback), THEME-03 (THEME_PATH/THEME_URL-vakiot shimistä; admin ei lataa shimmiä).

Riippuvuus: Plan 06-01 luo `public/themes/default/theme.json` (jotta realpath() resoloi themes-hakemiston, RESEARCH Pitfall 2) ja `active_theme`-rivin DB:hen (RESEARCH Pitfall 3). Siksi wave 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-teema-infrastruktuuri/06-CONTEXT.md
@.planning/phases/06-teema-infrastruktuuri/06-RESEARCH.md
@.planning/phases/06-teema-infrastruktuuri/06-PATTERNS.md
@public/src/includes/config.php
@public/src/includes/db.php
@public/src/includes/helpers.php
@public/pages/index.php
</context>

<artifacts_this_phase_produces>
Symbolit jotka tämä plan luo (drift-verifioinnin ulkopuolelle):

- Uusi tiedosto: `public/src/includes/theme.php`
- Uusi vakio: `THEME_PATH` (absoluuttinen palvelinpolku aktiiviseen teemakansioon, trailing DIRECTORY_SEPARATOR)
- Uusi vakio: `THEME_URL` (selattava URL aktiiviseen teemaan, `SITE_URL . '/themes/' . $themeName . '/'`)
- Uusi vakio: `THEMES_ROOT` (absoluuttinen palvelinpolku `public/themes/`-juureen, trailing DIRECTORY_SEPARATOR)
- Uusi funktio: `resolveThemePath(string $subPath): string|false`
- Muutettu tiedosto: `public/pages/index.php` (yksi require_once-rivi lisätty)

Olemassa olevat symbolit joita käytetään (EI luotu tässä): `getDB()` (db.php), `SITE_URL` (config.php), `settings`-taulu, `active_theme`-rivi (06-01).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Luo theme.php-shim vakioineen ja resolveThemePath()-funktioineen</name>
  <files>public/src/includes/theme.php</files>
  <read_first>
    - public/src/includes/config.php (SITE_URL-vakio + define()/__DIR__-pattern; UPLOADS_DIR/UPLOADS_URL malli THEME_PATH/THEME_URL:lle)
    - public/src/includes/db.php (getDB()-singleton allekirjoitus + if(!defined())-guard-pattern; require_once-ketju)
    - public/src/includes/helpers.php (validate_file_name() preg_match-allowlist + docblock-tyyli; HUOM: theme.php EI saa olla require-kutsussa täällä eikä db.php:ssä)
    - .planning/phases/06-teema-infrastruktuuri/06-RESEARCH.md (Pattern 1: theme.php-shim rakenne rivit 169–237; Pitfalls 1–4 rivit 307–345)
    - .planning/phases/06-teema-infrastruktuuri/06-PATTERNS.md (theme.php-osio rivit 20–84; Shared Patterns rivit 152–188)
  </read_first>
  <behavior>
    - resolveThemePath('pages/index.php'): default-teemassa ei vielä ole pages/index.php → fallback yrittää default-teemaa → tiedosto puuttuu → palauttaa false (ei kaatu)
    - resolveThemePath('theme.json'): tiedosto on olemassa public/themes/default/theme.json → palauttaa absoluuttisen polun joka alkaa THEME_PATH-prefixillä
    - resolveThemePath('../../etc/passwd'): realpath resoloi teemakansion ulkopuolelle → str_starts_with prefix-check epäonnistuu sekä aktiivisessa että defaultissa → palauttaa false
    - resolveThemePath('../../../config.php'): prefix-check epäonnistuu → false (ei vuoda projektin tiedostoja)
    - THEME_PATH, THEME_URL, THEMES_ROOT ovat määritelty shimin latauksen jälkeen
    - active_theme puuttuu DB:stä → fetchColumn() palauttaa false → Elvis-fallback antaa 'default'
    - Teemanimi joka ei läpäise allowlistia (esim. '../evil') → korvataan 'default':lla ennen polun rakentamista
  </behavior>
  <action>
    Luo uusi tiedosto `public/src/includes/theme.php`. Noudata RESEARCH Pattern 1:tä (rivit 169–237) ja PATTERNS-tiedoston shared patterns -osiota.

    Tiedoston header-docblock: kuvaa että tämä on teemashim joka tarjoaa THEME_PATH/THEME_URL/resolveThemePath, ladataan VAIN julkisilla sivuilla db.php:n require_oncen jälkeen, ja sisältää eksplisiittisen varoituksen `ÄLÄ lisää tätä db.php:hen — admin-sivut eivät saa ladata shimmiä` (D-09, RESEARCH Pitfall 4).

    Alustuslohko `if (!defined('THEME_PATH')) { ... }`-guardin sisällä (PATTERNS Constant definition guard):
    (1) Lue aktiivinen teema: `$db = getDB();` → prepared statement `SELECT setting_value FROM settings WHERE setting_key = :k LIMIT 1` placeholderilla `:k` = `'active_theme'` → `$rawTheme = $stmt->fetchColumn() ?: 'default';`. Käytä `?:` (Elvis), EI `??` — PDO fetchColumn() palauttaa `false` puuttuvalle riville (RESEARCH Pitfall 3).
    (2) Validoi teemanimi: `$themeName = preg_match('/^[a-zA-Z0-9_-]+$/', $rawTheme) ? $rawTheme : 'default';` (allowlist, ei pisteitä — PATTERNS rivit 182–188).
    (3) Resolvoi themes-juuri: `$themesRoot = realpath(__DIR__ . '/../../themes');`. Jos `realpath()` palauttaa `false` (themes-hakemistoa ei ole), rakenna fallback-polku `__DIR__ . '/../../themes'`-pohjalta ettei shim kuole (RESEARCH Pitfall 2). Lopullinen THEMES_ROOT-arvo päättyy aina `DIRECTORY_SEPARATOR`-merkkiin.
    (4) `define('THEMES_ROOT', <themesRoot> . DIRECTORY_SEPARATOR);`
    (5) `define('THEME_PATH', THEMES_ROOT . $themeName . DIRECTORY_SEPARATOR);` — trailing DIRECTORY_SEPARATOR on PAKOLLINEN prefix-checkin oikeellisuudelle (RESEARCH Pitfall 1 & anti-pattern rivit 275): ilman sitä `/themes/defaultevil/` matchaisi `/themes/default`-prefixin.
    (6) `define('THEME_URL', SITE_URL . '/themes/' . $themeName . '/');` (RESEARCH rivit 388–395).

    Funktio `resolveThemePath(string $subPath): string|false` (D-03, D-05, D-06; guard-lohkon ULKOPUOLELLA jotta se on aina käytettävissä):
    - Docblock `@param string $subPath` + `@return string|false` helpers.php-tyyliin.
    - Normalisoi separaattorit: `$subPath = ltrim(str_replace(['/', '\\'], DIRECTORY_SEPARATOR, $subPath), DIRECTORY_SEPARATOR);`
    - Aktiivinen teema: `$real = realpath(THEME_PATH . $subPath);` → jos `$real !== false && str_starts_with($real, THEME_PATH)` → `return $real;` (käytä `str_starts_with`, EI `strpos` — RESEARCH anti-pattern rivit 273).
    - Default-fallback: rakenna `$defaultPath = THEMES_ROOT . 'default' . DIRECTORY_SEPARATOR;` → `$realDefault = realpath($defaultPath . $subPath);` → jos `$realDefault !== false && str_starts_with($realDefault, $defaultPath)` → `return $realDefault;`.
    - Muuten `return false;`.

    HUOM PHP-versio (RESEARCH Open Question 1, Assumption A1/A2): `str_starts_with()` ja `string|false` union return type vaativat PHP 8.0+. Kehitysympäristö (Docker) tukee tätä. Pidä molemmat — Altervista-yhteensopivuus varmistetaan Phase 9:ssä.
  </action>
  <verify>
    <automated>cd "C:/Users/annii/Documents/dev/PHP/simgamestable-template-php" && php -l public/src/includes/theme.php && grep -q "function resolveThemePath" public/src/includes/theme.php && grep -q "str_starts_with" public/src/includes/theme.php && grep -q "if (!defined('THEME_PATH'))" public/src/includes/theme.php && grep -q "fetchColumn() ?:" public/src/includes/theme.php && ! grep -q "fetchColumn() ??" public/src/includes/theme.php && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `php -l public/src/includes/theme.php` raportoi `No syntax errors detected`
    - theme.php sisältää `function resolveThemePath(string $subPath): string|false` (tai eksakti signatuuri `function resolveThemePath`)
    - theme.php sisältää `if (!defined('THEME_PATH'))` -guardin
    - theme.php sisältää `define('THEME_PATH'`, `define('THEME_URL'` ja `define('THEMES_ROOT'`
    - theme.php käyttää `str_starts_with(` prefix-checkiin (ei `strpos`)
    - theme.php käyttää `fetchColumn() ?:` (Elvis), EI `fetchColumn() ??`
    - theme.php sisältää docblock-varoituksen ettei sitä saa lisätä db.php:hen
    - Behavior: integraatiotestissä (Task 2) `resolveThemePath('../../etc/passwd')` palauttaa `false` ja `resolveThemePath('theme.json')` palauttaa polun joka alkaa THEME_PATH-prefixillä
  </acceptance_criteria>
  <done>theme.php parsii ilman virheitä, määrittelee kolme vakiota guardin sisällä, tarjoaa path-traversal-suojatun resolveThemePath()-funktion default-fallbackilla, eikä viittaa db.php:hen tai helpers.php:hen lataajana.</done>
</task>

<task type="auto">
  <name>Task 2: Lisää theme.php-integraatiotodistus index.php:hen ja verifioi shim</name>
  <files>public/pages/index.php</files>
  <read_first>
    - public/pages/index.php (nykyinen include-ketju rivit 1–10; require_once db.php on rivillä 2)
    - public/src/includes/theme.php (Task 1:ssä luotu shim — varmista THEME_PATH/resolveThemePath olemassa)
    - .planning/phases/06-teema-infrastruktuuri/06-CONTEXT.md (D-07: vain yksi rivi lisätään, muu logiikka pysyy ennallaan)
    - .planning/phases/06-teema-infrastruktuuri/06-RESEARCH.md (Pattern 3: index.php integration proof rivit 257–267)
  </read_first>
  <action>
    Lisää `public/pages/index.php`:hen TASAN yksi rivi (D-07, RESEARCH Pattern 3): `require_once __DIR__ . '/../src/includes/theme.php';` välittömästi olemassa olevan `require_once __DIR__ . '/../src/includes/db.php';` -rivin (rivi 2) JÄLKEEN, ennen `$page_title`-määrittelyä. Lisää rivin loppuun selventävä kommentti esim. `// Phase 6: teemashim`. ÄLÄ muuta mitään muuta logiikkaa tiedostossa — ei $db-kutsuja, ei kyselyitä, ei renderöintiä. Käytä eksaktia `__DIR__ . '/../src/includes/`-prefixiä kuten olemassa oleva db.php-rivi.

    Verifiointia varten: aja CLI-skripti joka asettaa minimaalisen ympäristön (SITE_URL-env), lataa db.php:n ja theme.php:n, ja tarkistaa että (a) THEME_PATH ja THEME_URL ovat määritelty, (b) `resolveThemePath('theme.json')` palauttaa polun joka alkaa THEME_PATH:lla, (c) `resolveThemePath('../../etc/passwd')` palauttaa false. Tämä vaatii toimivan DB-yhteyden (Docker localhost:8080) tai active_theme-rivin; jos DB ei ole saatavilla CLI:ssä, verifioi vähintään index.php:n include-rivi ja theme.php:n syntaksi, ja dokumentoi manuaalinen selaintarkistus (var_dump THEME_PATH/THEME_URL osoitteessa http://localhost:8080/pages/index.php) SUMMARY:yn.
  </action>
  <verify>
    <automated>cd "C:/Users/annii/Documents/dev/PHP/simgamestable-template-php" && grep -c "require_once __DIR__ . '/../src/includes/theme.php'" public/pages/index.php | grep -q '^1$' && grep -n "require_once __DIR__ . '/../src/includes/db.php'" public/pages/index.php >/dev/null && php -l public/pages/index.php && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `public/pages/index.php` sisältää TASAN yhden `require_once __DIR__ . '/../src/includes/theme.php'` -rivin
    - theme.php-require esiintyy db.php-requiren jälkeen tiedostossa
    - `php -l public/pages/index.php` raportoi `No syntax errors detected`
    - Mikään olemassa oleva logiikka (hevoslaskenta, kyselyt, $page_title) ei ole muuttunut — diff näyttää vain yhden lisätyn rivin
    - Behavior (manuaali tai CLI jos DB saatavilla): http://localhost:8080/pages/index.php -lataus määrittelee THEME_PATH ja THEME_URL; `resolveThemePath('../../etc/passwd')` palauttaa false
  </acceptance_criteria>
  <done>index.php lataa theme.php:n db.php:n jälkeen yhdellä rivillä, sivu latautuu virheittä, ja shim määrittelee teemavakiot julkisessa kontekstissa samalla kun resolveThemePath torjuu path-traversal-syötteet.</done>
</task>

</tasks>

<threat_model>
- **Path traversal / LFI (V5 Input Validation, STRIDE Tampering + Info Disclosure):** `resolveThemePath($subPath)` saa Phase 8:ssa polkukomponentteja; pahantahtoinen syöte kuten `../../etc/passwd` tai `../../../config.php` yrittäisi vuotaa palvelimen tiedostoja. Mitigation: `realpath()` normalisoi polun (resoloi `../`, symlinkit, dekoodatut merkit) ja `str_starts_with($real, THEME_PATH)` -prefix-check (trailing DIRECTORY_SEPARATOR) varmistaa että lopullinen polku pysyy teemakansion sisällä. Sekä aktiivinen että default-fallback tekevät saman tarkistuksen. Verifioitu Task 2:n behavior-testillä (`../../etc/passwd` → false).
- **Theme name injection (V5 Input Validation, STRIDE Tampering):** DB:stä luettu `active_theme`-arvo voisi teoriassa sisältää polkutraversaalimerkkejä. Mitigation: `preg_match('/^[a-zA-Z0-9_-]+$/')` allowlist ENNEN mitään tiedosto-operaatiota; epäkelpo nimi korvataan `'default'`:lla. Null-byte injection (`theme\0name`) torjutaan samalla allowlistilla (null-tavu ei kuulu merkistöön).
- **Admin-eristyksen rikkominen (V4 Access Control):** Jos joku lisää `require_once theme.php`:n db.php:hen, admin-sivut alkavat ladata shimin ja tehdä turhan DB-kyselyn jokaisella admin-pyynnöllä (D-09 rikko). Mitigation: theme.php:n header-docblock sisältää eksplisiittisen varoituksen; theme.php require_once-kutsutaan VAIN public/pages/index.php:stä, ei koskaan db.php:stä tai helpers.php:stä. Verifioitu: theme.php ei esiinny db.php:n require-ketjussa.
- **SQL injection active_theme-luvussa (V5):** Mitigation: prepared statement named placeholderilla (`:k`), ei merkkijonokonkatenaatiota — noudattaa olemassa olevaa SEC-01-patternia.
</threat_model>

<verification>
- `php -l public/src/includes/theme.php` ja `php -l public/pages/index.php` → No syntax errors
- `grep "function resolveThemePath" public/src/includes/theme.php` → osuma
- `grep "str_starts_with" public/src/includes/theme.php` → osuma (ei strpos prefix-checkiin)
- `grep -c "require_once.*theme.php" public/pages/index.php` → 1
- Manuaali (selain, Docker localhost:8080): lataa /pages/index.php, var_dump(THEME_PATH, THEME_URL) → molemmat määritelty; var_dump(resolveThemePath('../../etc/passwd')) → false
- Manuaali (admin-eristys): lataa admin-sivu, var_dump(defined('THEME_PATH')) → false
</verification>

<success_criteria>
- theme.php määrittelee THEME_PATH, THEME_URL ja THEMES_ROOT guardin sisällä, lukee active_theme:n Elvis-fallbackilla
- resolveThemePath() palauttaa absoluuttisen polun olemassa oleville teematiedostoille (aktiivinen → default), `false` puuttuville ja path-traversal-syötteille
- index.php lataa shimin yhdellä rivillä db.php:n jälkeen, muu logiikka muuttumaton
- Admin-sivut eivät lataa theme.php:tä (THEME_PATH ei määritelty admin-kontekstissa)
- THEME-01 ja THEME-03 toteutettu ja verifioitu
</success_criteria>

<output>
Create `.planning/phases/06-teema-infrastruktuuri/06-02-SUMMARY.md` when done
</output>
