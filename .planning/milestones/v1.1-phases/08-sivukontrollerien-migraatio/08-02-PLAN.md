---
phase: 08-sivukontrollerien-migraatio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/pages/yhteystiedot.php
  - public/pages/ajankohtaista.php
  - public/pages/postaus.php
autonomous: true
requirements: [THEME-08, THEME-09]
must_haves:
  truths:
    - "GET /pages/yhteystiedot.php returns HTTP 200 rendering contact info from the default theme template"
    - "GET /pages/ajankohtaista.php returns HTTP 200 rendering the blog listing from the default theme template"
    - "GET /pages/postaus.php?slug=<valid> returns HTTP 200; GET /pages/postaus.php with no slug returns HTTP 404 with no themed body"
    - "None of the 3 controllers contain inline HTML, a header.php require, or a footer.php require"
    - "Each of the 3 controllers ends with a resolveThemePath('pages/X.php') delegation guarded by a 404 fallback"
    - "Each of the 3 controllers carries the identical Model B root-override hook (D-02)"
  artifacts:
    - public/pages/yhteystiedot.php
    - public/pages/ajankohtaista.php
    - public/pages/postaus.php
  key_links:
    - "controller data scope -> require resolveThemePath('pages/X.php') -> public/themes/default/pages/X.php"
    - "postaus.php missing/not-found record -> unified http_response_code(404); exit; (theme-page.php convention)"
---

<objective>
Migrate `yhteystiedot.php` (THEME-08, last of the 5 base pages here), plus the two blog controllers `ajankohtaista.php` and `postaus.php` (THEME-09), to data-only controllers delegating rendering via `require resolveThemePath('pages/X.php')`.

Note on THEME-09 naming: the ROADMAP labels the blog controllers "blogi.php, postaus.php"; the actual on-disk blog-listing controller is `ajankohtaista.php` (renamed) and `postaus.php` is the single-post page. Both are covered here.

Applies both mechanisms per **D-01**: canonical Model B root-override hook (**D-02**) added to all 3, and Malli A delegation as the final line. Old inline HTML deleted per **D-04**; template-owned helpers (`$MONTHS_FI`, static-page `$page_title`, `$postYear`/`$postMonth`) removed per **D-05**. The two inline 404 branches in `postaus.php` are unified to the silent `http_response_code(404); exit;` convention from `theme-page.php` (Claude's Discretion, resolved per PATTERNS).

Purpose: Complete the base-page + blog controllers so a DB theme switch changes appearance with zero controller edits.
Output: 3 migrated controllers; no new persistent symbols.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sivukontrollerien-migraatio/08-CONTEXT.md
@.planning/phases/08-sivukontrollerien-migraatio/08-PATTERNS.md
@.planning/phases/08-sivukontrollerien-migraatio/08-UI-SPEC.md
@public/src/includes/theme.php
@public/pages/theme-page.php
</context>

<artifacts_produced>
No new persistent symbols. Modifies 3 existing files in place:
- `public/pages/yhteystiedot.php` (data-only controller)
- `public/pages/ajankohtaista.php` (data-only controller)
- `public/pages/postaus.php` (data-only controller, unified 404)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migrate yhteystiedot.php to data-only + add Model B hook</name>
  <files>public/pages/yhteystiedot.php</files>
  <read_first>
    - public/pages/yhteystiedot.php (current: only db.php required, settings query + variable extraction lines 4-16, `$page_title` line 18, inline HTML lines 20-90)
    - public/themes/default/pages/yhteystiedot.php (paired template — self-sets `$page_title = 'Yhteystiedot';` line 2; variable contract: `$stable_name`, `$vrl_id`, `$email`, `$nickname`, `$forum_url` — all strings, may be empty)
    - 08-PATTERNS.md section "yhteystiedot.php"
  </read_first>
  <action>
    Add `require_once __DIR__ . '/../src/includes/theme.php';` after the db.php require line.

    Insert the canonical Model B hook after the require_once lines: comment "Jos aktiivisella teemalla on oma yhteystietosivu, käytetään sitä", `$_vt_themeFile = resolveThemePath('yhteystiedot.php');`, guarded by `if ($_vt_themeFile !== false && !str_starts_with(THEME_PATH, THEMES_ROOT . 'default' . DIRECTORY_SEPARATOR)) { require $_vt_themeFile; exit; }` (**D-02**).

    Keep the settings query and the extraction of `$stable_name`, `$vrl_id`, `$email`, `$nickname`, `$forum_url` (current lines 4-16).

    Delete `$page_title = 'Yhteystiedot';` (current line 18) — template self-sets it (**D-05**).

    Delete the inline HTML block: header include through footer include (current lines 20-90) per **D-04**.

    Append the guarded Malli A delegation for `resolveThemePath('pages/yhteystiedot.php')`.
  </action>
  <acceptance_criteria>
    - `grep -c "includes/theme.php" public/pages/yhteystiedot.php` is >= 1
    - `grep -v '^[[:space:]]*//' public/pages/yhteystiedot.php | grep -c 'includes/header.php'` returns 0
    - `grep -v '^[[:space:]]*//' public/pages/yhteystiedot.php | grep -c 'includes/footer.php'` returns 0
    - `grep -c "resolveThemePath('yhteystiedot.php')" public/pages/yhteystiedot.php` returns 1
    - `grep -c "resolveThemePath('pages/yhteystiedot.php')" public/pages/yhteystiedot.php` returns 1
    - `grep -c "page_title" public/pages/yhteystiedot.php` returns 0
    - `docker exec virtuaalitalli-web php -l /var/www/html/pages/yhteystiedot.php` reports "No syntax errors detected"
    - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/pages/yhteystiedot.php` returns 200
  </acceptance_criteria>
  <verify>
    <automated>docker exec virtuaalitalli-web php -l /var/www/html/pages/yhteystiedot.php && test "$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/pages/yhteystiedot.php)" = "200" && test "$(grep -c "resolveThemePath('pages/yhteystiedot.php')" public/pages/yhteystiedot.php)" = "1" && test "$(grep -c 'page_title' public/pages/yhteystiedot.php)" = "0"</automated>
  </verify>
  <done>yhteystiedot.php is data-only: theme.php required, canonical Model B hook, settings extraction, guarded Malli A delegation, no inline HTML / `$page_title`. Page renders at HTTP 200.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ajankohtaista.php (blog listing) to data-only</name>
  <files>public/pages/ajankohtaista.php</files>
  <read_first>
    - public/pages/ajankohtaista.php (current: only db.php required, `$page_title` line 4, `$MONTHS_FI` lines 8-12, filter parsing + query branches + archive query lines 5-53, inline HTML lines 55-135)
    - public/themes/default/pages/ajankohtaista.php (paired template — owns `$MONTHS_FI` lines 3-7, self-sets `$page_title = 'Ajankohtaista';` line 9; variable contract: `$posts` array, `$archive` nested `[yr][mo]=count`, `$yearFilter`, `$monthFilter` ints)
    - 08-PATTERNS.md section "ajankohtaista.php"
  </read_first>
  <action>
    Add `require_once __DIR__ . '/../src/includes/theme.php';` after the db.php require line.

    Insert the canonical Model B hook after the require_once lines: comment "Jos aktiivisella teemalla on oma ajankohtaista-sivu, käytetään sitä", `$_vt_themeFile = resolveThemePath('ajankohtaista.php');`, guarded by the same condition as the other controllers (**D-02**).

    Delete `$page_title = 'Ajankohtaista';` (current line 4) and the `$MONTHS_FI` array (current lines 8-12) — both template-owned (**D-05**).

    Keep the filter-parameter parsing (`$yearFilter`/`$monthFilter`), the query branches producing `$posts`, and the archive query producing `$archive` (current lines 5-53, minus the two removed items).

    Delete the inline HTML block: header include through footer include (current lines 55-135) per **D-04**.

    Append the guarded Malli A delegation for `resolveThemePath('pages/ajankohtaista.php')`.
  </action>
  <acceptance_criteria>
    - `grep -c "includes/theme.php" public/pages/ajankohtaista.php` is >= 1
    - `grep -v '^[[:space:]]*//' public/pages/ajankohtaista.php | grep -c 'includes/header.php'` returns 0
    - `grep -v '^[[:space:]]*//' public/pages/ajankohtaista.php | grep -c 'includes/footer.php'` returns 0
    - `grep -c "resolveThemePath('ajankohtaista.php')" public/pages/ajankohtaista.php` returns 1
    - `grep -c "resolveThemePath('pages/ajankohtaista.php')" public/pages/ajankohtaista.php` returns 1
    - `grep -c "MONTHS_FI" public/pages/ajankohtaista.php` returns 0 (array template-owned)
    - `docker exec virtuaalitalli-web php -l /var/www/html/pages/ajankohtaista.php` reports "No syntax errors detected"
    - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/pages/ajankohtaista.php` returns 200
  </acceptance_criteria>
  <verify>
    <automated>docker exec virtuaalitalli-web php -l /var/www/html/pages/ajankohtaista.php && test "$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/pages/ajankohtaista.php)" = "200" && test "$(grep -c "resolveThemePath('pages/ajankohtaista.php')" public/pages/ajankohtaista.php)" = "1" && test "$(grep -c 'MONTHS_FI' public/pages/ajankohtaista.php)" = "0"</automated>
  </verify>
  <done>ajankohtaista.php is data-only: theme.php required, canonical Model B hook, filter/query/archive logic, guarded Malli A delegation, no inline HTML / `$MONTHS_FI` / `$page_title`. Blog listing renders at HTTP 200.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate postaus.php (single post) to data-only + unify 404</name>
  <files>public/pages/postaus.php</files>
  <read_first>
    - public/pages/postaus.php (current: only db.php required, slug/id fetch lines 4-14, inline-HTML 404 branch lines 15-22 (no param) and lines 26-33 (not found), dynamic `$page_title = $post['title'];` line 35, prev/next/archive lines 37-66, `$MONTHS_FI` lines 69-73, `$postYear`/`$postMonth` lines 96-97, inline HTML lines 75-151)
    - public/themes/default/pages/postaus.php (paired template — owns `$MONTHS_FI` lines 3-7, computes `$postYear`/`$postMonth` from `$post['created_at']` at lines 30-31, requires its own header.php but does NOT set `$page_title`; variable contract: `$post` assoc array (title, content, created_at, slug), `$prev`, `$next` (arrays|false), `$archive` nested array)
    - public/pages/theme-page.php (canonical `http_response_code(404); exit;` convention, lines 7-10 / 23-26 / 29-32)
    - 08-PATTERNS.md section "postaus.php" and "404 handling unification"
  </read_first>
  <action>
    Add `require_once __DIR__ . '/../src/includes/theme.php';` after the db.php require line.

    Insert the canonical Model B hook after the require_once lines: comment "Jos aktiivisella teemalla on oma postaussivu, käytetään sitä", `$_vt_themeFile = resolveThemePath('postaus.php');`, guarded by the same condition (**D-02**).

    Replace BOTH inline-HTML 404 branches (the no-param branch, current lines 15-22, and the not-found branch, current lines 26-33) with the silent unified idiom: `http_response_code(404);` then `exit;` — no `$page_title`, no header/footer include, no echoed body (matching theme-page.php).

    KEEP `$page_title = $post['title'];` (current line 35) — unlike the static-title pages, the postaus template does NOT self-set `$page_title`; it relies on the controller setting it before the template's own header include renders the <title>. Do not remove this line.

    Keep the post fetch, prev/next queries, and archive query (current lines 4-66, minus the replaced 404 branches).

    Delete the `$MONTHS_FI` array (current lines 69-73) and the `$postYear`/`$postMonth` computation (current lines 96-97) — the template owns `$MONTHS_FI` and computes the year/month itself from `$post['created_at']` (**D-05**).

    Delete the remaining inline HTML block: header include through footer include (current lines 75-151) per **D-04**.

    Append the guarded Malli A delegation for `resolveThemePath('pages/postaus.php')`.
  </action>
  <acceptance_criteria>
    - `grep -c "includes/theme.php" public/pages/postaus.php` is >= 1
    - `grep -v '^[[:space:]]*//' public/pages/postaus.php | grep -c 'includes/header.php'` returns 0 (both 404 branches and body HTML removed)
    - `grep -v '^[[:space:]]*//' public/pages/postaus.php | grep -c 'includes/footer.php'` returns 0
    - `grep -c "resolveThemePath('postaus.php')" public/pages/postaus.php` returns 1
    - `grep -c "resolveThemePath('pages/postaus.php')" public/pages/postaus.php` returns 1
    - `grep -c "MONTHS_FI" public/pages/postaus.php` returns 0 (array template-owned)
    - `grep -c 'http_response_code(404)' public/pages/postaus.php` is >= 2 (both branches unified to silent 404)
    - `grep -c "\$page_title = \$post" public/pages/postaus.php` returns 1 (dynamic title kept — template does not self-set it)
    - `docker exec virtuaalitalli-web php -l /var/www/html/pages/postaus.php` reports "No syntax errors detected"
    - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/pages/postaus.php` returns 404 (no slug → silent 404)
    - With a valid slug from the DB, `curl -s -o /dev/null -w '%{http_code}' "http://localhost:8080/pages/postaus.php?slug=<slug>"` returns 200
  </acceptance_criteria>
  <verify>
    <automated>docker exec virtuaalitalli-web php -l /var/www/html/pages/postaus.php && test "$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/pages/postaus.php)" = "404" && SLUG=$(docker exec virtuaalitalli-db mysql -uvt_user -pvt_password virtuaalitalli -N -e "SELECT slug FROM posts LIMIT 1") && test "$(curl -s -o /dev/null -w '%{http_code}' "http://localhost:8080/pages/postaus.php?slug=$SLUG")" = "200" && test "$(grep -c 'MONTHS_FI' public/pages/postaus.php)" = "0"</automated>
  </verify>
  <done>postaus.php is data-only: theme.php required, canonical Model B hook, both 404 branches unified to silent `http_response_code(404); exit;`, dynamic `$page_title` kept, guarded Malli A delegation, no inline HTML / `$MONTHS_FI` / `$postYear`/`$postMonth`. Valid slug renders 200, missing slug returns 404.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → controller | HTTP GET with optional `$_GET['slug']` / `$_GET['id']` (postaus) |
| controller → filesystem | `resolveThemePath()` maps a subpath to a theme file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Tampering | postaus.php `$_GET['slug']`/`$_GET['id']` | low | mitigate | Input handling is UNCHANGED by this phase: slug still `preg_replace('/[^a-z0-9\-]/', ...)`, id still `(int)`, queries stay prepared statements. No new sink. |
| T-08-04 | Tampering | resolveThemePath() path arguments | low | mitigate | Arguments are hardcoded literals (`'postaus.php'`, `'pages/postaus.php'`); no user input reaches the path. theme.php preg_match/realpath/prefix-check unchanged. |
| T-08-05 | Information Disclosure | unified 404 path (postaus + Malli A fallback) | low | mitigate | Silent `http_response_code(404); exit;` with no body — removes the previously-rendered error markup, reducing (not increasing) disclosure surface. |
| T-08-SC | Tampering | package installs (npm/pip/cargo) | low | accept | No package installs — pure PHP edits. |
</threat_model>

<verification>
- All 3 controllers pass `php -l` via the web container.
- yhteystiedot.php and ajankohtaista.php return HTTP 200; postaus.php returns 404 without a slug and 200 with a valid slug.
- No controller contains a header.php or footer.php include (comment-filtered grep 0).
- Each controller has one canonical Model B hook and one guarded Malli A delegation.
- `$MONTHS_FI` / static `$page_title` removed; postaus keeps its dynamic `$page_title`; postaus 404 branches unified to silent 404.
</verification>

<success_criteria>
yhteystiedot.php, ajankohtaista.php, and postaus.php are data-only controllers delegating all rendering to the active theme, with identical Model B hooks, guarded Malli A delegations, unified silent 404 handling in postaus, and correct HTTP behavior (200 for listings/valid post, 404 for missing post).
</success_criteria>

<output>
Create `.planning/phases/08-sivukontrollerien-migraatio/08-02-SUMMARY.md` when done.
</output>
